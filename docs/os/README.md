# OS Subsystems

Each subsystem is a standalone repo at `/home/macro/Repos/{name}/` with its own CLAUDE.md as the authoritative source. Individual module docs have been archived — read the repo's CLAUDE.md for current state.

See [CLAUDE.md Standalone Repos table](../../CLAUDE.md) for the full list with versions and port status.

---

Pre-1.0 OS subsystems in development: [docs/development/os/](../development/os/README.md)
Non-OS domain libraries: [docs/applications/libs/](../applications/libs/)
