# AGNOS Binaries & Tools — Released (v1.0+)

> Standalone v1.0+ binaries and CLI tools in the AGNOS stack — the kernel, shell, init, package manager, build system, and the sovereign userland (coreutils, editors, viewers, terminal-aesthetics tools). Distinct from the reusable **[library crates](libs/README.md)** and from the larger **[consumer applications](README.md)** (Abacus, Aequi, …).
>
> Stdlib-folded distfiles (`sandhi`, `niyama`) are documented under [libs/ § Stdlib-Folded](libs/README.md).
>
> **Verify against each repo's `VERSION` before acting on a single row.**

See also: [libs/](libs/README.md) | [Applications index](README.md)

---

## Binaries & Tools (23)

| Crate | Version | Domain |
|-------|---------|--------|
| [agnos](https://github.com/MacCracken/agnos) | 1.56.40 | AGNOS kernel (Cyrius-native, ELF64 + sovereign UEFI handoff via gnoboot). Cyrius pin drifts per cut — see [state.md](../development/state.md). **Iron-validated on archaemenid (real Zen)** through boot-to-shell with a live multi-command keyboard. Arc history: MVP boot-to-shell (1.30.9) → storage (1.31.x: NVMe/AHCI/USB-MS/RAM-disk/VirtIO + GPT + 5-backend block dispatch) → networking (1.32.x: r8169 GbE + ARP/IPv4/UDP/TCP + DHCP) → ext2/4 + FAT/exFAT write (1.33-34) → comms substrate (1.35: DNS/ICMP/NTP/RTC/mmap) → exec-from-disk + VFS routing (1.40) → **shell→agnoshi** (1.41, iron-validated) → perf + sysinfo/klug (1.42) → graphics + DOOM (1.43, iron-validated) → **multi-threading / preemptive scheduling** (1.44: schedulable `&` jobs, SMP-AP wake+park, per-proc env) → **1.45.x TLS → HTTPS**: ring-3 net/entropy/clock syscalls #45-#57 (getrandom, time_unix, TCP sockets, UDP, ICMP-echo, server sockets) — all of Phase A net-tools kernel-unblocked → 1.46.x SMP → 1.52.x HDA audio → 1.53.x kernel FP/SIMD (⛔ the two-process half retracted 2026-08-03 as a rigged selftest; the FP/SIMD half stands) → **1.54.x GPU compute** → **1.55.x display/scanout + ATOM** (incl. ACPI S5 self-poweroff, iron) → **1.56.x 3D raster, modeset, native scanout + console pan**. ⭐ **2026-08-03, iron:** the aethersafha desktop hosts two real client windows on archaemenid at `cpus online: 4` — 278 frames, keys delivered, clean quit. ⛔ **1.56.40 is an OPEN, unburned cycle** (the local-IPC channel band). Closed-arc ledger + live kernel size → [state.md](../development/state.md). |
| [agnoshi](https://github.com/MacCracken/agnoshi) | 1.8.6 | AI shell (Cyrius) — the userland interactive shell, exec'd from disk in ring 3 (shell-separation arc, iron-validated). 1.5.0 dropped its in-process coreutils verbs → delegates to the **kriya `/bin` dispatcher + owl `cat`**; 1.6.x added `&` background jobs (job table + poll loop, schedulable via agnos 1.44.x); 1.7.0 inherits per-process env; 1.8.0 added two-stage shell pipelines `cmd1 \| cmd2` (store-and-forward over the kernel pipe buffer); 1.8.2 = cyrius 6.3.x migration + test-suite hardening (shared `tests/harness.cyr`; fixed the `verify_sudo_path` stack-smash from the 6.3.13 byte-sized-`var[]` change + a latent `build_safe_env` Str/cstring fault); 1.8.3 `cmd > file` output redirection; 1.8.4 AI stays enabled on agnos; 1.8.5 platform power control (`reboot` / `poweroff` / `halt`); **1.8.6 = a foreground program no longer freezes the scheduler**. Cyrius pin 6.3.34. Real entry is `src/agnsh.cyr` (`src/main.cyr` is dead legacy). |
| [agora](https://github.com/MacCracken/agora) | 1.6.6 | Telnet-served BBS (Greek ἀγορά — *civic marketplace / public assembly*), Cyrius-native. Multi-user, multi-board threaded boards; sigil-backed Ed25519 challenge/response auth; per-board posting policy; single-process poll-multiplex concurrency (was fork-per-connection through 1.5.x); audit-hardened input; frozen ABI; full RFC 854/1143/1073/1091/1184 telnet conformance. **v1.0.0 shipped 2026-05-23 — iron-validated on archaemenid** (telnet round-trip + 8-user concurrent fanout green). **v1.1.0 door-games subsystem** (ADR 0009 — pure-module game logic); **v1.2.0 "Persistent Universe"** (ADR 0010) made all **three door games** shared-world multiplayer — **PA** (TradeWars-lineage space-empire trading), **Smuggler** (contraband running; shared per-district police heat), **The Handler** (spy-runner field-pressure) — via a `flock`'d *lock → read → compute → write* world transaction per game on disk, plus cross-game leaderboards. **Both shipped since**: **1.3.0 chat area + Eliza** (live multi-user teleconference on the same `flock`'d shared-disk discipline; Weizenbaum's DOCTOR as a door + a private `/eliza` side-channel) and **1.4.0 the `descent` door** — the BBS→MUD gateway into cyrius-yeomans-descent. **1.5.0 runs agora on AGNOS**; **1.6.0 replaced fork-per-connection with a single-process poll multiplex** (concurrent players on agnos), hardened through the 1.6.1-1.6.6 audit arc. Server-stage app; anchors the BBS/MUD aesthetic cluster (consumes the network-tools family, kii, anuenue, bnrmr). |
| [anuenue](https://github.com/MacCracken/anuenue) | 1.2.0 | `lolcat`-equivalent — ānuenue (Hawaiian: *rainbow*). Stdin → stdout rainbow-tint pipe filter, Cyrius-native: no file I/O, no network, no fork/exec. Tints each UTF-8 grapheme cluster along an HSV cycle and emits ANSI via **darshana**; honours `NO_COLOR` with byte-identical passthrough, `--color=256` xterm quantization, deterministic `-s` seeding, and an animated mode. Flag surface frozen at v1.0; CLI parsing moved to **cmdit** at 1.1.4. **1.2.0 adds a library surface** — `cyrius distlib` exports the pure HSV phase geometry so a consumer can tint in-process instead of shelling out. Polynesian/Hawaiian micro-cluster with `hapi`, `kii`. Consumers: agora (BBS MOTD), iam splashes, bnrmr banners. |
| [argonaut](https://github.com/MacCracken/argonaut) | 1.8.4 | Init system library (Cyrius) — depends on agnodrm (née agnosys; rewire pending a consumer cut). v1.7.0 adds BOOT_MINIMAL agnoshi-as-no-deps-console-service (unblocks closed-beta MVP without aethersafha). |
| [ark](https://github.com/MacCracken/ark) | 1.4.1 | Package manager (Cyrius). **Graduated to v1.0** (was 0.8.0 pre-1.0) — consumes **mela 1.0** (`mela_fetch_artifact` / `registry_client_new`) to fetch `.ark` packages over mela's transport (the mela v1.0 consumer gate; 263 tests). Deps: nous, sigil, mela. |
| [bannermanor](https://github.com/MacCracken/bannermanor) | 1.1.2 | `figlet`-equivalent ASCII-art banner generator (binary `bnrmr` — vowel-dropped per the `commandress`→`cmdrs` compression pattern). English-wordplay naming lane. Drives login MOTDs, script intros, splash text. **Graduated to v1.0+ 2026-05-20 PM** — CLI flag surface, CYML font format (schema=1), and default in-tree font set (block / slim / big) all frozen as the v1.0 contract. Cyrius pin 6.2.24. Consumers: end-user shells; agnoshi MOTD; iam (logo rendering); darshana, mihi. |
| [commandress](https://github.com/MacCracken/commandress) | 1.1.3 | Structured shell prompt renderer for agnoshi, bash, and zsh via per-shell adapters. Sovereign-stack equivalent of starship, in Cyrius. Binary name `cmdrs` (short for *commandress*). Stateless, segment-based, config-driven, zero non-stdlib deps. **Graduated to v1.0+ 2026-05-18** — segment renderer + config layer stabilized after the 2026-05-15 scaffold. Cyrius pin 6.4.66. Consumers: agnoshi, bash, zsh (prompt-hook adapters). |
| [cyim](https://github.com/MacCracken/cyim) | 1.8.0 | Sovereign modal text editor (Cyrius-native, VIM-inspired, zero attack surface, no embedded scripting). Consumes vyakarana + niyama (regex via stdlib fold v5.9.0); consumers: agnoshi, aethersafha, daimon-orchestrated agents. |
| [cyim-lsp](https://github.com/MacCracken/cyim-lsp) | 1.5.1 | Language Server Protocol companion to cyim (Cyrius-native). Editor-agnostic LSP backend serving cyim's grammar/regex/symbol surfaces over LSP. |
| [darshini](https://github.com/MacCracken/darshini) | 1.3.0 | `eza`-equivalent pretty file listing — colors, icons, git-status column, tree view, mime-type recognition (Sanskrit दर्शिनी — *she who shows*). Sibling-not-competitor to kriya's minimal `ls`; both ship. **Graduated from the terminal-aesthetics "planned" quintet** — was the last of the five queued. Consumes darshana (TTY/ANSI). |
| [hapi](https://github.com/MacCracken/hapi) | 1.0.3 | GNU `stow`-equivalent — dotfile / symlink farm manager. Dual-read name: Hawaiian हपी (*happy*) + **H**ome **A**sset **P**rovisioning **I**nterface; first Pacific Islands word in the AGNOS naming surface. CYML manifest per package, capability-bounded execution (`$HOME` only by default), lightweight audit trail. **Graduated to v1.0+.** Consumers: end-user shells; agnova (dotfile bootstrap); mihi. |
| [iam](https://github.com/MacCracken/iam) | 1.1.5 | fastfetch/neofetch-equivalent system-info display for login MOTD + screenshot flex. Pure inverse of `whoami` — whoami says who the user is, iam says what the system is. Thin presentation layer over the mihi probe library. **Graduated to v1.0+ 2026-05-20** straight to 1.0.0 on cyrius 6.0.1. English-wordplay/trickster naming lane. Consumes mihi 1.2.1 (probe) + darshana (color/TTY). **Renders on agnos (QEMU-validated 2026-07-02)** — 1.1.5 heap-alloc'd its scratch buffers to fit the ~12 KB agnos user stack. |
| [ifran](https://github.com/MacCracken/ifran) | 2.2.0 | **The AGNOS training control plane** (عرفان — *gnosis*; Cyrius port SHIPPED 2026-07-05, Rust line held at `rust-old/`). Orchestration around a training run — never the math: **jobs** (fork+pipe+execve, exit propagation, patra run store) · signed **model store** (tula validate + Ed25519 verify vs the operator key + sha256 content addressing) · **datasets** (content-addressed corpora + REAL exact-line dedup) · **sweeps** (grid + seeded-deterministic random, ~0.6 ms/job overhead) · **evals** (sibling gates as recorded benchmarks + metric extraction) · **preferences** (DPO/KTO store + escaped JSONL export). CLI on **cmdit**. Proven: attn11 trained, anukūlana gated (maxrel captured), tarka ALL-GATES-PASS — entirely as ifran jobs. **2.1.0 (2026-07-05): Lane 1 executor hardening** — `timeout_s` (SIGKILL → `timed-out`), PID-on-row + `reap` (orphans definitely-dead rows only), quoted args, `show <id>` (record + log tail); the cross-repo lanes closed same day (anukūlana 1.1.1 `--sk` → `verified` ingest; tarka 1.1.2 `--pref` ingestion). **2.2.0 (same day): the rust-old pre-removal-audit additives** — sweep leaderboard (`sweep best`) + budgets (`max_trials`/`time_budget_s`), `dataset validate`, 4-state prefs (`tie`/`bothbad`) + pair `conf`; remaining audit items roadmapped with blockers. GPL-3.0-only; suite 120. Deps patra/sigil/tula/cmdit. |
| [kii](https://github.com/MacCracken/kii) | 1.4.1 | `chafa`/`jp2a`/`viu`-equivalent — image → ANSI/ASCII-art converter for terminal display. Four-layered name (Hawaiian *image* + East Asian *ki* 気 *life-force* + back-half of *a-scii* + functional convergence). Reads PNG/JPEG/GIF/BMP, quantizes to a terminal palette + glyph set, emits ANSI sized to cols × rows. **Graduated to v1.0+.** Consumers: bannermanor (logo rendering), iam (system splash), BBS MOTD pipeline, MUD room illustrations, anuenue. |
| [kriya](https://github.com/MacCracken/kriya) | 1.1.7 | Coreutils-equivalent for AGNOS (Sanskrit क्रिया — *action, operation, verb*). One repo, many small static utilities (`cp`, `mv`, `rm`, `mkdir`, `echo`, `wc`, `find`, `grep`, `xargs`, …) sharing infrastructure. BusyBox-style dispatcher + symlinks per utility. **Graduated to v1.0+ 2026-05-18** after M5 closeout (grep + find + xargs). Sovereign-replacement boundaries: owl owns `cat`, cyim owns `vim`, sit owns `git`, chakshu owns `htop`, agnoshi owns shell builtins; kriya fills the gaps. Cyrius pin 6.4.20. Consumers: agnoshi (PATH lookup), zugot (install-time symlinks). |
| [kybernet](https://github.com/MacCracken/kybernet) | 1.4.0 | PID 1 init binary (Cyrius) — depends on argonaut. v1.3.5: edge-boot TPM/dm-verity/LUKS re-sourced from sigil 3.9.0, vestigial agnosys-core dropped (decomposition). **v1.4.0 = toolchain leap to cyrius 6.4.62 + full dependency refresh and a THIN sigil surface** — the PID-1 binary drops 14.35 MB → 0.96 MB (x86_64) / 1.29 MB (aarch64), 177/177 tests, both arches clean; it stops pulling the monolithic `dist/sigil.cyr`. |
| [mela](https://github.com/MacCracken/mela) | 1.0.1 | **App & agent marketplace** — Cyrius-native (Hindi मेला, *fair/marketplace*). **Full Rust→Cyrius port complete + graduated to v1.0** (2026-06-17): all 9 Rust modules (6208 lines) ported across 12 Cyrius modules, **492 parity tests** + fuzzed parsers. Both trust gates enforced end-to-end at `pipeline_install` — Ed25519 publisher signature (RFC 8032 KAT) + SHA-256 integrity, every publication appended to a hash-chained transparency log. Real I/O: HTTP/HTTPS/DNS via **sandhi**, on-disk install (zip-slip-guarded ustar). Ships `dist/mela.cyr` (`[lib]`); **ark consumes it (green)** for `.ark` fetch. Pin 6.2.21. Deps: sigil / agnostik / sankoch / sandhi; consumers ark (fetch), daimon (discovery). |
| [mirshi](https://github.com/MacCracken/mirshi) | 1.11.0 | **AGNOS↔Linux mirror-shim** — a userland syscall-translation supervisor (WSL1/Wine/gVisor pico-process model, **not** kernel-emu/VM): agnos userland runs in a plain Docker container, no QEMU. Per-number handler table (agnos `exit`=0 vs Linux 60, net band #45-57 sovereign, mmap#27 2 MB-granular), root-child argv forwarding, seccomp-notify path. **1.11.0 adds `kbscan#42`** — a synthesized Set-1 scancode stream off its own stdin, so full-screen agnos TUIs (cyim ≥ 1.8.0) that poll per-key instead of line-cooked `read#5` run headless under mirshi. Drives the ecosystem's real userland tools (the agnosticos **mirshi-fanout** vehicle). **Graduated to v1.0+** (was 0.1.0 pre-1.0, scaffolded 2026-06-29). Complements — never replaces — QEMU+iron (owns userland-at-scale + Linux-compat, not kernel-SMP/net). |
| [nous](https://github.com/MacCracken/nous) | 1.3.1 | Package resolver (Cyrius) |
| [owl](https://github.com/MacCracken/owl) | 1.4.4 | Watchful file viewer — `cat`/`bat` replacement (Cyrius-native, **O**bservant **W**atcher of **L**ines). `-p` byte-identical cat drop-in; decorated mode adds token highlighting + paging + a **VCS change-marker gutter via `sit`** (1.4.0). Consumes vyakarana (tokenization) + bayan + sit. **AGNOS-compile note:** owl-on-agnos is blocked by its sit→tls transitive chain — needs the TLS stack adapted for `CYRIUS_TARGET_AGNOS` AND a `sit`→agnos port; the delegation's working owl was 1.3.8 (cat-only, pre-sit). |
| [sit](https://github.com/MacCracken/sit) | 1.3.5 | Sovereign version control — Cyrius-native git replacement (smriti, स्मृति — *memory*). `sit_repo_open`/`sit_diff_path`/`sit_repo_close` library surface + a `wire`/`wire_http`/`serve` https remote layer (which pulls net/tls/tls_native/http). Deps: sankoch (compression), sigil (hashing), patra (object store). No libgit2, no C, no FFI. **Graduated to v1.0+** (was 0.8.5 pre-1.0). Consumers: end user, owl (VCS-gutter), ark. |
| [takumi](https://github.com/MacCracken/takumi) | 1.1.1 | Build system (Cyrius). **Rust→Cyrius port complete + graduated to v1.0** (parity reached; was 0.8.0 in-port). Deps: sigil. |

---

*Versions swept 2026-08-05 against local `VERSION` files — **no membership change, 22 entries** (no binary crossed v1.0 in the window; the two graduations found this sweep, **samay 1.0.1** and **vani 1.1.3**, are libraries and went to [libs/](libs/README.md)). **15 version bumps**: agnos 1.51.8→**1.56.40** · agnoshi→1.8.6 · agora 1.5.0→**1.6.6** · argonaut→1.8.4 · commandress→1.1.3 · cyim 1.7.5→**1.8.0** · darshini 1.2.1→**1.3.0** · hapi→1.0.3 · kii→1.4.1 · kriya→1.1.7 · kybernet 1.3.5→**1.4.0** · mirshi 1.9.0→**1.11.0** · owl→1.4.4 · sit→1.3.5 · takumi→1.1.1. Unchanged: ark 1.4.1, bannermanor 1.1.2, cyim-lsp 1.5.1, iam 1.1.5, ifran 2.2.0, mela 1.0.1, nous 1.3.1. **Pins re-read from each repo's `cyrius.cyml`**: bannermanor 6.0.1→**6.2.24** · commandress 5.11.64→**6.4.66** · kriya 5.11.61→**6.4.20**; the agnos pin moves every cut and is now a pointer to [state.md](../development/state.md). Prior sweep 2026-07-05 (ifran → **2.2.0** same day; **+ifran 2.0.0** — the Tier-A Rust→Cyrius port shipped, 21→22 entries). Prior 2026-07-03 (three-registry audit; +mirshi graduated from pre-1.0, 20→21 entries; agnos→1.51.8, ark 1.0.0→1.4.1, sit→1.3.1, kii→1.4.0, agora→1.5.0, mela→1.0.1, nous→1.3.1, takumi→1.1.0). This table lists v1.0+ crates, which live under `docs/applications/`.*
