---
name: AGNOS Ecosystem State
description: Live cross-repo state — Cyrius cycle, pin-lag spectrum, active sweeps, carry-forward debt
type: state
---

# AGNOS Ecosystem — Current State

> **⚠ NOT A LOG.** This file is **live state with pointers** — current truth only, plus links to where the history lives. Iron attempt history → [`iron-nuc-zen-log.md`](iron-nuc-zen-log.md). Per-repo release history → each repo's `CHANGELOG.md`. Crate versions → the two registry pointers below. If you find yourself writing prose narrative here, it belongs in one of those other files.

> **Cyrius toolchain**: 6.0.1 (v6.0.0 cycle opened 2026-05-19; same-day .1 patch fixed a UEFI-emit `fncallN` regression — see [`issues/2026-05-19-cycc-6.0.0-uefi-fncall-ud2-emit.md`](issues/2026-05-19-cycc-6.0.0-uefi-fncall-ud2-emit.md)). v5.11.x closed at **5.11.69** on 2026-05-19.
> **Last refresh**: 2026-05-25 — **1.32.6 CLOSED (cut, awaiting user tag) → 1.32.7 OPEN**. Theme of both: **r8169 RX delivery of UNICAST frames on iron** (broadcast + multicast PROVEN at **1.32.5 — RELEASED 2026-05-25**). **1.32.6 SHIPPED gateway L2 reachability on iron** (RFC-826 ARP sender-snoop, bite 4): `arp: REPLY gw_mac=d4:6a:91:ce:70:60` → `net: L2 OK -- gateway MAC cached` — first gateway reachability in the entire 1.32.x arc, MAC learned with ZERO unicast RX. **🆕🆕🆕 CURRENT HEAD — 1.32.7 bite-2 (on-LAN unicast-TCP discriminator) LANDED + build-verified, BURN-READY.** bite-1 (IDR re-assert post-`CR.RE`) **BURNED 2026-05-25 → FALSIFIED** ([`1327-agnos-1.32.7-bite1-idr-post-cr-re-reassert-l2-ok-gateway-cached-still-no-synack-unicast-drop-survives.jpg`](iron-nuc-zen-photos/1327-agnos-1.32.7-bite1-idr-post-cr-re-reassert-l2-ok-gateway-cached-still-no-synack-unicast-drop-survives.jpg), phone-label `1327_TCP_Failure`): FB byte-identical to the bite-4/6 burns — `arp: REPLY gw_mac=212:106:145:206:112:96` → `net: L2 OK -- gateway MAC cached` → `tcp: connect 1.1.1.1:80` → `net: L3+TCP FAIL -- SYN sent but no SYN+ACK`. The post-enable IDR write is NOT the lever ⇒ **the entire L2 accept/filter layer is now genuinely exonerated** (accept nibble + AAP + CPlusCmd Normal_mode + IDR physical-match all re-asserted post-`CR.RE`, unicast still drops); the defect is BELOW the accept/filter layer. bite-1's rubric named `rx_ucasts` instrumentation as the next escape; **instead, per user direction, bite-2 runs a behavioral discriminator first** — split **raw unicast RX** from **gateway/off-LAN routing**. AGNOS now auto-discovers an on-LAN peer (the `mbp-arp-rx-probe.sh` harness on `.121`, snooped from its broadcast who-has = ZERO unicast RX), connects to `peer:80` on the **same L2 segment** (no gateway, no NAT), and prints `net: LAN-TCP OK`/`FAIL` **before** the 1.1.1.1 test — the peer's SYN+ACK is the first unicast frame AGNOS must RX, from an endpoint we own and log. `net.cyr`: `net_peer_*` slots + capture in `net_handle_arp` (non-gateway who-has-for-us) + `route_next_hop_mac` on-LAN peer branch; `main.cyr`: ~2s peer-wait + on-LAN `tcp_connect` before 1.1.1.1. The MBP harness was rewritten from the ARP-injection probe into a logging on-LAN TCP webservice (listener on :80 + pcap + stimulus that doubles as peer-discovery + verdict). Build 622,656 → **623,928 B** (+1,272 B), `test.sh` 4/4 + `ext2-smoke.sh` 5/5, multiboot2 ELF64 OK, `build/agnos` reflects HEAD, VERSION untouched (1.32.7 open). **Burn-ready** — NOT auto-proposed per [[feedback_iron_burns_block_other_work]]; rubric: `net: LAN-TCP OK` (+ MBP `ACCEPTED` log) → raw unicast RX WORKS, 1.1.1.1 failure is gateway/off-LAN-specific (RE-SCOPE); `net: LAN-TCP FAIL` (MBP pcap logs SYN-in + SYN+ACK-out, no AGNOS ACK) → unicast-RX drop confirmed at an owned/logged endpoint (no gateway/NAT/Cloudflare confound), `rx_ucasts` instrumentation now justified. Receipt: [`iron-nuc-zen-log.md#tracker-1327-cycle`](iron-nuc-zen-log.md#tracker-1327-cycle) bite 2. _Prior bite-1 head follows for traceability._ **🆕🆕 PRIOR HEAD — 1.32.7 bite-1 (IDR physical-match filter re-asserted post-`CR.RE`, `r8169.cyr` step 11c) LANDED + build-verified (BURNED→FALSIFIED above).** Unicast TCP residual survived 1.32.6: bite-6 (CPlusCmd `Normal_mode` `0x0008`→`0x2061` + AAP-drop) **BURNED 2026-05-25 → FALSIFIED for unicast** ([`1326-agnos-1.32.6-bite6-cplus-normal-mode-burn-l2-ok-gateway-cached-still-no-synack-unicast-drop-survives.jpg`](iron-nuc-zen-photos/1326-agnos-1.32.6-bite6-cplus-normal-mode-burn-l2-ok-gateway-cached-still-no-synack-unicast-drop-survives.jpg), phone-label `1326_still_ack_issue`): FB byte-identical to the bite-4 burn — `tcp: connect 1.1.1.1:80` → `net: L3+TCP FAIL -- SYN sent but no SYN+ACK`. Normal_mode is correct + retained (the proven-Linux value) but NOT the gate. **Root-cause reframe**: broadcast + multicast deliver via the **MAR all-1s hash** (a SEPARATE register, latched fine pre-`CR.RE`), so they prove NOTHING about unicast; the **IDR physical-match path has never delivered a frame on iron** across the whole arc. bite-7 (1.32.5) proved this VER_46 stepping silently drops filter-config writes issued while RX is disabled (Linux `05212ba8132b`) — the accept nibble only engaged once re-asserted post-`CR.RE`. The **IDR0-5 write is the LONE filter register still left only pre-`CR.RE`**; bite-2 reordered it but never moved it past enable, so its "IDR exonerated" verdict is CONFOUNDED exactly like the pre-enable AAP (Attempt 104) + CPlusCmd (bite-6) burns. **1.32.7 fix**: re-assert IDR0/IDR4 *after* `CR.RE` (Cfg9346-unlock, hi+commit, lo latches the 6-byte filter +commit, lock), mirroring the proven step-11b accept re-assert; reuses `mac_lo`/`mac_hi` from step 2. Build 622,560 → **622,656 B** (+96 B), `test.sh` 4/4 + `ext2-smoke.sh` 5/5, multiboot2 ELF64 OK, `build/agnos` reflects HEAD, VERSION bumped 1.32.6→1.32.7 (user-directed cycle open). **Burn-ready** — NOT auto-proposed per [[feedback_iron_burns_block_other_work]]; rubric: FB reads `net: L3+TCP OK -- outbound TCP handshake established` (or the gateway unicast ARP reply clears `arp_pending` via the *unicast* path) → IDR pre-enable write WAS the dropped lever; still FAIL → accept+filter layer GENUINELY exonerated (not confounded) → next escape is the `rx_ucasts` tally (reg `0x28`, instrumentation, deferred per [[feedback_no_instrumentation_means_no_instrumentation]]). Receipt: [`iron-nuc-zen-log.md#tracker-1327-cycle`](iron-nuc-zen-log.md#tracker-1327-cycle). _Prior 1.32.6 bite-4/6 head follows for traceability._ **🆕🆕 PRIOR HEAD (bite-4 ARP snoop IRON-PROVEN, L2 reachable):** Burn 2026-05-25 ([`1326-agnos-1.32.6-arp-snoop-l2-ok-gateway-cached-tcp-syn-no-synack.jpg`](iron-nuc-zen-photos/1326-agnos-1.32.6-arp-snoop-l2-ok-gateway-cached-tcp-syn-no-synack.jpg), phone-label `1326_Gateway_Connected`): FB read `arp: REPLY gw_mac=212:106:145:206:112:96` (= `d4:6a:91:ce:70:60`, the Araknis gateway) → `net: L2 OK -- gateway MAC cached` — **first gateway reachability on iron in the whole arc**, MAC learned with ZERO unicast RX (snooped from the gateway's own broadcast `who-has`) → RFC-826 snoop now proven on iron, not just Linux. Boot then walked into L3: `tcp: connect 1.1.1.1:80` → `net: L3+TCP FAIL -- SYN sent but no SYN+ACK`. **The SYN+ACK is a UNICAST frame to AGNOS's MAC** (boot-probe comment, `main.cyr:729-732`) → with TX proven (4×) + routing resolving 1.1.1.1 → cached gateway MAC + SYN egressing, the parked **APM/unicast-RX gap (bite-2/3 verdict) is isolated as the sole live blocker** — same class never proven on iron, now load-bearing. Poll budget exonerated: `tcp_connect`'s 200× `arch_wait()`/`hlt` loop is timer-bounded (~2 s, ample for a Cloudflare RTT). Honest alternative — gateway source-guarding the static `.222` — was the WEAK hypothesis (user runs static leases on this Araknis routinely; garage switch pinned `.244`; network unchanged but for upstream provider). **✅ DISAMBIGUATED 2026-05-25 (no burn) → AGNOS unicast RX CONFIRMED as the blocker.** `scripts/dhcp-probe/src/tcp_syn_probe.cyr` sent a BYTE-FAITHFUL AGNOS SYN (`ip_build`/`tcp_checksum_compute` ported verbatim from `net.cyr`) to `1.1.1.1:80` from `.222`+AGNOS MAC over Linux's r8169 (AF_PACKET, no VFIO, box stayed reachable): **SYN+ACK received from `1.1.1.1:80` flags=`0x12`**, gateway MAC self-resolved to `d4:6a:91:ce:70:60`, `who-has .222` answered 0× (gateway snooped our MAC off the SYN). ⇒ **gateway-source-guard + malformed-SYN both RULED OUT** — the SYN is well-formed, the static `.222` is accepted, the unicast reply returns to the wire; only unicast-class RX delivery differs on iron, and TCP has NO broadcast-snoop route-around. **Next move (NOT a burn, NOT auto-launched): crack "why the RxConfig accept nibble (APM+AAP) won't latch on iron despite the write"** — bite-3's VFIO-readback plan is DEAD (unbinds the sole link), so the clean zero-burn replacement is an `ethtool -d enp1s0` register dump of the LIVE WORKING Linux r8169 on this exact chip, diffed against AGNOS's `r8169.cyr` write sequence (+ a behavioral multi-source re-derive of accept-filter engagement timing as the parallel track). **🎯 bite-6 fix LANDED + build-verified (no burn yet):** `ethtool -d enp1s0` of the LIVE WORKING Linux r8169 on this exact chip (saved: [`r8169-live-linux-register-dump-2026-05-25.md`](r8169-live-linux-register-dump-2026-05-25.md)) gave the proven config CPlusCmd=`0x2061`, RxConfig=`0x0002CF0E`. A 3-source agent audit (Linux `r8169_main.c` + FreeBSD `if_rlreg.h` + RTL8168 datasheet) **killed the bit-17/RMW lead** (Linux never authors it; FreeBSD blind-writes without it + RXes unicast) and pinned the gate to **CPlusCmd `Normal_mode` (0x2000) — AGNOS's bare `0x0008` write ZEROED it**, the lone source-confirmed never-burned divergence; the prior CPlusCmd/profile A/B burns were CONFOUNDED (all ran with Normal_mode cleared). Fix in `r8169.cyr`: CPlusCmd `0x0008`→`0x2061`, RxConfig profile `0xEF00`→`0xCF00` + accept `0x0F`→`0x0E` (drop AAP), all stale comments rewritten. Build **622,560 B** (12:48 rebuild, reflects working tree), `test.sh` 4/4 + `ext2-smoke.sh` 5/5, multiboot2 ELF64 OK, VERSION untouched (1.32.6 open). **Burn-ready** — NOT auto-proposed per [[feedback_iron_burns_block_other_work]]; rubric: FB reads `net: L3+TCP OK -- outbound TCP handshake established` instead of the SYN+ACK failure. Receipt: [`iron-nuc-zen-log.md#tracker-1326-cycle`](iron-nuc-zen-log.md#tracker-1326-cycle) bites 4 + 5 + 6. _Prior REFRAME head follows._ **🆕🆕 PRIOR HEAD — REFRAME: the bug was at the ARP layer, NOT the driver (RFC 826 sender-snoop), Linux-validated WITHOUT an iron burn.** Per user direction 2026-05-25 (*"the driver works — it's ARP and DHCP"*): the entire 1.32.5→1.32.6 chase of "r8169 unicast-class RX delivery" (bites 1/2/3 + a VFIO userland-driver probe) was the WRONG LAYER. The driver is proven (TX + broadcast + multicast all on iron). The defect: `net_handle_arp` (`net.cyr:551`) learned the gateway MAC ONLY from a *solicited unicast reply* (`oper==2` matching the pending request) — violating RFC 826, which snoops the sender IP→MAC from EVERY ARP frame. **Fix landed**: snoop sender from any ARP frame (request OR reply), gated on `arp_pending_ip` (security posture unchanged). The Araknis gateway broadcasts its own `who-has` (sender=`.1`, mac=`d4:6a:91:ce:70:60`) which AGNOS already receives as BROADCAST (the FB `arp_in=N` count) → gateway MAC learnable with **ZERO unicast RX**. **Zero-burn validation** (`scripts/dhcp-probe/src/arp_snoop_check.cyr`, AF_PACKET — NIC stays bound to Linux so the box stays reachable, NO VFIO): 15 s capture saw the gateway as ARP sender **10× broadcast + 5× unicast**; the exact new snoop logic clears `arp_pending` from the broadcast frames the OLD reply-only code discarded → CONFIRMED load-bearing. Build **622,496 B** (working tree), `test.sh` 4/4 + `ext2-smoke.sh` 5/5, multiboot2 ELF64 OK, `build/agnos` reflects working tree, VERSION untouched (1.32.6 open). **Burn-ready** — NOT auto-proposed per [[feedback_iron_burns_block_other_work]]; rubric: FB reads `arp: REPLY gw_mac=d4:6a:91:ce:70:60` → `net: L2 OK` instead of `reply pending`. **VFIO userland-driver approach KILLED** (binds `enp1s0` = the only link = kills the connection the test runs over; user deleted the scripts). The driver-side unicast-RX question (bite-2 verdict, parked) is now MOOT for ARP/DHCP reachability. Receipt: [`iron-nuc-zen-log.md#tracker-1326-cycle`](iron-nuc-zen-log.md#tracker-1326-cycle) bite 4. **🔧 STACK INTO THE BITE-4 BURN — fix the shell `test` command for iron**: `sh_cmd_test` (`kernel/user/test.cyr:409`, dispatched `shell.cyr:558`) runs the kernel suite (pmm/heap/vfs/proc/syscall/kstdlib/initrd) but emits **only** via `serial_print`/`serial_println` → INVISIBLE on archaemenid per [[feedback_no_serial_on_iron]] (no serial cable), so `agnos> test` prints nothing readable on iron today. Re-route its output to the framebuffer (`kprintln`) so it produces visible PASS/FAIL on iron — that converts the in-shell suite into a reusable iron testing surface (later extendable with net/ARP/DHCP checks so reachability can be re-run interactively from the shell instead of a fresh boot). Folding it into the bite-4 burn means the reset (every burn = full power-cycle + re-cable + re-watch) buys two fixes, not one. _Prior bite-1/bite-2 burn detail follows._ **CURRENT HEAD — honest L2 RX self-test BURNED 2026-05-25 ~02:00 PDT (`1325-agnos-1.32.5-honest-l2-rx-selftest-rx-alive-on-fb.jpg`) → 🎯 RX ALIVE CONFIRMED ON THE FRAMEBUFFER:** the FB now reads `net: L2 RX ALIVE rx=10 arp_in=7 arp_ans=1 -- gateway unicast reply pending` (replacing the old misleading `net: L1/L2 FAILED`) — **10 frames delivered, 7 inbound ARP, 1 ARP reply TX'd by AGNOS** — the bite-7 broadcast-RX breakthrough now visible WITHOUT a pcap, and the "FB lied for 4 days" diagnostic defect retired (a partial-RX NIC no longer reads identically to a dead one). `rx=10 > arp_in=7` ⇒ 3 non-ARP frames also delivered, so broadcast **and** multicast classes pass; the lone remaining gap is the gateway's *unicast* (APM-class) reply, still pending — same narrowed gap as bite-7, now FB-visible. Boot net probe (`main.cyr:684-721`) counts frames `net_poll()` handled + ARP replies the responder TX'd (`net_arp_replies_sent`, `net.cyr:592`); on timeout it prints the ALIVE line above (or `net: L2 RX SILENT -- 0 frames in ~5s` when truly dead). Build 622,408 B (HEAD `52a66f8`), `test.sh` 4/4 + `ext2-smoke.sh` 5/5, multiboot2 OK, `build/agnos` reflects HEAD. **🆕 CURRENT HEAD — bite 1 (ARP retransmit) BURNED 2026-05-25 → catalogued [`1326-agnos-1.32.6-arp-retransmit-rx15-arpin11-arpans2-unicast-drop-systematic.jpg`](iron-nuc-zen-photos/1326-agnos-1.32.6-arp-retransmit-rx15-arpin11-arpans2-unicast-drop-systematic.jpg) (user phone-label `1326_LX2_Still_No_Gate`) → discriminator fired its SYSTEMATIC branch; bite 2 (deep driver APM re-derive) BURNED 2026-05-25 (no photo — user-reported FB line) → FALSIFIED.** LX2 (bite-1) FB read `net: L2 RX ALIVE rx=15 arp_in=11 arp_ans=2 -- gateway unicast reply pending`: `arp_ans=2` proves the retransmit loop egressed (AGNOS answered 2 inbound who-has-`.222`), `arp_in=11` proves the ring delivered 11 broadcast ARP frames — yet the gateway's *unicast* reply never cleared `arp_pending_ip`. **Unicast drop is SYSTEMATIC, not a transient elicitation miss.** The OLD "leading next bite" (restore the IDR write-back bite H removed) was **FALSIFIED by reading the code**: the write-back is present and correct. Bite 2's 3-source re-derive (BSD triangle / iPXE+Haiku+U-Boot / Linux+datasheet+erratum-git-history) found the divergence is the *protocol*, not the presence. **Two convergent fixes landed** (`r8169.cyr`): (1) **IDR write-back reordered to Linux `rtl_rar_set` shape** (`r8169_main.c:2559-2573`) — MAC4/high-half FIRST + `rtl_pci_commit` readback, THEN MAC0/low-half + readback; the low-word write latches the 6-byte physical-match filter, so the high bytes must be resident first, and the readback blocks posted-write reordering on fast PCIe from latching a torn address (a torn unicast filter drops the gateway reply while broadcast/multicast pass — exact signature). (2) **Post-`CR.RE` RxConfig changed from read-modify-write to one clean full write** of `RXCFG_DEFAULTS|accept` + commit (Linux commit `05212ba8132b`: RxConfig writes while RX-disabled are dropped, so the pre-enable write was a no-op and the RMW may never have landed the profile word). **Decisive reframe**: `MAR=all-1s` makes broadcast pass via the multicast hash (`ff:ff:ff` = all-1s bucket), so broadcast+multicast delivery does NOT prove the accept nibble engages — and Linux's force-allmulti VER_46 patch (`efa5f1311c49`) was **reverted** (`6a26310273c3`) after a maintainer confirmed a correctly-initialized VER_46 receives unicast WITHOUT allmulti → the symptom is an init-protocol bug, not a missing workaround. Also corrected a stale CPlusCmd comment (`r8169.cyr:59-64`) that contradicted the code per [[feedback_audit_re_derive_dont_validate_comments]]. Build **622,544 B** (working tree), `test.sh` 4/4 + `ext2-smoke.sh` 5/5 (all backends reach shell, no boot regression; r8169 is iron-only, QEMU uses virtio_net), multiboot2 ELF64 OK, `build/agnos` reflects working tree. NOT auto-proposed per [[feedback_iron_burns_block_other_work]]. **Bite-2 burn outcome (2026-05-25, no photo — user-reported FB)**: `net: L2 RX ALIVE rx=15 arp_in=7 arp_ans=1 -- gateway unicast reply pending` → the discriminator's **"still `reply pending`" branch fired** ⇒ rar_set order/flush + clean post-`CR.RE` RxConfig write are **NOT load-bearing**; the unicast (APM-class) drop is BELOW the accept+IDR-filter layer (both now exonerated). `arp_in=7 arp_ans=1` < bite-1's `11`/`2` is LAN broadcast-ARP traffic variance, not a regression (`rx=15` total held identical; the load-bearing signal is `reply pending` persisting). Next escape per rubric: behavioral `r8169_init_rx`/`r8169_poll` descriptor-OWN handoff re-derive FIRST; hardware tally-counter `rx_ucasts` readback (reg `0x28`) is instrumentation, deferred per [[feedback_no_instrumentation_means_no_instrumentation]] until convergent behavioral levers are exhausted. Receipt: [`iron-nuc-zen-log.md#tracker-1326-cycle`](iron-nuc-zen-log.md#tracker-1326-cycle). **🎯 BREAKTHROUGH — bite-7 (post-RX-enable accept-filter re-assert, `r8169.cyr:639-664`, 621,896 B) BURNED 2026-05-25 ~01:18 PDT (`1325_pcap_attempt_4.pcapng`) → broadcast RX PROVEN ON IRON.** AGNOS received `.121`'s broadcast ARP `who-has 192.168.1.222` and egressed a correct unicast reply (`01:18:57.527723 b0:41:6f:0c:e4:25 > 42:c2:df:db:ee:78 Reply 192.168.1.222 is-at b0:41:6f:0c:e4:25`) — the **first inbound frame acted upon on iron in the entire arc**. Full chain ran: r8169 RX → `r8169_poll` → `net_poll` → `net_handle_arp` (`net.cyr:566`) → reply TX (`net.cyr:592`). **This FALSIFIES the Attempt-104/bite-6 framing "bug is DOWNSTREAM in descriptor OWN/DMA/ring delivery" — the ring delivers, poll/dispatch/rearm is healthy.** The `_3` reckoning's contradiction #1 (accept nibble clobbered/ignored when written only pre-`CR.RE`) is RESOLVED: re-asserting it AFTER `CR.RE` made broadcast deliver — the lone untried convergent lever the `_3` ladder reckoning named. **Remaining gap (narrowed, NOT closed):** boot still printed the failed-gateway verdict because its sole gate is the gateway's *unicast* ARP reply (`main.cyr:686-721`) — invisible at `.121`'s vantage AND a different accept class (APM) than the broadcast (AB) now proven; **unicast-class RX delivery is still unconfirmed**. The honest-self-test above is the direct response — the next burn will SHOW `rx>0/arp_ans>0` on the FB. Receipt: [`iron-nuc-zen-log.md` § bite-7 breakthrough](iron-nuc-zen-log.md). _Prior bite-7-PENDING + bite-6 burn detail follows._ Bite-7 (PENDING-BURN context, resolved above): post-RX-enable accept-filter re-assert — read-modify-write the RxConfig accept nibble AFTER `CR.RE`, mirroring OpenBSD `re_iff` / Linux `rtl_set_rx_mode` / iPXE (all program the accept filter at/after RX-enable; AGNOS wrote it ONLY pre-enable on every burn). From-scratch 3-agent re-derive (BSD triangle / iPXE / RTL8168H spec); the iPXE-vs-spec CPlusCmd conflict was resolved by **git history not source-picking** — CPRX\|CPTX (`0x000B` Attempts 104+bite-5 / `0x0008` bite-6) and RxConfig profile (`0xCF00` Attempt 98 / `0xEF00` current) were each burned BOTH ways and falsified, so the pre-vs-post-enable accept-nibble timing is the lone untried convergent lever. Mechanism fit: multicast rides the SEPARATE MAR register (lands) while bcast/ucast/AAP live in the accept nibble (ignored-if-written-before-enable, per Linux stable patch) → the exact multicast-passes/bcast-ucast-drop split. _Prior bite-6 burn detail follows._ **bite-6 (RXDV-gate settle + CPlusCmd correction, 621,768 B) BURNED 2026-05-25 ~00:03 PDT (`1325_pcap_attempt_3.pcapng`) → FALSIFIED — RX still silent.** This pcap is the **cleanest RX vantage of the entire arc** and it CLOSES the vantage-blindness that weakened Attempts 101–104: the capture host `.121` (`42:c2:df:db:ee:78`) is on AGNOS's L2 segment AND actively probed AGNOS — 1 directed unicast ICMP echo to `b0`'s exact MAC + **63 broadcast `who-has 192.168.1.222`** queries — so AGNOS's *reply* (if any) lands at the capturer by construction. AGNOS, which implements a working ARP responder (`net.cyr:560-593`) and has TX proven 4× over, answered **none** of it; its only frame in 96 s was the single outbound ARP request. **RX-drop is now proven DIRECTLY (not inferred from the vantage-blind missing-gateway-reply).** Both RXDV-settle and CPlusCmd FALSIFIED. **Ladder reckoning per [[feedback_stop_letter_laddering]]**: 7 burns (97/98/99/103/104/`_2`/`_3`) on this symptom; the escape is a from-scratch multi-source re-derive of the WHOLE RX path (RxConfig write-effectiveness + ring + poll + rearm) treating all comments as suspect per [[feedback_audit_re_derive_dont_validate_comments]] — NOT bite-7-another-register, NO instrumentation per [[feedback_no_instrumentation_means_no_instrumentation]]. Two unexamined contradictions to drive it: (1) AAP-promiscuous-set-but-still-dropping while multicast passed (single 32-bit RxConfig write — is the write even landing?); (2) only-solicited-responses-drop (DHCP OFFER / ARP reply) while unsolicited-periodic-multicast passed, which has no L2 meaning. NOT auto-proposed per [[feedback_iron_burns_block_other_work]]. _Superseded bite-6 PENDING-BURN detail follows for traceability._ (Pre-burn:) Two parallel research agents re-derived the 8168H RX-engine bring-up multi-source (Linux/FreeBSD/OpenBSD/NetBSD/U-Boot/iPXE): **(1) RXDV-gate settle (PRIMARY)** — AGNOS clears `RXDV_GATED_EN` (MISC 0xF0 bit 19) but enabled `CR.RE` µs later; Linux pairs the clear with `fsleep(2000)` (~2 ms), and without the settle the RX validator opens *after* RX starts → RX silent while TX (never consults the gate) egresses fine = the exact iron signature; U-Boot clears this bit specifically for *"DHCP failures after kernel reboots"* = archaemenid's warm-boot-from-Linux scenario; added a ~4–8 ms MMIO-read spin before CR.RE (`r8169.cyr:546` region). **(2) CPlusCmd correction (SECONDARY)** — `0x000B→0x0008` (PCIMulRW only); `0x0001|0x0002` are the legacy-8139C+ "C+ RX/TX enable" map, but on the gigabit 8168 those bits are the INTT interrupt-moderation field (Linux `INTT_MASK`) — the "nine-burn CPlusCmd trap" per [[feedback_audit_re_derive_dont_validate_comments]]; inert under IMR=0 but unambiguously wrong. Research also CONFIRMED bite H was right to delete the Linux ephy/ERI/MAC-OCP/firmware clone (FreeBSD/OpenBSD/NetBSD drive this stepping with none of it). `test.sh` 4/4 + `ext2-smoke.sh` 5/5; multiboot2 OK; `build/agnos` reflects HEAD. NOT auto-proposed per [[feedback_iron_burns_block_other_work]]. **Prior bite-5 RxMaxSize fix (`0x4000`→`0x05F3`) BURNED 2026-05-24 23:42 (`1325_pcap_attempt_2.pcapng`) → FALSIFIED** ("no change"; hypothesis inconsistent with multicast-passing of Attempts 97–99) — RMS candidate CLOSED, `0x05F3` retained as correct-but-not-the-bug; that pcap is TX re-proof #3 from the same wrong-vantage switched port (`42:c2:df:db:ee:78`=.121, holding a live SSH session into archaemenid's Linux `b0`/.141 lease → structurally blind to the gateway→`b0` unicast reply). _Superseded Attempt-104 / RxMaxSize lead follows for traceability._ **`1324_tcp_capture.pcapng` burn (~18:55 PDT) PROVES r8169 TX on iron** — AGNOS's broadcast ARP request egressed the wire byte-correct (`b0:41:6f:0c:e4:25 > ff:ff:ff:ff:ff:ff who-has 192.168.1.1 tell 192.168.1.222`, exact match to the FB `arp: request -> 192.168.1.1` line). The gateway's unicast reply is INVISIBLE from the capture vantage (host `42:c2:df:db:ee:78` = `.121`/`.101`, a regular switched port NOT a SPAN/mirror — every unicast frame in the 201-pkt capture involves that one host, so a unicast reply to `b0` never floods to it; same Attempt-101 wrong-vantage trap). Combined with Attempt 102's Linux proof (gateway unicast-replies to this exact MAC+IP frame AND Linux RX delivers) + the cross-era multicast-only RX signature (Attempts 97–103 delivered `01:00:5e` multicast only, never broadcast OFFER, never unicast reply): the bug isolates to **r8169 RX dropping broadcast + unicast classes (multicast passes); TX is exonerated.** The 1.32.4 disambiguator's TX half is CLOSED — no re-burn for TX. agnos VERSION bumped 1.32.4 → 1.32.5 (user-named cycle open). **RX fix LANDED + build-verified** (bites 1+2): multi-source re-derivation found AGNOS's RxConfig accept mask lacked **AAP** (iPXE `realtek_open` sets it unconditionally; H-stepping L2 filter is quirky per Linux VER_46 erratum) → added `AAP` (`accept = AAP|AB|AM|APM`, RxConfig `0xEF0F`) + removed the unconditional `FS|LS` discard gate in `r8169_poll` (BSD-convergent, JUMBOV2-only). AAP doubles as a **bisector** (if RX still drops, fault is downstream of the L2 filter). Build **621,704 B** (−112 B vs Attempt 103), `test.sh` 4/4 + `ext2-smoke.sh` 5/5 + `tcp-listen-smoke` 1/2 (baseline), multiboot2 OK, shell banner v1.32.5. `build/agnos` reflects HEAD. **Iron Attempt 104 BURNED 2026-05-24 ~19:58 PDT → FALSIFIED** (photo + companion pcap catalogued): `arp: TIMEOUT` / `net: L1/L2 FAILED`, shell v1.32.5 reached byte-clean. **The AAP bisector fired its falsification branch** — AAP (promiscuous catch-all) + FS\|LS-discard-gate removal STILL don't deliver the gateway's unicast reply ⇒ **L2 accept-mask layer EXONERATED** (h1 accept-write, h2 APM-vs-zeroed-IDR, AAP all falsified by construction); bug is DOWNSTREAM in **r8169 RX descriptor OWN/DMA/ring delivery**. Second-machine `1325_pcap_test.pcapng` (top-level, sibling of `1324_tcp_capture.pcapng`) **RE-PROVES TX** — AGNOS's broadcast ARP egressed byte-correct (`b0:41:6f:0c:e4:25 > ff:ff:ff:ff:ff:ff who-has 192.168.1.1 tell 192.168.1.222` @ 19:58:36, identical shape to 1324) at the AAP build (no TX regression) — BUT the capture host `42:c2:df:db:ee:78` is again a regular switched port (every ARP reply in the 7.5-min capture has `42:c2` as one party; zero third-party unicast floods), so the gateway's unicast reply is invisible from that vantage (same Attempt-101 trap; the SPAN/mirror port the Attempt-103 escalation called for is still owed). **Next move (NOT auto-proposed per [[feedback_iron_burns_block_other_work]])**: MMIO / descriptor-DMA observability on the RX ring (CMOS-stamp post-poll descriptor OWN/status + RDSAR/CmdReg) OR multi-source re-derive `r8169_init_rx`/`r8169_poll` OWN-handling per [[feedback_audit_re_derive_dont_validate_comments]]. Photo: [`attempt-104-agnos-1.32.5-aap-rxcfg-arp-timeout-rx-downstream-of-l2-filter.jpg`](iron-nuc-zen-photos/attempt-104-agnos-1.32.5-aap-rxcfg-arp-timeout-rx-downstream-of-l2-filter.jpg); receipt: [`iron-nuc-zen-log.md` § Attempt 104](iron-nuc-zen-log.md) + [`r8169-rx-path-audit.md` § 1.32.5 addendum](r8169-rx-path-audit.md) (addendum still owes a post-Attempt-104 closing note that AAP was falsified). **POST-ATTEMPT-104 FIX LANDED 2026-05-24 20:41 — RxMaxSize field-overflow (`r8169.cyr:557`, RMS `0x4000` → `0x05F3`)**: a full RX-path re-audit (probe order / init_rx / bring-up envelope / poll / rearm / dispatch — all confirmed prior-art-correct) found ONE divergence — RxMaxSize (0xDA) is a 13-14 bit field on the 8168, so `0x4000` sets bit 14 OUTSIDE it → reads as RMS=**0** → NIC rejects all inbound frames while TX (which never consults RMS) egresses fine ⇒ **matches the exact TX-works/RX-silent iron signature**. No prior-art driver writes `0x4000` (Linux `RX_BUF_SIZE=0x05F3`, FreeBSD=buffer length); the malformed value was introduced at Attempt 97 while the FS\|LS gate + wrong RxConfig profile masked it — both since fixed, so it's now a live load-bearing candidate. Build **621,704 B** (constant swap, no size change), `test.sh` 4/4 + `ext2-smoke.sh` 5/5, multiboot2 OK, `build/agnos` reflects HEAD. This is a behavioral fix, not instrumentation — the next burn TESTS it. NOT auto-proposed. Tracker bite 5 at [`iron-nuc-zen-log.md#tracker-1325-cycle`](iron-nuc-zen-log.md#tracker-1325-cycle). _Superseded 1.32.4 detail follows for traceability._ (**1.32.4 CLOSED — cut 2026-05-24, awaiting user tag (CHANGELOG 1.32.4 release entry landed); Attempt 103 BURNED 2026-05-24 ~17:24 PDT → FALSIFIED — EEPROM `b0` MAC build (LAA removed) STILL ARP-times-out (`net: STATIC ip=192.168.1.222` → `arp: TIMEOUT -- gateway did not reply within ~5s` → `net: L1/L2 FAILED`; shell v1.32.4 reached byte-clean, no regression). This is EXACTLY the pre-burn falsification branch: with `b0` now matching the `arp-probe-raw` frame the gateway answered on Linux, the filter MAC is EXONERATED and the bug is isolated PURELY to r8169.cyr RX delivery on iron (TX wire-egress OR RX-ring delivery — both inside `r8169.cyr` MMIO/descriptor-DMA). Next move per rubric: MMIO / descriptor-DMA observability on the RX ring, NOT another construction/chip-init audit (closed). The clean TX-vs-RX disambiguator is a SECOND-MACHINE `tcpdump -nn -e arp` on a mirrored switch port during a burn (single-machine archaemenid can't sniff its own boot). Photo: `attempt-103-agnos-1.32.4-eeprom-b0-mac-arp-timeout-rx-isolated.jpg`; receipt: [`iron-nuc-zen-log.md` § Attempt 103](iron-nuc-zen-log.md). PRIOR: Attempt 102 BURNED → FALSIFIED (LAA `b2` build still ARP-times-out); zero-burn Linux probes then PROVED frame construction wire-correct (`arp-probe-raw` drew `gateway 192.168.1.1 is at d4:6a:91:ce:70:60` from real `b0`/`.222` while Linux held a live `b0` lease; `dhcp-probe-raw` leased `.129`) and isolated the bug to r8169 RX delivery; a datasheet-level re-derivation found NO structural fault in the filter/ring/poll; LAA override REMOVED (`r8169.cyr:367`) so the next burn tests only the RX-ring code; build 621,880 → 621,816 B, `test.sh` 4/4 + `ext2-smoke.sh` 5/5; Attempt 103 has since BURNED → FALSIFIED (see lead above — `b0` build still ARP-times-out, RX-delivery isolated). Fresh-Linux env restored — `qemu-system-x86`/`aarch64` + `tcpdump` + `parted` + `mtools` + `gptfdisk` reinstalled (home dir + cyrius 6.0.1 toolchain + 70 repos survived the OS reinstall). Chip-init/construction audit threads CLOSED by Linux proof — [`r8169-chip-init-audit.md`](r8169-chip-init-audit.md) top banner + [`iron-nuc-zen-log.md` § Attempt 102](iron-nuc-zen-log.md). Drive-swap topology-independence validated, and the install-plan disk-shape REALIZED 2026-05-24: AGNOS is now self-contained on the 1TB internal NVMe `nvme0n1` (p1 256MiB FAT32 `AGNOS-BOOT` ESP + p2 25GiB ext4 `agnos-fs`, provisioned via `install-usb.sh /dev/nvme0n1`); Linux root on SATA WD Blue `sda`; 2TB Crucial `sdb` freed → i9. AGNOS boots fully from internal NVMe (USB-stick boot surface retired). Pending before true installable-state: 1.33.x ext4 WRITE + agnova self-install. Superseded LAA/PM detail below retained for traceability.** PM zero-burn discriminators: `dhcp-probe-raw` ACK'd a fresh `.129` OFFER from gateway over our MAC via AF_PACKET; `arping -I enp1s0 -c 3 192.168.1.1` got 3/3 unicast replies in 0.7-1.4 ms. Combined ⇒ wire + protocol + gateway-replies-to-our-MAC all PROVEN without a burn; Attempt 101's missing pcap reply explained by sniffer-on-wrong-switch-port. **Refined T3**: IP-source-guard, not dup-MAC suppression. PM fix: `r8169.cyr:358-369` flips U/L bit on byte 0 (`b0:41:6f:0c:e4:25` → `b2:41:6f:0c:e4:25`); main.cyr IP reverted `.124` → `.222`. Build: 621,640 → **621,880 B**. AM context retained below for traceability. Driving audit: [`dhcp-and-outbound-l3-audit-2026-05-24.md`](dhcp-and-outbound-l3-audit-2026-05-24.md) — consolidated DHCP review vs RFC 2131/2132/826/1122 + multi-source prior art (Linux / lwIP / iPXE / FreeBSD / OpenBSD / NetBSD / U-Boot / Plan 9). Fresh finding: `udp_send` + `tcp_send_pkt` hardcoded broadcast MAC; off-LAN unicast (e.g., 1.1.1.1) could not route through the gateway. Convergent fix landed: `net_gateway_mac[8]` persistent slot + `route_next_hop_mac(dst, out_mac)` helper consulted by both send paths. Main boot flow: ARP probe → on REPLY cache MAC → `tcp_connect(1.1.1.1, 80)` with PASS/FAIL prints. Print-byte-length bug from Attempt 101 fixed (UTF-8 `→`/`—` → ASCII `->`/`--`; "arp request is going to 192.168.1" misread root-caused as a 26-byte declared print on a 28-byte UTF-8 string). Inbound TCP_LISTEN_SMOKE soft-disabled (already opt-in via env var at `scripts/build.sh:97` — no edit needed; production build doesn't define). DHCP 10-bundle from [`dhcp-offer-downstream-audit.md`](dhcp-offer-downstream-audit.md) still landed but unexercised — one-line flip in `main.cyr` re-enables it as escape thread (T7) if Attempt 102 still blocked. **Build verified**: `agnos/build/agnos` = **621,640 B** (+~4,448 B vs Attempt 101 envelope, route helper + outbound test + gateway-MAC slot). cyrius 6.0.1 + gnoboot 0.4.2 unchanged. NO burn auto-proposed per [[feedback_iron_burns_block_other_work]]; pre-attempt rubric + 7 threads-to-pull-if-blocked pinned at [`iron-nuc-zen-log.md#tracker-1324-cycle`](iron-nuc-zen-log.md#tracker-1324-cycle) § Attempt 102 prep.). Highlights:
> - **QEMU full DHCP cycle** verified via pcap: DISCOVER (291 B) → SLIRP OFFER (590 B) → REQUEST (298 B) → SLIRP ACK (590 B) → gratuitous ARP. tcp_accept: conn_id=1 also confirmed.
> - **Iron Attempt 96 ground-truth correction**: branch (a1) wire/server FALSIFIED on archaemenid same-day — pulled `ip -br link` from the current Linux session and confirmed enp1s0 (r8169 chip, MAC b0:41:6f:0c:e4:25) is actively leased at 192.168.1.124/24 from 192.168.1.1. Wire + server + chip all confirmed working under Linux.
> - **`agnosticos/docs/development/r8169-rx-path-audit.md`** — multi-source convergent audit (Linux v6.6 rtl_rx + OpenBSD re_rxeof + FreeBSD re_rxeof + RTL8168 datasheet). Pre-burn verdict: load-bearing bug is `r8169_poll` returning after a single descriptor check. **Post-Attempt-97 verdict: that fix LANDED mechanically but it wasn't load-bearing for the OFFER-timeout symptom** — `r8169_poll` now walks the ring healthily, but the OFFER was not in the ring to be walked. See Attempt 97 outcome below.
> - **`agnos/kernel/core/r8169.cyr` 5-part bundle** (~50 LOC net): Part A multi-frame budget loop in r8169_poll; Part B `RES` (0x00200000) + `FS|LS` (0x30000000) gating constants; Part C `r8169_rx_rearm` helper with EOR read-preserve mirroring Linux `rtl8169_mark_to_asic`; **Part D CMOS-stamp hot-path elimination** (audit S-1, removes ~8 µs per-poll tax — stamps now fire only on state transitions); **Part E RxMaxSize aligned with Linux convergent** (audit S-2, 0x05F3 → 0x4000 effective-disable). Build 616,744 → **617,000 B (+256 B net)**. `test.sh` 4/4 + `ext2-smoke.sh` 5/5 + tcp-listen-smoke QEMU DHCP cycle all clean — zero regression.
> - **Iron Attempt 97 — PARTIAL 2026-05-23 ~14:30 PDT.** Photos catalogued: [`attempt-97-…-pt1-r8169-link-up-preserved-bios-rings-up-storage-clean.jpg`](iron-nuc-zen-photos/attempt-97-agnos-1.32.3-pt1-r8169-link-up-preserved-bios-rings-up-storage-clean.jpg) + [`attempt-97-…-pt2-rx-multi-frame-loop-16-frames-offer-timeout-persists.jpg`](iron-nuc-zen-photos/attempt-97-agnos-1.32.3-pt2-rx-multi-frame-loop-16-frames-offer-timeout-persists.jpg). CMOS readback: 0x58=0x01 (probe done), 0x59=0x01 (PHY link UP — FIX #10 safe path), 0x5A=0x02 (TX sends = DISCOVER + retransmit only, REQUEST never fired), 0x5B=0x30 (TX healthy), **0x5C=0x10 (16 frames consumed — Part A + D effective; vs pre-fix stuck-at-1)**, 0x5D=0x78 (EOR + FS + LS + MAR — Part B + C preserved EOR through wraparound), 0x5E=0x01 (IPv4 multicast first byte `01:00:5e:…` — none of the 16 frames was an OFFER). Per the pre-burn rubric verbatim: `0x5A stays at 0x02 → REQUEST never sent → OFFER never reached dhcp_init's match loop`. **The 5-part bundle did its job mechanically — `r8169_poll` now walks the ring healthily — but the OFFER was not in the ring to be walked.** Root cause is upstream of `r8169_poll`. Two pre-bound next branches:
>     - **(b1) RxConfig high-bits / AcceptBroadcast audit.** DHCP OFFER from server-to-no-IP-client lands as L2 broadcast (`ff:ff:ff:ff:ff:ff`); if `AB` bit isn't set in RxConfig, chip drops OFFER before ring sees it. Multicast (`01:00:5e:…`) passing through implies `AM` is set, so filter mask is partially correct. Code-audit `agnos/kernel/core/r8169.cyr` RxConfig write site against Linux `r8169_main.c` + RTL8168 datasheet § 7 — ~10 LOC candidate fix, no burn needed to confirm by code-read.
>     - **(b2) DHCP server same-MAC lease silence.** Linux dhclient holds an active lease on `b0:41:6f:0c:e4:25 → 192.168.1.124`. Some DHCP servers issue no OFFER for a same-MAC DISCOVER while another lease is active, expecting a REQUEST instead (RFC-compliant). Disambiguation: `tcpdump -i enp1s0 -nn -X 'port 67 or port 68'` from Linux session while AGNOS boots — if no OFFER on the wire, the bug isn't in AGNOS.
> - **Diagnostic work post-Attempt-97 — ZERO additional burns** per [[feedback_iron_burns_block_other_work]]:
>     - **Python AF_PACKET DHCP probe** (`/tmp/dhcp_probe.py`) sent two synthetic DISCOVERs from this Linux session — one from a fresh synthetic MAC, one from real `b0:41:6f:0c:e4:25`. Both got broadcast OFFERs (L2 `ff:ff:ff:ff:ff:ff` + L3 `255.255.255.255`) within ~1 s. Branches (b1) AB-filter-missing + (b2) server same-MAC-lease-silence FALSIFIED.
>     - **AGNOS-in-QEMU + pcap decode** (`/tmp/agnos_qemu_dhcp.sh` + `/tmp/pcap_decode.py`) showed SLIRP's OFFER is the same byte-shape (L2 broadcast + L3 broadcast) as real Araknis. AGNOS catches it cleanly via virtio_net → full DHCP cycle. AGNOS upper layers (`net_poll` / `net_handle_udp` / `udp_recv_from` / `dhcp_init`) proven clean on broadcast OFFERs. Bug isolated to r8169 driver.
>     - **Linux journalctl on archaemenid** gave the chip identification: `r8169 0000:01:00.0 eth0: RTL8168h/8111h, b0:41:6f:0c:e4:25, XID 541, IRQ 85`.
>     - **Linux v7.0 r8169 source** (fetched fresh from torvalds/linux/v7.0): XID 541 → `RTL_GIGA_MAC_VER_46` (r8169_main.c:136). VER_46 falls in `rtl_init_rxcfg` VER_40..52 branch (line 2601-2602) → `RxConfig = RX128_INT_EN | RX_MULTI_EN | RX_DMA_BURST | RX_EARLY_OFF = 0xCF00`. AGNOS was writing legacy `0xE700` (VER_07..17 profile). Diff = `RX_EARLY_OFF` (bit 11) NOT set on AGNOS.
> - **Fix LANDED 2026-05-23 15:31 PDT**: `agnos/kernel/core/r8169.cyr:109` — `R8169_RXCFG_DEFAULTS = 0xE700` → `0xCF00`. One 16-bit constant. Source comment updated with full Linux line-cite + per-mac_version table for future audits.
> - **Build verified 2026-05-23 15:37 PDT**: `build/agnos` 617,000 B production. `scripts/test.sh` 4/4 PASS + `ext2-smoke.sh` 5/5 + 5/5 regression cross-check + `tcp-listen-smoke.sh` 1/2 (matches pre-fix baseline; scenario-1 is the unrelated SLIRP-RX gap). QEMU virtio_net DHCP cycle completes end-to-end. Zero regression. Pre-burn rubric pinned at iron-nuc-zen-log § Attempt 98 prep.
> - MVP gate green on iron since Attempt 68 / 1.30.9; **stayed green at Attempt 97**; **expected green at Attempt 98**.
>
> **Sibling repos opened today** (separate from 1.32.3 iron-burn surface): `yo` (ping/Yodel-Out, scaffolded + committed) and `dig` (DNS resolver, scaffolded + committed). Both target the AGNOS kernel via the future ICMP / UDP-53 / TCP-53 syscall exposures (yo roadmap 0.2.x + dig roadmap 0.2.x). **`dig` is the `taar` extraction trigger** per user direction 2026-05-23 (revised from earlier "three-consumer cycle-open" plan to the proven mihi → iam/chakshu second-consumer-trigger shape). See respective repos' `docs/development/roadmap.md` for the milestone chain. Correction to the 2026-05-22 sync: Attempt 95 was NOT deferred — it ran 2026-05-22 23:42 PDT with identical `dhcp: OFFER timeout` symptom; the prior-session agent missed logging it before context loss. Photo `attempt-95-agnos-1.32.1-post-fix-phy-up-tx-rx-still-wedged.jpg` is the evidence; iron-nuc-zen-log § Attempt 95 closes the accountability gap. **1.32.1 closed with the bug surviving** — 6-FIX bundle was insufficient. Post-burn full-stack sweep (per user direction *"DO ALL THE BUGS; DON'T want to do 2 then burn then 2more"*) surfaced FOUR additional bugs; the load-bearing one is FIX #10, which explains the Attempt 94→95 regression (CMOS 0x5B went from 0x30 cleared to 0xb0 stuck; 0x5E went from 0x01 multicast to 0x00 dead — NIC TX/RX engines wedged because FIX #3's unconditional `BMCR.ANRESTART` took the link down during init_rx/init_tx). **FIX #7** (IDR0..IDR5 reprogrammed after reset; defensive, matches Linux/BSD/Haiku prior art — 1.32.1 audit FINDING #6 flagged this but FIX #6 only renamed RxConfig constants, missing the load-bearing IDR write-back) + **FIX #8** (UDP listener buf 256→1024, net_handle_udp cap 248→1016, dhcp_init rx[320]→rx[1024] — DHCP OFFER/ACK options blob no longer truncated past server-id + subnet + gateway) + **FIX #9** (DISCOVER + REQUEST midpoint retransmit at iter==400 — RFC 2131 §4.4.1 partial compliance) + **FIX #10** (PHY autoneg restart only if link is actually down; reads BMSR.LinkStatus first with IEEE 802.3 §22.2.4.2 latching-low double-read — preserves the BIOS-established link state through init_rx/init_tx). Build trajectory: 604,096 B production (1.32.1 close) → **605,056 B production (1.32.2 cycle work in-flight, post-FIX-7+8+9+10)**. `scripts/test.sh` 4/4 + `ext2-smoke.sh` 5/5 + 5/5 regression + `tcp-listen-smoke.sh` 1/2 (pre-existing iron-only SLIRP-RX gap, matches baseline). Storage trio + GPT + ext4 mount + scheduler + tcp_listen + kybernet + shell all byte-clean. **Attempt 96 deferred per [[feedback_iron_burns_block_other_work]]** — not auto-proposed; iron-validation rubric in [`dhcp-end-to-end-audit.md` § 10.2](dhcp-end-to-end-audit.md). Carry-forward continues for i225-V driver bite C (Intel iron pending) + BBS/MUD userland (out-of-cycle).
>
> **Current scope** — **1.32.4 CLOSED (cut 2026-05-24, awaiting user tag) — Attempt 102 BURNED → FALSIFIED (LAA `b2` build still ARP-times-out); the `arp-probe-raw` + `dhcp-probe-raw` Linux AF_PACKET probes then PROVED AGNOS's frame construction wire-correct and the gateway replies to us, isolating the bug to r8169 RX delivery; LAA override REMOVED (`r8169.cyr:367`) → IDR reverts to EEPROM `b0`, aligning AGNOS's wire+filter identity with the proven probe; **Attempt 103 BURNED 2026-05-24 → FALSIFIED — `b0` build STILL ARP-times-out → filter MAC EXONERATED, bug isolated PURELY to r8169.cyr RX delivery on iron (escalate to MMIO/descriptor-DMA RX-ring observability, NOT another chip-init audit)**. Historical LAA/PM detail follows (superseded — kept for traceability). Driving audit: [`dhcp-and-outbound-l3-audit-2026-05-24.md`](dhcp-and-outbound-l3-audit-2026-05-24.md) consolidates DHCP review vs RFCs + multi-source prior art (Linux / lwIP / iPXE / *BSD / U-Boot / Plan 9). AM finding: hardcoded broadcast MAC at `net.cyr:112-124` (`udp_send`) and `net.cyr:731-758` (`tcp_send_pkt`) — off-LAN unicast (1.1.1.1) could not route through the gateway. Convergent fix landed at `net.cyr:85-110` (`route_next_hop_mac` helper) + `net.cyr:11-16` (persistent `net_gateway_mac` slot, independent of `arp_cache_*`). Main boot flow at `main.cyr:687-715`: ARP probe → on REPLY cache MAC → `tcp_connect(ip4(1,1,1,1), 80, 49152)` with PASS/FAIL prints. Print-byte-length bug from Attempt 101 root-caused + fixed (UTF-8 `→`/`—` are 3 bytes each; declared lengths counted chars not bytes; replaced with ASCII `->`/`--`). **PM addendum (2026-05-24)**: zero-burn discriminators confirmed wire + protocol + gateway-responds-to-our-MAC via two Linux-userland probes — (1) `scripts/dhcp-probe/build/dhcp-probe-raw enp1s0` issued AGNOS-shape DISCOVER over AF_PACKET, gateway returned OFFER `.129` → REQUEST → ACK with our manufactured MAC `b0:41:6f:0c:e4:25` *on top of* Linux dhclient's `.124` lease (no strict DAI); (2) `arping -I enp1s0 -c 3 192.168.1.1` got 3/3 unicast replies from `d4:6a:91:ce:70:60` in 0.7-1.4 ms. **Refined T3**: not dup-MAC suppression — IP-source-guard. Attempt 101's missing reply in the pcap is explained by sniffer-on-wrong-switch-port (modern switches don't flood unicast). The gateway likely *did* reply to AGNOS's ARP from `.222` and either AGNOS r8169 RX dropped the unicast, OR the router IP-source-guarded `b0:…` claiming `.222` while bound to `.124`. **Sovereign-MAC fix landed PM**: `r8169.cyr:358-369` sets U/L bit on byte 0 after IDR read (`b0:…` → `b2:41:6f:0c:e4:25`); Cfg9346-wrapped IDR0/IDR4 writeback at line 515-516 propagates the override into the chip's hardware unicast filter. `main.cyr:675-680` reverted IP `.124` → `.222` since the differentiator is now the MAC. **Build verified PM**: `agnos/build/agnos` = **621,880 B** (`scripts/build.sh`, x86_64); `scripts/test.sh` 4/4 PASS; multiboot2 ELF64 OK; cyrius 6.0.1 + gnoboot 0.4.2 unchanged. DHCP 10-bundle from [`dhcp-offer-downstream-audit.md`](dhcp-offer-downstream-audit.md) still landed-but-unexercised; reserved as T7 escape thread. **NO burn auto-proposed**; pre-attempt PASS rubric + companion tcpdump capture (now OPTIONAL — probe+arping subsume it) + 7 threads-to-pull-if-blocked pinned at [`iron-nuc-zen-log.md#tracker-1324-cycle`](iron-nuc-zen-log.md#tracker-1324-cycle) § Attempt 102 prep.
>
> **Build burned at Attempt 103** (2026-05-24 ~17:24 PDT → FALSIFIED): **621,816 B** production (LAA removed, −64 B vs Attempt 102), agnos HEAD `18a6fc4 "release cut"`. cyrius 6.0.1 + gnoboot 0.4.2 unchanged. `test.sh` 4/4 + `ext2-smoke.sh` 5/5. Storage + GPT + ext2 mount + kybernet + shell byte-clean (edit scope was the r8169 MAC line only). Actual boot block (ASCII, from photo):
> ```
> r8169: MAC=176:65:111:12:228:37          (b0:41:6f:0c:e4:25 — EEPROM MAC, LAA removed; scrolled off top of FB)
> net: STATIC ip=192.168.1.222 gw=192.168.1.1
> arp: request -> 192.168.1.1
> arp: TIMEOUT -- gateway did not reply within ~5s
> net: L1/L2 FAILED -- cannot reach gateway, skipping L3 test
> ```
> **ARP TIMED OUT with the EEPROM `b0` MAC** ⇒ the rubric's falsification branch fired: the filter MAC is EXONERATED (it now matches the `arp-probe-raw` the gateway answered on Linux at Attempt 102), so the bug is purely r8169.cyr RX-ring delivery on iron — TX wire-egress not actually clocking the frame onto the cable, OR the gateway's unicast reply not being delivered up the RX ring to `r8169_poll`. The L3 (`tcp: connect 1.1.1.1:80`) test was correctly skipped behind the failed ARP gate. **Next move per rubric: MMIO / descriptor-DMA observability on the RX ring, NOT another construction/chip-init audit (that surface is closed).** The TX-vs-RX byte-exact split needs a SECOND-MACHINE `tcpdump -nn -e arp` on a mirrored switch port during a burn — single-machine archaemenid can't sniff its own boot (the Attempt 101 "wrong-switch-port" miss is the cautionary note). NOT auto-proposed per [[feedback_iron_burns_block_other_work]].
>
> **Out-of-cycle carry-forward**: **i225-V driver port** — explicitly paired with the **i9 / Intel-NIC iron cycle post-archaemenid-migration** per user direction 2026-05-24 (NOT in 1.32.5 close-out). **agora v2 port** — BBS userland v1.0.0 already shipped on Linux at `/home/macro/Repos/agora/`; v2 ports to AGNOS once the 1.32.x networking arc proves end-to-end on iron (post-1.32.5 close at the earliest). **cyrius-yeomans-decent** — MUD, title ***Yeoman's Descent*** (project-lead-first-name × MUD-genre, English-wordplay lane; slug typo `decent` → `descent` to be fixed by user later — README already carries the correct spelling); repo scaffolded with `src/`/`tests/`/`docs/`/`lib/`, minor starter work in flight as of 2026-05-24. The 1.31.x storage + filesystem arc closed at 1.31.7 (Iron Attempt 91 PASS); 1.32.0 networking arc closed (TCP/UDP server primitives + DHCP client + r8169 Phases 1-4). MVP gate stays green on iron from Attempt 68 / 1.30.9; gnoboot 0.4.2; cyrius pin 6.0.1.
>
> **Recent history** (for full detail consult CHANGELOG + iron-nuc-zen-log):
> - **1.31.0** — NVMe Phase 1-5 + cycle-open production-lean (KTEST/XHCI_VERBOSE compile gates). Iron debut Attempt 80 (Crucial P3 2 TB).
> - **1.31.1** — GPT Phase 1-3 + AHCI/SATA Phase 1-4. Iron debut Attempt 81 (WD Blue SA510 2 TB SATA SSD, PASS-WITH-CAVEAT, three carry-forward patches to 1.31.2).
> - **1.31.2** — AHCI carry-forward triplet + cyrius pin graduation (5.11.64 → 6.0.1) + USB MS Phase 1-4 + 2.5 + 2.6 + 2.7. Iron Attempts 82 (AHCI carry-forward PASS) → 83 PARTIAL → 84 PARTIAL → 85 FALSIFIED → 86 FALSIFIED.
> - **1.31.3** — USB MS Phase 2.8 eight-bug repair stack (bulk-timeout extension + strict TRB matching + SHORT_PACKET residue check + `msc_scsi_exec` unified retry-recover wrapper + drain reposition + 64-bit `param_hi`). Iron Attempt 87 PASS (full INQUIRY/TUR/RC10 chain on Silicon Motion stick).
> - **1.31.4** — RAM-disk backend (`RAMDISK_ENABLE=1` build-flag-gated, OpenBSD MINIROOTSIZE pattern) + VirtIO 1.x modern virtio-blk-pci rewrite (full modern PCI cap-list + MMIO + 64-bit FEATURES_OK + polled used-ring). QEMU 5/5 green; iron Attempt 88 PASS as no-regression burn.
> - **1.31.5** — ext2/ext4 read-only Phase 1-4 (~870 LOC) + multi-source convergent audit ([`ext2-ext4-extents-prior-art.md`](ext2-ext4-extents-prior-art.md)) + fatfs hardening (GPT-protective-MBR BPB-zero guard). QEMU 5/5 green on both ext2 and ext4 images. Iron Attempt 89 PASS as no-regression burn (with new NVMe `agnos-fs` ext4 carve correctly enumerated by GPT Phase 3; ext2 mount predictably silent without bite G).
> - **1.31.6 CLOSED 2026-05-22** — Eight-bite cleanup / hardening / audit cycle: all of A (ext2 input validation) + B (fatfs BPB validation) + C (drop boot-time smoke hook) + D (Cyrius `var X[N]` byte/u64 gotcha → feedback memory) + E (`ext2-iron-burn-audit.md`) + F (state/roadmap/iron-log sweep) + G (multi-backend ext2 probe + `blk_read_on` helper) + H (partition-aware mount via GPT consumption) landed; **smoke-surfaced fixes**: `blk_mark_registered` helper (AHCI/USB-MS visibility to probe in NVMe-primary topologies) + `GPT_TYPE_LINUX_FS_LO` one-byte typo at `gpt.cyr:214` (Linux-FS GUID classification). **Iron Attempt 90 PASS**: ext4 victory lap mounted NVMe `agnos-fs` p3 with `blocksize=4096 inode_size=256 inodes_per_group=8192`, `agnos> ls /` returned `./ ../ lost+found/ hello.txt` byte-exact from real Linux ext4 dirent table on iron NAND. **Closes the 1.31.x storage arc.** See § *1.31.6 cleanup cycle* below.
> - **1.31.7 CLOSED 2026-05-22** — Filesystem follow-ups + shell-UX cycle. ✅ (D) `ls -la` flag-aware dispatch (~25 LOC), ✅ (B) bare-name `cat` ext2 fall-through (~20 LOC), ✅ (C) `cd` + `pwd` + CWD scoping (~190 LOC), ✅ (A) ext4 64BIT Phase 5 (~25 LOC + audit doc + smoke 5), ✅ (E) cycle-close sweep + Attempt 91 transcript. **Iron Attempt 91 PASS**: user re-carved NVMe `agnos-fs` p3 with default `mkfs.ext4` (drops `-O ^64bit`, so on-disk `s_feature_incompat` sets 0x80 = 64BIT); Phase 5 BGDT-stride-aware mount fired cleanly; `agnos> cd lost+found` traversed; `agnos> cat hello.txt` returned `agnos 1.31.7 iron Attempt 91   ext4 64BIT validated   2026-05-22` byte-exact. Storage trio (NVMe/AHCI/USB-MS) byte-matched Attempt 90 — no regression. Build 571,296 → 578,432 B (+7,136 B / ~260 LOC). `scripts/test.sh` 4/4 + `scripts/ext2-smoke.sh` 5/5 (smoke 5 = new 64BIT-flagged ext4) + 5/5 regression cross-check. Closes row 7b + row 7c of agnos roadmap.
> - **1.32.0 CLOSED 2026-05-22** — **Networking arc cycle, feature-complete** (opened + closed same-day per user direction). Landed: TCP server primitives (bite A, ~188 LOC), UDP server primitives (bite F, ~95 LOC), DHCP client RFC 2131 (bite G, ~260 LOC), r8169 driver Phases 1-4 (bite B, ~400 LOC), iron Attempts 92 + 93 on archaemenid (bite D), DHCP gate predicate fix at `main.cyr:655`, cycle-close sweep (bite E). **Iron evidence**: Attempt 92 PARTIAL surfaced gate predicate bug; same-day fix `if (vnet_active != 0 || nic_ready() != 0)`; **Attempt 93 PARTIAL VERIFIED** — `dhcp: DISCOVER` egresses through r8169 path on iron for the first time; new failure mode `dhcp: OFFER timeout` is **carry-forward to 1.32.1** along with i225-V driver bite C and BBS/MUD userland. Storage trio + GPT + ext4 mount + kybernet + shell all byte-clean (no regression from Attempts 90/91). Multi-source convergent per [`network-arc-prior-art.md`](network-arc-prior-art.md) + pre-burn audit at [`r8169-iron-burn-audit.md`](r8169-iron-burn-audit.md). Build 578,432 B (1.31.7 close) → **601,392 B (1.32.0 close, `TCP_LISTEN_SMOKE=1`)** = +22,960 B / ~1,000 LOC. cyrius pin stays on 6.0.1. gnoboot stays on 0.4.2. See § *1.32.0 cycle* below for full bite table.
> - **1.32.1 CLOSED 2026-05-22 (TAGGED by user; Attempt 95 iron-validation DEFERRED)** — Cycle reshaped mid-stream via audit-driven repair. Pre-Attempt-94 theme: r8169 driver-level OFFER-timeout debug. Bites A+B+C landed in commit `b12e25a`; Iron Attempt 94 PARTIAL surfaced that the audit-§ 10 framing was wrong (r8169 functional; OFFER-timeout root cause UPSTREAM of NIC). Same-day audit ([`dhcp-end-to-end-audit.md`](dhcp-end-to-end-audit.md)) found six wiring bugs; user direction "fix all the issues" + "all still in 1.32.1 cycle work" → all 6 FIXes landed same-day in the existing cycle window. PRIMARY fix: `nic_mac()` backend-agnostic kernel-MAC accessor — replaces 7 sites in net.cyr that read uninitialized `vnet_mac` (= zero on iron without virtio); root cause of Attempt 94 OFFER timeout. Tag written by user per discipline *"tag was going to happen regardless of [burn] result"* — Attempt 95 iron-validation moves to whatever cycle opens next. Final build: **604,096 B production / 604,904 B TCP_LISTEN_SMOKE**. `scripts/test.sh` 4/4 + `ext2-smoke.sh` 5/5 + 5/5 regression + `tcp-listen-smoke.sh` 1/2 (matches pre-fix baseline; scenario-1 SLIRP-RX gap iron-only). cyrius 6.0.1 + gnoboot 0.4.2 unchanged. See § *1.32.1 cycle* below + [`iron-nuc-zen-log.md` § Attempt 94](iron-nuc-zen-log.md) (Attempt 95 burn deferred — first-burn-of-next-cycle scope).
>
> **Crate registries**: [`planning/shared-crates.md`](planning/shared-crates.md) (full, incl. pre-1.0); [`docs/applications/libs/README.md`](../applications/libs/README.md) (v1.0+ subset).

### 1.32.3 cycle (OPEN — BSD/iPXE-shape r8169 init rewrite LANDED; Iron Attempt 100 PENDING USER BURN; Linux MCU body bundle REJECTED unburned)

> **🎯 Open hypothesis + expectations**: [`iron-nuc-zen-log.md#tracker-1323-cycle`](iron-nuc-zen-log.md#tracker-1323-cycle) — full per-CMOS-slot PASS/PARTIAL/FALSIFIED rubric for Attempt 98 + recovery checklist for fresh agents landing cold.

Cycle theme: **find the actual load-bearing iron OFFER-timeout bug WITHOUT any more burns** per [[feedback_iron_burns_block_other_work]]. Threads landed in this cycle:

| # | Bite | Iron-validatable? | LOC actual | Status |
|---|------|-------------------|-------------|--------|
| A | **virtio-net modern rewrite + audit doc** — `virtio_blk.cyr`-shape modern PCI cap-list, full MMIO, FEATURES_OK, polled used-ring. | QEMU only | ~510 LOC in `virtio_net.cyr` | ✅ **LANDED 2026-05-23** — QEMU DHCP works end-to-end (pcap-verified: DISCOVER 291 B → SLIRP OFFER 590 B (L2+L3 broadcast) → REQUEST 298 B → SLIRP ACK 590 B → gratuitous ARP) |
| B | **r8169 RX-path 5-part bundle** — Part A multi-frame budget loop, Part B RES + FS\|LS gating, Part C `r8169_rx_rearm` with EOR preserve, Part D state-transition CMOS stamps (~8 µs/poll tax removed), Part E RxMaxSize Linux-converge. | Iron Attempt 97 | ~50 LOC net in `r8169.cyr` | ✅ **LANDED 2026-05-23** — code complete; **iron Attempt 97 PARTIAL**: 5-part bundle works mechanically (16 frames consumed vs stuck-at-1 baseline; EOR + MAR + state-transition stamps healthy) but **NOT load-bearing for OFFER timeout** (16 frames all multicast, OFFER never in ring → root cause upstream of `r8169_poll`). |
| C | **post-Attempt-97 multi-source diagnostics** — Python AF_PACKET DHCP probe (`/tmp/dhcp_probe.py`) + AGNOS-in-QEMU pcap (`/tmp/agnos_qemu_dhcp.sh` + `/tmp/pcap_decode.py`) + Linux journalctl chip-ID + Linux v7.0 r8169 source fetch. **All without a burn.** | n/a (diagnostics) | 0 LOC AGNOS-side; ~350 LOC investigation scripts | ✅ **LANDED 2026-05-23 15:00-15:30 PDT** — falsified branches (b1) AB-filter-missing (code-read: AB IS set in OR) + (b2) server same-MAC silence (server cheerfully OFFERed both synthetic + real MAC). Identified archaemenid chip as **RTL8168h XID 541 = mac_version 46**; Linux `rtl_init_rxcfg` VER_40..52 branch writes `0xCF00` (RX128_INT_EN \| RX_MULTI_EN \| RX_DMA_BURST \| RX_EARLY_OFF); AGNOS was writing `0xE700` (legacy VER_07..17 8168A/B profile). Diff was `RX_EARLY_OFF` (bit 11) clear → broadcast frames dropped mid-DMA. |
| D | **Chip-rev RxConfig fix** — `R8169_RXCFG_DEFAULTS = 0xE700` → `0xCF00`. ONE 16-bit constant. Source comment expanded with Linux line-cite + per-mac_version table. | Iron Attempt 98 | 1 LOC change + ~17 LOC comment | ✅ **LANDED 2026-05-23 15:31 PDT** + ⚠ **FALSIFIED on iron at Attempt 98** — CMOS byte-identical to Attempt 97 (16 multicast frames, no OFFER). `RX_EARLY_OFF` was a real bug but not load-bearing for the OFFER-timeout symptom. |
| E | **5-bite minimum-viable bundle** — Cfg9346-wrapped 32-bit MAC writes (items 2/3), RXDV-gate disable (item 1), `rtl_reset_packet_filter` (item 4), MAR=all-1s multicast hash (item 5), RxConfig-RMW-after-CR.RE+TE (item 8). Per `r8169-chip-init-audit.md` (post-Attempt-98 audit). | Iron Attempt 99 | ~80 LOC in `r8169.cyr` | ✅ **LANDED 2026-05-23 ~16:30 PDT** + ⚠ **FALSIFIED on iron at Attempt 99** — photo `1323_after_fixes_failure.jpg` at agnosticos top level; CMOS readback again byte-identical to Attempts 97/98 (0x58=0x01 0x59=0x01 0x5A=0x02 0x5B=0x30 0x5C=0x10 0x5D=0x78 0x5E=0x01). Hardware unicast filter + RXDV gate + packet-filter-reset + multicast hash all attempted; broadcast OFFER still not reaching the ring. |
| F | **Post-Attempt-99 bundle — full `rtl_hw_start_8168h_1` body + Linux-exact `rtl_hw_start` envelope (items 6+7+9+10+11 stacked)**. ~250 LOC port of Linux v7.0 chip-MCU body: MAC-OCP/EPHY/ERI helpers + `r8169_hw_start_8168h_1` body + ASPM/CLKREQ disable + PCIe L2/L3 disable + Cfg9346-wrapped envelope. | Iron Attempt 100 PENDING (before pivot) | ~250 LOC in `r8169.cyr` | ⚠️ **LANDED 2026-05-23 ~18:10 PDT BUT REMOVED 2026-05-23 ~19:15 PDT — never burned**. Build had reached 622,616 B. User pushback on Linux-centric audit lineage → multi-source research (BSD + iPXE + datasheet) falsified the load-bearing assumption (see bite H). Code REVERTED in bite H rewrite. |
| H | **BSD + iPXE-shape rewrite — REJECT the Linux MCU clone path.** Three parallel research agents pulled FreeBSD `sys/dev/re/if_re.c` + OpenBSD/NetBSD `re.c`/`rtl8169.c` + iPXE `src/drivers/net/realtek.c` + RTL8111B/8168B datasheet + Linux 6.6.2 erratum patch. Four-source agreement: (1) Linux's `rtl_hw_start_8168h_1` body is NOT load-bearing for BCAST/UCAST RX (FreeBSD has ZERO `rl_ephy_write`/`rl_eri_write`/`rl_mac_ocp_write` calls; iPXE works on this chip family with ~6 writes total); (2) RxConfig profile for 8168G_PLUS family is `0xEF00` (RXFTH=7 \| EARLYOFFV2 \| MXDMA=7), NOT Linux's `0xCF00`; (3) RxConfig + accept bits go in ONE write BEFORE `CR=TE\|RE` (NetBSD `rtl8169.c:916` carves out `RTKQ_TXRXEN_LATER` **specifically for `RTK_HWREV_8168H`**); (4) datasheet §2.3 says `CR_RST` preserves IDR0-5 (EEPROM-autoloaded — no writeback needed); §2.9 says Cfg9346 is for CONFIG0-5 only, NOT MAR/RCR/IDR. **Deleted** all chip-MCU helpers + body + ASPM + L2/L3 + Cfg9346 envelope + 32-bit MAC writeback + unused register/flag constants (~280 LOC). **Changed**: `R8169_RXCFG_DEFAULTS` 0xCF00→0xEF00; `r8169_probe` post-reset rewrote to 14-LOC iPXE shape (CPlusCmd PCIMulRW + RXDV gate clear + MAR all-1s); `r8169_init_tx` tail single-write `store32(RXCFG, 0xEF00 \| AB\|AM\|APM)` BEFORE `CR=TE\|RE`. | Iron Attempt 100 PENDING USER BURN | ~−280 LOC, ~+30 LOC = **~−250 LOC net** in `r8169.cyr` (1183 → 840 lines) | ✅ **LANDED 2026-05-23 ~19:15 PDT** — Build 622,616 → **617,192 B** production (`scripts/build.sh`) / **617,984 B** with `TCP_LISTEN_SMOKE=1`. `scripts/test.sh` 4/4 PASS + `scripts/ext2-smoke.sh` 5/5 + 5/5 regression cross-check + `scripts/tcp-listen-smoke.sh` 1/2 (matches baseline; scenario-1 SLIRP-RX gap iron-only). Full DHCP cycle completes on QEMU virtio_net (DISCOVER → OFFER → REQUEST → ACK). Per `feedback_iron_burns_block_other_work` Attempt 100 NOT auto-proposed. See [`r8169-chip-init-audit.md` § BSD + iPXE convergence](r8169-chip-init-audit.md) for the four-source receipt + falsification rubric + zero-burn escalation path. |
| G | **Cycle-close sweep + Attempt 100 transcript** | n/a (doc-roll) | n/a | ⏳ pending burn outcome |

**Convergent investigation receipt** (durable; survives session restart per [[feedback_read_state_at_session_start]]):

1. **Wire baseline**: AF_PACKET probe sent two DISCOVERs from Linux, both got broadcast OFFERs from Araknis switch (192.168.1.1, MAC d4:6a:91:ce:70:60) within ~1 s. Server responds even to active-lease MAC; broadcasts L2+L3 honoring bcast flag.
2. **AGNOS upper-layer baseline**: QEMU + SLIRP DHCP pcap shows SLIRP OFFER is also L2+L3 broadcast (same byte-shape as Araknis), AGNOS catches it cleanly via virtio_net. Upper layers proven clean for broadcast OFFERs.
3. **Chip identification**: `journalctl -k -b 0 | grep r8169` → `RTL8168h/8111h, XID 541, IRQ 85`.
4. **Linux source authority**: `curl https://raw.githubusercontent.com/torvalds/linux/v7.0/drivers/net/ethernet/realtek/r8169_main.c -o /tmp/r8169_main.c`; line 136 maps XID 541 → `RTL_GIGA_MAC_VER_46`; line 2601-2602 maps VER_40..52 → `0xCF00 | accept_bits`.
5. **AGNOS source diff**: `agnos/kernel/core/r8169.cyr:91-94` (pre-fix) had `R8169_RXCFG_DEFAULTS = 0xE700` matching Linux's legacy VER_07..17 branch (line 2594) — wrong chip family entirely.
6. **Lesson** ([[feedback_redesign_dont_reinvent]] follow-up): when porting from a driver with per-chip-revision dispatch, the audit MUST walk the dispatch table for the exact chip; don't lift the "default profile" and assume generality. Folding into audit doc after burn closes the loop.

**Build trajectory across 1.32.3** (with all threads landed):
- 1.32.2 close (Attempt 96 build): 605,056 B
- 1.32.3 r8169 RX-path 5-part bundle (Attempt 97 build): **617,000 B** (+11,944 B / ~440 LOC virtio-net modern + 5-part bundle + audit + smoke harness)
- 1.32.3 RxConfig chip-rev fix (Attempt 98 build): **617,000 B** (same — constant swap doesn't change instruction size)
- 1.32.3 minimum-viable Linux clone (Attempt 99 build): **622,616 B** (+5,616 B / ~250 LOC Linux MCU body port — FALSIFIED on iron)
- 1.32.3 BSD/iPXE rewrite (Attempt 100 build, unburned): **617,192 B** (−5,424 B; ~−250 LOC net deletion of Linux MCU body + Cfg9346/ASPM/L2-L3 helpers; RxConfig profile 0xCF00→0xEF00; single-write accept-bits before CR.RE)

cyrius pin stays on 6.0.1. gnoboot stays on 0.4.2.

### 1.32.0 cycle (CLOSED 2026-05-22 — networking arc feature-complete; Iron Attempts 92 + 93 burned)

> **🎯 Hypothesis tested + result**: [`iron-nuc-zen-log.md#tracker-1320-cycle`](iron-nuc-zen-log.md#tracker-1320-cycle) — networking arc PARTIAL VALIDATED on iron; r8169 lit clean, DHCP gate-fix verified, OFFER-timeout failure mode carried to 1.32.1.

Cycle theme: **kernel server-side TCP primitives + real-iron NIC drivers (r8169 family first, i225-V queued)** with userland consumer apps **BBS + MUD** in parallel out-of-cycle (their own standalone repos). User-stated discipline: *"we will track the items we still need to write work for but will wait until we are on that particular iron"* — every bite below is PENDING IRON; the audit doc + bite table are the durable planning surface, code touches wait for archaemenid time.

**Strategic destination — INSTALLABLE STATE on archaemenid.** The networking push isn't BBS/MUD for their own sake — it's the next ring on the device-class spiral that gets AGNOS to the point of installing itself onto archaemenid's internal NVMe (rather than booting from the USB-stick dev surface every burn). The full path spans multiple cycles:
- **1.32.x (this cycle)** — networking (TCP server primitives + r8169/i225-V NIC drivers); agnova can fetch/talk to the network.
- **1.33.x WRITE** — ext2/4 mutation (block/inode allocator, dirent insertion, file create/truncate/unlink, mkdir/rmdir, write-through commit); agnova can lay down a kernel + initrd + filesystem onto a target disk. Per roadmap row 15.
- **Installable-state cycle (slot TBD)** — agnova matures to functional installer (currently "Ported" per CLAUDE.md repo table); bootloader-install path writes gnoboot + agnos kernel to the target ESP; agnos-fs gets the system tree laid in. archaemenid graduates from "boot-via-USB-stick dev surface" → "primary AGNOS install on internal NVMe."

That graduation event triggers the user's planned hardware migration — **dual-boot pivot, not Linux eviction** (refined 2026-05-22 per user direction). Disk reshuffle within archaemenid's chassis:
- **Beelink internal NVMe slot**: the 1TB ex-USB drive (currently the agnos-test stick) gets pulled from USB-bridge enclosure and installed as the new internal M.2 NVMe → hosts **AGNOS primary**.
- **Beelink internal SATA slot**: WD Blue SA510 2TB **stays in archaemenid** → repurposed as **Linux daily-driver root** (Linux migrates within-machine from the Crucial NVMe to the SATA drive before the Crucial leaves the chassis). UEFI firmware F-key boot menu picks between AGNOS and Linux per boot.
- **OUT of archaemenid → into i9**: the 2TB Crucial P3 NVMe.
- **Test-stick role**: refilled from spare USB Mass Storage inventory.
- i9 ends with **5 TB across three drives**: 1 TB Gen5 NVMe ~14k MB/s R/W (Linux daily-driver) + 2 TB Windows holdout (until the Cyrius + AGNOS stack reaches Windows-workload parity) + 2 TB Crucial P3 (migrated, storage tier).

Pivot rationale: by 1.33.x WRITE landing AGNOS crosses the **demo→base** threshold (per [roadmap maturity arc](../development/roadmap.md#maturity-arc)) — usable as a primary boot target. Linux stays around on SATA as the dual-boot fallback / day-job surface. NOT a claim that AGNOS replaces Linux on day one; that's the **server-stage** event when most archaemenid Linux usage (server-flavored) gets natively absorbed.

Captured durably in [[project_hardware_catalog]] + [[project_archaemenid_install_plan]] + [[project_agnos_maturity_arc]].

**Companion tooling — `scripts/install-usb.sh` upgrade (2026-05-22):** the host-side provisioning script gained a second GPT partition step. Provision mode now creates `256 MiB ESP` (FAT32, gnoboot + kernel + initramfs) + `25 GiB ext4 agnos-fs` (GPT name + fs label `agnos-fs`, default `mkfs.ext4` → 64BIT-enabled per Attempt 91 profile), pre-seeded with `/hello.txt`, `/welcome.txt`, `/agnos/notes.txt`. `--update` mode stays ESP-only — writes the user lands on `agnos-fs` (especially 1.33.x WRITE bring-up writes) survive every kernel-iteration cycle. This is the **base-stage** persistence surface getting prepped ahead of 1.33.x landing. Per [[feedback_prefer_mount_modify_over_reflash]], AGNOS-side seed changes should mount the partition from Linux and edit in place — reserve `--update` reflash for kernel/gnoboot binary changes.

| # | Bite | Iron-validatable? | LOC estimate | Status |
|---|------|-------------------|--------------|--------|
| A | **TCP server primitives** — `tcp_listen(port)` / `tcp_bind(port, ip)` / `tcp_accept(listen_id)` / passive-open SYN handler + SYN_RCVD state branch + `tcp_find_listen` + ARP REQUEST handler + flag-field metadata bit packing. | QEMU + VirtIO-net (scenario 2 PASS); accept-success path blocked on SLIRP-inbound delivery (orthogonal QEMU issue) | ~188 LOC in `net.cyr` + ~40 smoke hook + ~180 smoke harness bash | ✅ **LANDED 2026-05-22 (code complete; 4/4 test.sh PASS; smoke 1/2 PASS — listen-no-connect scenario validates, accept-success deferred to iron NIC bite B/C)** |
| F | **UDP server-side primitives** — `udp_bind(port)` / `udp_recv_from(listener_id, buf, maxlen, &src_ip, &src_port)` / `udp_send_from` (egress with explicit src_ip override) / `udp_find_listener` / per-listener kmalloc'd 256B buf / 8-listener table + per-arrival dispatch in `net_handle_udp`. Foundation for bite G. | QEMU (orthogonal SLIRP-RX gap blocks end-to-end smoke); iron-NIC bite B/C validates end-to-end | ~95 LOC in `net.cyr` | ✅ **LANDED 2026-05-22 (code complete; 4/4 test.sh PASS; integrated smoke via bite G)** |
| G | **DHCP client** — RFC 2131 DISCOVER → OFFER → REQUEST → ACK. `dhcp_init()` at boot, builds the 240-byte BOOTP fixed header + options (53/55/50/54), parses option blob (msg-type / server-id / netmask / gateway), sets net_ip/netmask/gateway on ACK. On failure leaves net_init's fallback (10.0.2.15 / 10.0.2.2 / 255.255.255.0) in place. Consumes bite F's udp_bind/udp_recv_from. | QEMU: DISCOVER sent but **no inbound** (zero udp_rx lines), same SLIRP-RX gap as bite A; iron-NIC bite B/C will validate the full exchange | ~260 LOC in `net.cyr` + ~5 LOC in `main.cyr` | ✅ **LANDED 2026-05-22 (code complete; 4/4 test.sh PASS; smoke iron-deferred)** |
| B | **r8169 driver** — Phases 1-4 ✅ all landed. Iron anchor queried via `lspci`/sysfs on archaemenid (per [[feedback_archaemenid_is_dev_host]]): `10ec:8168` rev 15 RTL8111/8168 at BDF `0000:01:00.0`, MAC `b0:41:6f:0c:e4:25`, BAR2 MMIO `0xFCF04000` (4 KB), BAR4 `0xFCF00000` (16 KB MSI-X). **Phase 2**: RX descriptor ring (16 entries × 16 B at page-aligned phys via pmm_alloc) + per-buffer pmm_alloc pages + `r8169_init_rx` + `r8169_poll`. **Phase 3**: TX descriptor ring (mirror shape) + `r8169_init_tx` + `r8169_send` + TPPoll NPQ kick. **Phase 4**: NIC dispatcher (`nic_ready` / `nic_send` / `nic_poll` in r8169.cyr; priority r8169 > virtio_net) + net.cyr migration (6 `virtio_net_send`→`nic_send`, 1 `virtio_net_poll`→`nic_poll`, 7 `vnet_active`→`nic_ready` checks) + agnos.cyr include reorder (r8169.cyr before net.cyr). Multi-source convergent per audit doc § 1.4. | All Phases 1-4 code: 4/4 test.sh PASS in QEMU + boot-smoke clean (silent no-op r8169 path; nic_send correctly dispatches through virtio_net_send on no-r8169 path — confirmed by `dhcp: DISCOVER` print). Phase 5 iron-burn Attempt 92+ validates the full chain on archaemenid (Phase 1 4 lines + Phase 2 RX-up line + Phase 3 TX-up line + bite-A scenario-1 accept-success + DHCP full cycle). | Phase 1 ~150 LOC + Phase 2 ~110 + Phase 3 ~85 + Phase 4 ~55 = **~400 LOC LANDED** | **Phases 1-4 ✅ LANDED 2026-05-22 (code complete + iron-anchored); Phase 5 = `r8169-iron-burn-audit.md` + Attempt 92+ burn PENDING IRON** |
| C | **i225-V driver** — Phase 1-3 mirror; targets Beelink-SER Intel-2.5GbE variants + future Intel iron (i9 post-migration). | **Iron-only** (Intel-NIC iron, post-migration ideally). | ~700–1100 LOC | **PENDING dedicated Intel-NIC iron + post-r8169 closeout** |
| D | **Iron debut burns** — first AGNOS LAN packets on archaemenid (r8169 driver Phases 1-4 + retroactive validation of bites A/F/G + DHCP gate fix verification). **Pre-burn audit landed** at [`r8169-iron-burn-audit.md`](r8169-iron-burn-audit.md) (9 hypotheses ranked; H1 = PHY-not-configured is highest-risk; success rubric defined; iron-burn checklist for user). | **Iron-only** (Attempts 92 + 93 burned 2026-05-22). | ~50 LOC test surface (already in driver; no new code) + ~200 LOC audit doc | ✅ **Attempt 92 + 93 BURNED 2026-05-22**: **Attempt 92 PARTIAL** lit r8169 Phases 1-4 byte-clean on iron (`found at 4243603456` = BAR2 0xFCF04000 byte-match lspci; `MAC=176:65:111:12:228:37` = `b0:41:6f:0c:e4:25` byte-match lspci; `chip-rev byte=0x87`; `reset OK; Phase 1 complete`; `RX ring up (16 desc)`; `TX ring up (16 desc)`) but DHCP silent — root cause: `main.cyr:655` gate keyed on `vnet_active` only (virtio-net flag), never fires `dhcp_init()` on r8169-only iron path. Same-day fix LANDED per user direction: explicit OR with generic abstraction on RHS — `if (vnet_active != 0 || nic_ready() != 0)` — so future NIC backends (i225-V queued) don't force another gate edit. **Attempt 93 PARTIAL VERIFIED the gate fix on iron** — `dhcp: DISCOVER` line now egresses through the r8169 path (was absent in Attempt 92); proves the gate predicate was the only thing blocking the H1/H7/H8 driver-level hypothesis surface from being reachable. New failure mode: `dhcp: OFFER timeout` — DISCOVER goes out, no OFFER comes back within the timeout window — moves to 1.32.1 carry-forward. Storage trio + GPT + ext4 mount + kybernet + shell all byte-clean across both burns (no-regression). Build 600,520 → 601,392 B + 4/4 test.sh PASS. See iron-nuc-zen-log § Attempts 92 + 93. |
| E | **Cycle-close sweep + Attempts 92 + 93 transcripts** — state/roadmap/iron-log sweep + CHANGELOG release entry + photo capture. | n/a (doc-roll). | n/a | ✅ **LANDED 2026-05-22** — three photos catalogued (`attempt-92-…-pt1`/`pt2.jpg` + `attempt-93-…-dhcp-gate-fix-verified-offer-timeout.jpg`); Attempts 92 + 93 receipts in iron-nuc-zen-log with verbatim chains, root-cause analysis, gate-fix derivation + verification, discriminator outcomes; CHANGELOG 1.32.0 release entry with cycle-close summary + carry-forward block; state.md refresh marker + cycle header + bite D + this row + 1.32.1 carry-forward section updated. **Awaiting user tag of 1.32.0 per [[feedback_no_unprompted_version_bumps]]; 1.32.1 cycle opens after tag.** |

**Multi-source convergent audit doc**: [`network-arc-prior-art.md`](network-arc-prior-art.md) per `feedback_redesign_dont_reinvent`. Sections: (1) r8169 references (Linux `drivers/net/ethernet/realtek/r8169_main.c` + FreeBSD `if_re.c` + OpenBSD `re.c` + NetBSD `re.c` + Haiku + RealTek datasheet), (2) i225-V references (Linux `drivers/net/ethernet/intel/igc/` + FreeBSD `igc/`), (3) TCP server-primitives references (Linux `net/ipv4/tcp_ipv4.c` + BSD socket impl + xinu listen-accept micro-impl), (4) BBS/MUD wire-protocol references (RFC 854 Telnet + RFC 1184 LINEMODE + DikuMUD protocol shape).

**Existing networking baseline** (kernel-side, pre-cycle):
- TCP **client**: `tcp_connect`/`send`/`recv`/`close` + full SYN/ACK/FIN state machine + checksum. ~532 LOC in `kernel/core/net.cyr`.
- TCP **server**: ❌ NONE — `tcp_listen`/`tcp_bind`/`tcp_accept` absent. First concrete kernel-shape gap surfaced by BBS/MUD consumer apps.
- UDP: egress only (`udp_build`, `udp_send`). Ingress + bind path needed for DNS.
- Real-iron NIC: ❌ NONE — `virtio_net.cyr` (148 LOC) covers QEMU only.

Build trajectory across the 1.32.0 cycle: 578,432 B (1.31.7 close) → 600,432 B (production, post-bite-A/F/G + r8169 Phases 1-4) → 600,520 B (`TCP_LISTEN_SMOKE=1` variant, Attempt 92 burn) → **601,392 B (post-gate-fix, `TCP_LISTEN_SMOKE=1`, Attempt 93 burn — cycle-close build)**. Total cycle delta: **+22,960 B / ~1,000 LOC** across `net.cyr` + `r8169.cyr` + `main.cyr`. cyrius pin stays on 6.0.1. gnoboot stays on 0.4.2.

### 1.32.2 cycle (OPEN 2026-05-23 — sweep-hardening; FIX #7/#8/#9/#10 landed; Attempt 96 pending user direction)

> **🎯 Open hypothesis + expectations**: [`iron-nuc-zen-log.md#tracker-1322-cycle`](iron-nuc-zen-log.md#tracker-1322-cycle) — what FIX #10 must show on Attempt 96 to confirm it's the load-bearing fix, and the falsification rubric per CMOS slot.

Cycle theme: **post-Attempt-95 full-stack sweep — find everything upstream + downstream of the original FIX-#1-#6 scope and land it together, no piecemeal iron-burn ladder**. Per user direction *"lets do a sweep hardening to insure nothing else is missed; don't want to piecemeal this"* + *"DO ALL THE BUGS"*. Three bugs surfaced; all landed in one bite to preserve a single Attempt 96 validation surface.

**Strategic posture**: this cycle is corrective, not feature-additive. The 1.32.0 cycle landed the networking arc feature-complete (TCP server + UDP server + DHCP client + r8169 driver). 1.32.1 attempted a 6-FIX wiring repair pre-Attempt-95. Attempt 95 showed the symptom unchanged, so 1.32.2 widens the audit scope: NIC hardware MAC filter (FIX #7), UDP buffer geometry (FIX #8), DHCP retransmission shape (FIX #9). Multi-source convergent per [[feedback_redesign_dont_reinvent]] — every fix cross-validated against Linux + OpenBSD + FreeBSD + NetBSD + Haiku + RealTek datasheet. Full writeup in [`dhcp-end-to-end-audit.md` § 10](dhcp-end-to-end-audit.md).

| # | Bite | Iron-validatable? | LOC actual | Status |
|---|------|-------------------|-------------|--------|
| A | **FIX #7 — IDR0..IDR5 reprogrammed after reset**. The NIC's hardware unicast MAC filter is zeroed by CR.RST; `RxConfig.APM=1` gates unicast acceptance on this filter. Pre-fix any unicast DHCP OFFER to our MAC was rejected at the hardware filter before reaching the RX ring. The 1.32.1 audit FINDING #6 saw this but FIX #6 only renamed RxConfig constants. Linux `rtl_rar_set` + OpenBSD `re_setaddr` + FreeBSD/NetBSD/Haiku all write MAC0/MAC4 after reset. AGNOS now does too. | Iron Attempt 96 (CMOS 0x5E expected to flip from 0x01 to 0xFF or 0xB0). | ~6 LOC in `r8169.cyr` step 8b. | ✅ **LANDED 2026-05-23** |
| B | **FIX #8 — UDP buffer + DHCP rx sized for full DHCP payloads**. `udp_bind` `kmalloc(256)` → `kmalloc(1024)`; `net_handle_udp` cap `248` → `1016`; `dhcp_init` local `var rx[320]` → `var rx[1024]` with both `udp_recv_from(... 320 ...)` call-sites bumped to 1024. Pre-fix DHCP OFFER/ACK option blob was truncated past server-id + subnet + gateway. | Iron Attempt 96 (REQUEST/ACK roundtrip surface). | ~6 LOC across 4 sites in `net.cyr`. | ✅ **LANDED 2026-05-23** |
| C | **FIX #9 — DHCP DISCOVER + REQUEST midpoint retransmit**. Pre-fix send-once-and-wait. Per RFC 2131 §4.4.1, clients SHOULD retransmit with exponential backoff. Minimum-viable form: single retransmit at `iter == 400` (mid-window) in both OFFER + ACK wait loops; same xid (RFC requires same session id). Full exp-backoff deferred to a later refinement cycle. | Iron Attempt 96 robustness surface. | ~30 LOC in `net.cyr` `dhcp_init`. | ✅ **LANDED 2026-05-23** |
| C2 | **FIX #10 — PHY autoneg restart only if link actually down**. The LOAD-BEARING fix per the Attempt 94→95 regression evidence (CMOS 0x5B 0x30→0xb0 + 0x5E 0x01→0x00). FIX #3's unconditional `BMCR.ANRESTART` took the link down for 1-3s post-restart; init_rx/init_tx/scheduler all ran within ~100ms during that link-down window; some RTL8168 variants wedge their TX/RX engines under that condition. Per Linux `phy_start` + OpenBSD `re_init` + FreeBSD `re_init_locked` — autoneg restart is a state-change event (suspend/media-change/link-drop), not a probe-time default. New shape: read BMSR.LinkStatus first (double-read for IEEE 802.3 §22.2.4.2 latching-low); preserve BIOS-up state through init. | Iron Attempt 96 (CMOS 0x5B expected back to 0x30 cleared; 0x5E ≥ 0x01). Boot block prints new line `r8169: PHY link up (preserved from BIOS)` as the on-screen confirmation. | ~10 LOC net delta in `r8169.cyr` `r8169_phy_init`. | ✅ **LANDED 2026-05-23** |
| D | **Iron Attempt 96 — validates FIX #7+#8+#9+#10 against Attempt 95 evidence base**. Target outcome: full DHCP cycle on archaemenid (DISCOVER → OFFER → REQUEST → ACK four lines). CMOS 0x5E flips from `0x01` (background multicast leftover) to `0xFF` (broadcast OFFER) OR `0xB0` (unicast OFFER to our MAC — unambiguous FIX #7 success signal). | Iron-only. | n/a | ⚠️ **ATTEMPT 96 BURNED 2026-05-23 — PARTIAL (4-FIX bundle FALSIFIED on the OFFER-timeout symptom)**. Boot block printed BOTH `r8169: PHY autoneg kicked (link async)` AND `r8169: link up` BEFORE RX/TX rings initialized — i.e., FIX #10's safe-path branch fired, link was up by init_rx/init_tx, the engine-wedge mechanism the bundle was designed to prevent did not apply. **DHCP still timed out** (`dhcp: DISCOVER → dhcp: OFFER timeout`). Storage trio + GPT + ext4 mount + scheduler + kybernet + shell byte-clean (no regression). Photos catalogued: [`attempt-96-…-r8169-link-up-rx-tx-rings-up.jpg`](iron-nuc-zen-photos/attempt-96-agnos-1.32.2-r8169-link-up-rx-tx-rings-up.jpg) + [`attempt-96-…-fix-7-8-9-10-offer-timeout-persists.jpg`](iron-nuc-zen-photos/attempt-96-agnos-1.32.2-fix-7-8-9-10-offer-timeout-persists.jpg). **NEXT STEP IS ZERO-COST READBACK, NOT A BURN**: `sudo ./scripts/read-boot-log.sh --verbose` (correct spelling — typo silently fell through to default summary; arg-parser else-branch fix landed same-bite) to read CMOS 0x58-0x5F from the current cold-boot's CMOS bank. 0x5B + 0x5E values disambiguate branch (a) engines healthy / root cause upstream of NIC vs branch (b) wedge via different mechanism than hypothesized. See [iron-nuc-zen-log § Attempt 96](iron-nuc-zen-log.md) for full receipt + branch rubric. |
| E | **Cycle-close sweep + Attempt 96 transcript**. CHANGELOG cycle-close summary; state/roadmap/iron-log sweep; photo catalogue. | n/a (doc-roll). | n/a | ⏳ Partially landed — iron-log Attempt 96 entry + photo catalogue + state.md refresh ✅ 2026-05-23; CHANGELOG cycle-close blocked on branch (a)/(b) outcome from CMOS readback. |

**Build trajectory in 1.32.2 (in-flight)**: 604,096 B production (1.32.1 close) → 605,064 B (post-FIX-7+8+9) → **605,056 B (post-FIX-7+8+9+10)**. FIX #10 net delta is slightly negative (one of the two BMSR-read sites is now inside the conditional `if (link down)` branch, not unconditional). Cycle delta: **+960 B** across `r8169.cyr` + `net.cyr`. cyrius pin stays on 6.0.1. gnoboot stays on 0.4.2.

**Out-of-cycle carry-forward continues** (unchanged from 1.32.1 cycle): i225-V driver port (Intel-NIC iron pending), BBS + MUD userland (separate standalone repos).

### 1.32.1 cycle (CLOSED 2026-05-22 — TAGGED by user; Attempt 95 burned 23:42 PDT, OFFER timeout SURVIVED — 6-FIX bundle insufficient; three additional bugs surfaced in 1.32.2 sweep)

> **🎯 Hypothesis tested + result**: [`iron-nuc-zen-log.md#tracker-1321-cycle`](iron-nuc-zen-log.md#tracker-1321-cycle) — 6-FIX bundle FALSIFIED on iron; FIX #3 introduced the NIC engine regression. Four shortcomings folded forward into 1.32.2 FIX #7-#10.

Cycle theme: **r8169 driver-level OFFER-timeout debug** — H1 PHY-not-configured / H7 TX OWN stuck / H8 RX OWN stuck, the audit-hypothesis surface from Attempt 93's pre-burn rubric, now reachable post-gate-fix. User-stated discipline: *"track items we still need to write work for, wait until on that particular iron"* — every bite below is PENDING IRON; audit doc + bite table are the durable planning surface, code touches wait for archaemenid time.

| # | Bite | Iron-validatable? | LOC estimate | Status |
|---|------|-------------------|--------------|--------|
| A | **OFFER-timeout audit doc** — §5b extension to [`r8169-iron-burn-audit.md`](r8169-iron-burn-audit.md): line-by-line examination of H1 (PHY not configured / no link) / H7 (TX OWN bit stuck) / H8 (RX OWN bit stuck) against current `r8169.cyr` code + multi-source prior art (Linux `r8169_main.c` + FreeBSD `if_re.c` + OpenBSD `re.c` + NetBSD `re.c` + Haiku + RTL8168 datasheet). | n/a (doc-only). | 0 LOC code; ~260 LOC audit prose. | ✅ **LANDED 2026-05-22** — audit doc grew 221 → 482 lines with § 10.1-10.8 covering: post-Attempt-93 reachability re-rank (6 of 9 hypotheses falsified, H1+H5+H7+H8 reachable, H1 top candidate); line-by-line H1/H7/H8 vs current code; CMOS slots 0x58-0x5F discriminator plan (avoiding xhci's 0x60-0x6F + AS1 0x56-0x57); corrective-patch shape per hypothesis (OpenBSD-converged minimum PHY init = primary fix); cycle ordering (B+C stack into Attempt 94). |
| B | **Discriminator instrumentation** — CMOS-bank stamps at slots 0x58-0x5F per audit § 10.5: 0x58 probe-done sentinel + 0x59 PHY-init outcome enum + 0x5A TX send count (saturating) + 0x5B TX desc 0 OWN bit + 0x5C RX poll count (saturating) + 0x5D RX desc 0 OWN bit + 0x5E RX buf first byte (DMA evidence) + 0x5F reserved. Stamps refresh every `r8169_poll` invocation (poll runs in scheduler idle = constantly fresh). Re-uses existing `xhci_cmos_stamp(slot, val)` primitive at `xhci_port.cyr:61-70` (generic two-byte CMOS write; xhci-prefixed name kept to avoid cross-cutting rename). | Iron-validatable (Attempt 94 reads CMOS via `read-boot-log` post-burn). | ~50 LOC in `r8169.cyr` (counter vars + 7 stamp slots + 5 stamp sites across probe/send/poll) + ~30 LOC in `scripts/src/read-boot-log.cyr` (decoder labels). | ✅ **LANDED 2026-05-22** — code complete; 4/4 test.sh PASS; build 601,392 → 603,784 B (+2,392 B / ~80 LOC effective). QEMU smoke unchanged (no r8169 device in QEMU = silent no-op path; virtio_net dispatcher still wins). |
| C | **Driver-level corrective patch — PHY init** — added `r8169_phy_read` / `r8169_phy_write` MDIO helpers using PHYAR register at MMIO 0x60 (per RTL8168 datasheet §11 + OpenBSD `re_phy_init` converged shape) + `r8169_phy_init` that reads BMCR, sets autoneg-enable + restart-autoneg, clears power-down, polls BMSR.LinkStatus (300 iterations × ~10ms = ~3s autoneg budget, matches BSD-converged timeout). Called from `r8169_probe` after reset OK, before "Phase 1 complete" print. Outcome printed inline (`r8169: link up` or `r8169: no link (autoneg timeout)`) AND stamped to CMOS 0x59 with fine-grained enum (1=link up, 2=autoneg timeout, 3=BMCR-write timeout, 4=BMSR-read timeout). | Iron-validatable (Attempt 94 — boot log + CMOS readback). | ~85 LOC in `r8169.cyr` (3 functions + 7 PHY-related constants + probe-site hook). | ✅ **LANDED 2026-05-22** — code complete; 4/4 test.sh PASS; **stacked with bite B in single build**. Multi-source convergent per audit § 10.2 (OpenBSD `re_phy_init` primary, cross-validated against Linux `r8169_phy_config.c` / FreeBSD `if_re.c` / NetBSD `re.c` / Haiku / RealTek datasheet §11+§13). |
| D | **Iron Attempt 94+** — re-burn with bite B+C bundle. Target outcome: full DHCP cycle on archaemenid. Post-burn `sudo ./scripts/read-boot-log.sh --verbose` reads CMOS 0x58-0x5F for per-hypothesis discriminator. Iron-burn checklist: build is current at `agnos/build/agnos` 603,784 B (TCP_LISTEN_SMOKE=1) or production-build re-run; cable connected; capture boot-log photo at shell prompt; then run read-boot-log under sudo for CMOS decode. | Iron-only (archaemenid r8169). | n/a (no new code in this row; bites B+C carry the surface). | ⚠️ **ATTEMPT 94 BURNED 2026-05-22 — PARTIAL (audit § 10 framing INVALIDATED on iron — r8169 is functional; OFFER-timeout root cause is upstream of NIC)** — bites B+C build verified on iron via shell banner `v1.32.1`. Bite-B CMOS 0x58-0x5F readback **falsified H7 + H8 directly** (0x5B=0x30 = NIC cleared TX OWN, 0x5E=0x01 = RX DMA captured a real byte, 0x5C=0xFF = poll loop saturated, 0x5A=2 = TX sends fired). 0x59=2 surface signal traces to a **bite-C BMSR-latching-low false negative**, not a real PHY failure (IEEE 802.3 §22.2.4.2 — Linux/OpenBSD/FreeBSD all read BMSR twice; bite C likely reads once). Storage trio + GPT + ext4 mount + scheduler + tcp_listen + kybernet + shell byte-clean. **Next move: end-to-end DHCP wiring audit doc (separate from r8169-iron-burn-audit) — trace `dhcp_init` → UDP/IP egress → r8169_send → wire, then RX path → udp_recv_from → OFFER parser. Bite C BMSR fix folded into the audit. NO Attempt 95 burn proposed until audit lands** per [[feedback_iron_burns_block_other_work]]. See [`iron-nuc-zen-log.md` § Attempt 94](iron-nuc-zen-log.md). |
| E | **Cycle-close sweep + Attempt 94+ transcript** — state/roadmap/iron-log sweep + photo capture + CHANGELOG cycle-close summary. | n/a (doc-roll). | n/a | ⏳ **IN-FLIGHT (re-cast post-readback)** — Attempt 94 transcript landed in iron-nuc-zen-log with corrected framing (r8169 functional, OFFER timeout upstream of NIC, bite C has BMSR-latching-low bug); photo catalogued (`iron-nuc-zen-photos/attempt-94-agnos-1.32.1-phy-init-instrumentation-offer-timeout-persists.jpg`); state.md bite D updated. **2026-05-22 reshape**: Per user direction "fix all the issues" + "all still in 1.32.1 cycle work, most likely close after [next iron] burn either way" — six fixes from [`dhcp-end-to-end-audit.md`](dhcp-end-to-end-audit.md) landed same-day IN the 1.32.1 cycle (no version bump per [[feedback_no_unprompted_version_bumps]]). 1.32.2 reshape retired; 1.32.1 closes at Attempt 95. |
| F | **DHCP end-to-end audit + 6-FIX wiring repair** (post-Attempt-94 reshape) — multi-source convergent audit doc + landed fixes. | Iron Attempt 95 validates. | ~177 LOC total | ✅ **AUDIT + ALL 6 FIXES LANDED 2026-05-22** in `agnosticos/docs/development/dhcp-end-to-end-audit.md` + `agnos/kernel/core/{net,r8169,main}.cyr`. FIX #1: `nic_mac()` backend-agnostic MAC accessor (PRIMARY root cause for OFFER timeout — `vnet_mac` was zero on iron, DHCP DISCOVER chaddr=00:00:00:00:00:00); FIX #2: `net_init(0,0,0)` on iron path; FIX #3: `r8169_phy_init` non-blocking kick (busy-wait was ~100× too short); FIX #4: OFFER+ACK chaddr validation per RFC 2131 §4.1.1; FIX #5: timeout 200→800 iter (~2s→~8s); FIX #6: RxConfig named-constant + datasheet citation (no behavioral change). Build 603,784 → **604,096 B production / 604,904 B TCP_LISTEN_SMOKE** (+312 / +584 B). `scripts/test.sh` 4/4 PASS + `ext2-smoke.sh` 5/5 + 5/5 regression cross-check + `tcp-listen-smoke.sh` 1/2 (matches pre-fix baseline, scenario 1 is iron-only SLIRP-RX gap). Zero QEMU regression. |
| G | **Iron Attempt 95** — full DHCP cycle validation. Target outcome: `dhcp: DISCOVER` → `dhcp: OFFER ip=<lan-IP>` → `dhcp: REQUEST` → `dhcp: ACK ip=<lan-IP> gw=<gw> mask=<mask>` four lines. New r8169 boot block ends in `PHY autoneg kicked; link up` or `(link async)`. CMOS 0x59 expected to flip 2→1; 0x5E expected to flip 0x01→0xFF or 0xB0. | Iron-only (archaemenid). | n/a | ⚠️ **ATTEMPT 95 BURNED 2026-05-22 23:42 PDT — PARTIAL (build correct on iron; 6-FIX bundle did NOT clear OFFER timeout)**. Logged retroactively 2026-05-23 — prior-session agent missed the entry before context loss; photo at `attempt-95-agnos-1.32.1-post-fix-phy-up-tx-rx-still-wedged.jpg` is the evidence. Symptom identical to Attempts 93+94: `dhcp: DISCOVER` → `dhcp: OFFER timeout`. Storage trio + GPT + ext4 mount + scheduler + kybernet + shell byte-clean. Three additional bugs surfaced in post-burn sweep (FIX #7 IDR reprogramming, FIX #8 UDP buf sizing, FIX #9 DHCP retransmit) — landed in 1.32.2; see § *1.32.2 cycle* below. **Attempt 96** validates the 1.32.2 FIXes against this evidence base — deferred to user. |

**Out-of-cycle carry-forward** (continues from 1.32.0; not gated by 1.32.1 close):

- **i225-V driver port** — Intel 2.5GbE family (~700-1100 LOC). Pending dedicated Intel-NIC iron — likely post-archaemenid-migration when i9 gains the Intel-NIC surface. Mirrors r8169 Phase 1-3.
- **BBS + MUD userland consumer apps** — separate standalone repos (out-of-cycle by design). Arrive when wire-end-to-end has DHCP ACK + TCP accept-success on iron. Move in parallel.

**Multi-source convergent audit shape**: `r8169-iron-burn-audit.md` § 5b extends the existing 9-hypothesis ranking. Section ordering: (5b.1) what Attempt 93 changed about hypothesis reachability; (5b.2) H1 PHY-init line-by-line vs current `r8169.cyr:reset_chip` + Linux `r8169_main.c:rtl_hw_phy_config_*` family; (5b.3) H7 TX OWN walk vs `r8169_send` post-write semantics + FreeBSD `re_encap` completion polling; (5b.4) H8 RX OWN walk vs `r8169_poll` semantics + OpenBSD `re_rxeof` ring-walk; (5b.5) discriminator-instrumentation proposal (CMOS-bank stamps, minimum-viable surface); (5b.6) corrective-patch shape per hypothesis (Linux convergent as primary, BSD/Haiku as cross-validation).

**Build trajectory in 1.32.1 (in-flight)**: 601,392 B (1.32.0 close) → 601,392 B (1.32.1 open, zero-structural-delta) → **603,784 B** (post-bite-B+C, `TCP_LISTEN_SMOKE=1`) / **602,976 B** (post-bite-B+C, production). Cycle delta so far: **+2,392 B / ~80 LOC effective in `r8169.cyr`** + ~30 LOC in `scripts/src/read-boot-log.cyr`. cyrius pin stays on 6.0.1. gnoboot stays on 0.4.2.

### 1.31.7 cycle (CLOSED 2026-05-22 — Iron Attempt 91 PASS)

Closes the ext4 64BIT pin from row 7b of the agnos roadmap + tightens the Phase-3 shell-UX papercuts surfaced in Attempt 90's transcript (bare-name `cat hello.txt` falling to initrd; `ls -la` failing with `ls: not found`). Five bites in smallest-first order to bank wins quickly; one cycle-close iron burn (Attempt 91 PASS) validates the four code bites + storage-trio no-regression. No new iron-validation surface — [`ext2-iron-burn-audit.md`](ext2-iron-burn-audit.md) (bite E of 1.31.6) covered Attempt 91 as well.

| # | Bite | Status | Notes |
|---|------|--------|-------|
| D | `ls -la` long-form dispatch (~25 LOC net) | ✅ landed + IRON-VALIDATED Attempt 91 (help output) | `sh_cmd_ls` parses flag tokens before the path; Phase-3 listing format unchanged (flags accepted syntactically). Same-touch cleanup: removed dead duplicate `ls`/`fatfs_ls` handler from the dispatcher. Build 571,296 → 571,728 B. |
| B | bare-name `cat` ext2 fall-through (~20 LOC net) | ✅ landed + IRON-VALIDATED Attempt 91 | `sh_cmd_cat` now prefixes bare names with `sh_cwd_path` and tries ext2 before falling back to initrd. Iron: `agnos> cat hello.txt` returned seed content byte-exact, vs Attempt 90's `file not found`. Build 571,728 → 572,080 B. |
| C | `cd` + `pwd` + CWD scoping (~190 LOC net) | ✅ landed + IRON-VALIDATED Attempt 91 | New globals (`sh_cwd_inode`/`sh_cwd_path`/`sh_cwd_len`) + helpers (`sh_cwd_init`, `sh_cwd_parent`, `sh_is_dir`) + verbs (`sh_cmd_cd` with `..`/`.`/`/`/absolute/relative + type-check; `sh_cmd_pwd`). `sh_cmd_cat` + `sh_cmd_ls` rewired to consume CWD. Multi-component relative paths (`cd a/b/c`) work transparently via existing path walker. Iron: `agnos> cd lost+found` walked into mke2fs-reserved subdir; `ls` returned `./ ../`; `agnos> cd` returned to root. Build 572,080 → 577,776 B. |
| A | ext4 64BIT support (Phase 5, ~25 LOC + new audit doc + smoke-5) | ✅ landed + IRON-VALIDATED Attempt 91 | Closes row 7b. New `ext2_desc_size` module global + `ext2_sb_desc_size` accessor + `s_desc_size` parse in `ext2_init` (gated on rev≥1 + 64BIT bit) + supported-incompat mask 0x6746 → 0x67C6 + `bg_inode_table_hi` guard in `ext2_get_inode` (refuses with "Phase 6 unlock" if non-zero). Multi-source prior-art doc landed at [`ext4-64bit-prior-art.md`](ext4-64bit-prior-art.md). Iron: user re-carved NVMe `agnos-fs` p3 with default `mkfs.ext4` (drops `-O ^64bit`, sets on-disk 64BIT bit); Phase 5 BGDT-stride-aware mount fired cleanly with `blocksize=4096, inode_size=256, inodes_per_group=8192`. Build 577,776 → 578,432 B. |
| E | cycle-close sweep + Attempt 91 transcript | ✅ landed + IRON-VALIDATED Attempt 91 | `scripts/ext2-smoke.sh` extended 4/4 → 5/5 (new `build_esp_plus_ext4_64bit_partition` builder + smoke 5; all five PASS + 5/5 regression cross-check). state.md / roadmap / iron-log sweep landed. Attempt 91 PASS receipt landed in CHANGELOG § Bite (E, part 2 of 2) + iron-nuc-zen-log § Attempt 91 with four-photo transcript. |

Build trajectory across the cycle: 571,296 B (1.31.6 close) → **578,432 B** (1.31.7 close, +7,136 B / ~260 LOC net). All five bites validated — four code bites by Iron Attempt 91, cycle-close sweep by transcript landing. `scripts/test.sh` 4/4 PASS and `scripts/ext2-smoke.sh` 5/5 PASS + 5/5 regression cross-check.

### 1.31.6 cleanup cycle (CLOSED 2026-05-22 — Iron Attempt 90 PASS)

Disciplined post-greenfield closeout for the 1.31.x storage + filesystem arc. Eight bites per agnos roadmap § 7a (~270 LOC + ~200 audit prose) — all eight landed; two smoke-surfaced bonus fixes also landed; iron Attempt 90 validated bites G + H + both smoke fixes in a single burn.

| # | Bite | Status | Notes |
|---|------|--------|-------|
| A | ext2 input validation sweep (~70 LOC) | ✅ landed | superblock-field bounds (log_block_size cap, blocks_per_group & inodes_per_group nonzero, inodes_per_group ≤ 1M, first_data_block ≤ 1), inode-num upper-bound check in `ext2_get_inode`, PATH_MAX cap in `ext2_path_lookup` |
| B | fatfs BPB validation sweep (~20 LOC) | ✅ landed | sectors_per_cluster ≤ 128 + power-of-two; num_fats nonzero + ≤ 2; root_entry_count ≤ 16384 |
| C | drop ext2 boot-time smoke hook from main.cyr (-50 LOC) | ✅ landed | `ls`/`cat` shell verbs are the real consumers now |
| D | Cyrius `var X[N]` byte-vs-u64 gotcha → feedback memory + CLAUDE.md note | ✅ landed | `feedback_cyrius_var_array_u64_units.md` written; CLAUDE.md note added with pointer to `agnos/kernel/core/ext2.cyr:28-44` |
| E | `agnosticos/docs/development/ext2-iron-burn-audit.md` (~200 prose) | ✅ landed | six hypotheses, success/partial-failure/falsified rubrics, multi-source prior art table, CMOS post-mortem checkpoints reserved (0x56/0x57/0x58) |
| F | state.md / roadmap / iron-log sweep | ✅ landed | this file + agnos roadmap + iron-nuc-zen-log Attempts 89 + 90 |
| **G** | **multi-backend ext2 probe + `blk_read_on` helper (~100 LOC)** | ✅ landed + IRON-VALIDATED Attempt 90 | `blk_registered` bitmask + `blk_read_on(tag, sector, buf)` + `blk_is_registered(tag)` in `block.cyr`; `ext2_backend` global + `ext2_blk_read` helper + `ext2_probe_backend` (with three-check sanity gate) + `ext2_init` rewritten as priority-order probe loop in `ext2.cyr`. Iron: `ext2: probe matched backend=2 partition_lba=3898638336` |
| H | partition-aware mount via GPT consumption (~50 LOC) | ✅ landed + IRON-VALIDATED Attempt 90 | `ext2_try_partition_mount(tag)` iterates GPT partition table for Linux-FS-GUID entries when whole-disk probe misses on `blk_active`. Iron: matched on `partition_lba=3898638336` = NVMe p3 |

**Smoke-surfaced bonus fixes (not in original bite plan; both iron-validated):**

- `blk_mark_registered(tag)` helper in `block.cyr`, called from `ahci.cyr` secondary path + `msc.cyr` tertiary paths. Without it, secondary/tertiary backends were invisible to the multi-backend probe in NVMe-primary topologies (exactly archaemenid's layout).
- `GPT_TYPE_LINUX_FS_LO` byte typo at `gpt.cyr:214`: `0x477284830FC663AF` → `0x477284830FC63DAF` (one byte: `0x63` → `0x3D`). Cosmetic bug masking a functional one — Linux-FS partitions printed as `(unknown type)` AND blocked bite H's GUID gate. Iron: p3 now prints as `[2] Linux FS agnos-fs LBA 3898638336-3907026943 (4096 MiB)` (vs `(unknown type) agnos-fs` at Attempt 89).

**Iron Attempt 90 receipt (2026-05-22 → PASS):** `ext2: mounted (blocksize=4096, inode_size=256, inodes_per_group=8192)` on the NVMe `agnos-fs` p3; `agnos> ls /` returned `./ ../ lost+found/ hello.txt` byte-exact from real Linux ext4 dirent table walked through the FreeBSD-shape extent leaf walker. Storage trio (NVMe/AHCI/USB-MS) byte-matched Attempt 89 — +2.3 KB cleanup-cycle delta regressed nothing. Build: 568,960 B (1.31.5) → **571,296 B** (1.31.6 close). Full transcript + three photos: [`iron-nuc-zen-log.md` § Attempt 90](iron-nuc-zen-log.md).

**Known carry-forward (next cycle assignments):**

- ~~Bare-name `cat hello.txt` falls through to initrd lookup~~ + ~~`ls -la` returning `ls: not found`~~ — **now scoped in 1.31.7 cycle as bites D + B + C (see § *1.31.7 cycle* above).**
- Per-backend GPT parsing — `gpt.cyr` only parses against `blk_active`. AHCI/SATA `agnos-fs` partition was prepared as Attempt 90 surface A but unreachable; defer to next storage-cycle reopening or to a real consumer surfacing demand.

### Next storage targets after NVMe + AHCI iron debuts

Cycle theme stays **storage** through 1.31.x. archaemenid has **two** physical block surfaces — the M.2 NVMe Crucial P3 2 TB (iron-validated as `nvme0` at Attempt 80) AND a 2.5" SATA WD Blue SA510 2 TB (iron-validated at Attempt 81 — actual capacity is 2 TB, not 1.8 TB as the hardware-catalog earlier recorded). With NVMe Phase 1-5 + AHCI/SATA Phase 1-4 + GPT Phase 1-3 + both iron debuts closed under 1.31.x, the next moves down the device list — ranked by archaemenid iron-validatability + LOC cost:

| # | Target | Iron-validatable on archaemenid? | LOC estimate | Notes |
|---|--------|-----------------------------------|--------------|-------|
| ~~1~~ | ~~**GPT parser**~~ | ✅ DONE 1.31.1 (Phase 1-3 landed; NVMe iron-validated arr-CRC-OK at Attempt 81) | ~870 | `kernel/core/gpt.cyr` shipped; table-less CRC32 + backup-header recovery + 7-GUID type classifier + `parts` shell cmd. |
| ~~2~~ | ~~**AHCI/SATA driver**~~ | ✅ DONE 1.31.1 (Phase 1-4 landed; WD Blue SA510 iron-validated at Attempt 81 — PASS-WITH-CAVEAT) | ~1,100 | `kernel/core/ahci.cyr` shipped; three carry-forward patches landed in 1.31.2 `[Unreleased]` (see § AHCI iron carry-forward). |
| ~~3a~~ | ~~**USB Mass Storage (BBB + SCSI)**~~ | ✅ Phase 1-4 + 2.5 + 2.6 + 2.7 in agnos 1.31.2 (cut as-is); **Phase 2.8 eight-bug repair stack in agnos 1.31.3 (CUT 2026-05-21)**; iron Attempts 83/84/85/86 PARTIAL/FALSIFIED; **Attempt 87 PASS** (full INQUIRY/TUR/RC10 chain on real Silicon Motion silicon — `vendor='General' product='USB Flash Disk' rev='1100'`, last_lba=252051455 blk=512B → 123072 MiB) | ~990 | Phase 1: class-interface discovery + bulk-EP enumeration + GET_MAX_LUN. Phase 2: `xhci_input_ctx_add_bulk_pair` + Configure Endpoint + Normal-TRB bulk transfer + full CBW/CSW BBB transport + TEST UNIT READY. Phase 3: INQUIRY (vendor/product/rev/PDT decode with ASCII right-trim) + READ CAPACITY(10) (last_lba + lba_bytes). Phase 4: READ(10) / WRITE(10) + `msc_blk_read / msc_blk_write / msc_blk_read_sectors` block-layer wrappers + `blk_register_usb_ms` + tertiary registration policy (NVMe primary > AHCI secondary > USB MS tertiary > VIRTIO fallback) + dispatch arms for BLK_USB_MS=4 + `msc_read_demo` (unconditional LBA 0 readback) + `msc_write_demo` (gated `MSC_RW_DEMO=1`, LBA-100 sentinel write+readback). QEMU validation **all four phases first-try clean** against `-device qemu-xhci -device usb-storage,bus=xhci.0`: TUR → Pass, INQUIRY → `QEMU/QEMU HARDDISK/2.5+/block`, READ CAPACITY → 16384 LBAs × 512B = 8 MiB, READ(10) LBA0 = zeros (blank), WRITE(10) → LBA100 round-trip PASS. Build trajectory `474,600 → 478,440 → 484,992 → 488,984 → 493,688 B` (+19,088 total, +4.0%). Pre-burn audit: [`usb-ms-iron-burn-audit.md`](usb-ms-iron-burn-audit.md). **Iron debut PARTIAL at Attempt 83 (2026-05-21)**: real-vendor USB 2.0 stick (VID=`0x0936` PID=`0x13E8`) at xhci port 3 enumerated through MSC Phase 1 + Configure Endpoint; TUR returned NOT_READY / CSW status != 0 (spec-anticipated cold-insertion behavior — audit § 3 hyp 2 matched); existing TUR-pass gate at `msc.cyr:317-381` stopped Phase 3 INQUIRY / RC10 / tertiary registration from running. Carry-forward triage in § USB MS iron carry-forward below — Phase 2.5 TUR-hardening + Phase 3 hoist out of TUR gate. |
| 3b | **Optical via USB MS (SCSI MMC profile)** — *bundles with 3a* | Yes (HP external USB Blu-ray drive on archaemenid) | ~200 additional | 1.31.2 cycle scope. Promoted from previously-punted "1.32.x+ ATAPI/AHCI" slot. SCSI INQUIRY peripheral-device-type=0x05 + 2048-B sector handling through `blk_lba_bytes` parameter (currently always 512) + a couple MMC opcodes (TEST UNIT READY, READ CAPACITY(10)). **First non-512-B-sector device on AGNOS** — forces GPT/fatfs consumers to honor `lba_bytes` instead of hardcoding `* 512`. Iron canary: Pitch Black BD-25 disc. |
| 4 | **RAM-disk backend** | No (RAM-only — no iron variable) | ~150 | **1.31.4 cycle scope** (was 1.31.3 — USB-MS Phase 2.8 consumed that slot at Attempt 87 PASS). Pure-RAM `/dev/ram0`-equivalent over `pmm_alloc` + tag-dispatch. Useful for tests + initrd-style workflows. |
| 5 | **VirtIO-blk modern (1.x)** | QEMU only (VirtIO doesn't exist on bare metal) | ~600 delta | **1.31.4 cycle scope** (was 1.31.3 — displaced one slot with RAM-disk). Upgrade existing transitional 0.9.5 `virtio_blk.cyr` to MMIO + feature negotiation. Keeps QEMU first-class on modern machine types. |
| 6 | **ext2 read-only** | Yes (filesystem layer; consumes GPT + block) | ~1000+ | **1.31.5 cycle scope** (displaced twice — was 1.31.3, then 1.31.4, now 1.31.5; the USB-MS Phase 2.8 closeout consumed the 1.31.3 slot and RAM-disk + VirtIO took the 1.31.4 slot). Filesystem class, not device. FAT16 read-only is the floor; ext2 buys real Linux disk semantics (inodes, indirect blocks). |

**Order through 1.31.x**: ~~GPT~~ ✅ → ~~AHCI/SATA~~ ✅ → ~~**USB MS + Optical via USB MS**~~ ✅ (USB MS closed at agnos 1.31.3 / Attempt 87 PASS; USB optical pending) → ~~**RAM-disk + VirtIO-blk modern**~~ ✅ (1.31.4 / Attempt 88 PASS as no-regression) → ~~**ext2 read-only**~~ ✅ (1.31.5 Phase 1-4 / Attempt 89 PASS as no-regression; ext2-mount-on-iron gated on 1.31.6 bite G) → ~~**cleanup cycle**~~ ✅ **(1.31.6 CLOSED 2026-05-22 / Attempt 90 PASS — multi-backend probe + partition-aware mount + ext4 extent walker IRON-VALIDATED on NVMe `agnos-fs` p3)** → ~~**filesystem follow-ups + shell UX**~~ ✅ **(1.31.7 CLOSED 2026-05-22 / Attempt 91 PASS — ext4 64BIT Phase 5 BGDT-stride code + `cd`/`pwd`/CWD scoping + bare-name `cat` + `ls -la` flag dispatch IRON-VALIDATED on re-carved 64BIT-enabled `agnos-fs` p3).** **STORAGE + FS CYCLE CLOSED.**

**Iron-validation coverage at each cut**: 1.31.0 → NVMe iron debut (Crucial P3 / Attempt 80). 1.31.1 → NVMe + SATA iron debut (WD Blue SA510 / Attempt 81 PASS-WITH-CAVEAT). 1.31.2 → AHCI carry-forward triplet iron-PASS (Attempt 82) + USB MS iron progression (Attempts 83/84 PARTIAL → 85/86 FALSIFIED). **1.31.3 → USB MS iron debut PASS (Phase 2.8 eight-bug stack / Attempt 87 full INQUIRY+TUR+RC10 on Silicon Motion silicon).** 1.31.4 → RAM-disk + VirtIO (QEMU only) + iron no-regression burn (Attempt 88). **1.31.5 → ext2/4 Phase 1-4 (QEMU 5/5 green) + iron no-regression burn (Attempt 89; new NVMe `agnos-fs` carve enumerated through GPT Phase 3; ext2 mount silently misses as predicted, gated on 1.31.6 bite G).** **1.31.6 → ext4 victory lap iron-PASS (Attempt 90: `ext2: probe matched backend=2 partition_lba=3898638336` + `ext2: mounted (blocksize=4096, inode_size=256, inodes_per_group=8192)` + `agnos> ls /` returned `./ ../ lost+found/ hello.txt` byte-exact from real Linux ext4 dirent table on iron NAND; closes 1.31.x storage arc).** **1.31.7 → ext4 64BIT + shell-UX victory lap iron-PASS (Attempt 91: re-carved `agnos-fs` p3 with default `mkfs.ext4` 64BIT-enabled mounted cleanly through Phase 5 BGDT-stride code; `agnos> cd lost+found` traversed; `agnos> cat hello.txt` returned `agnos 1.31.7 iron Attempt 91   ext4 64BIT validated   2026-05-22` byte-exact; storage trio byte-matched Attempt 90).**

#### AHCI iron carry-forward — ✅ all three landed in 1.31.2 `[Unreleased]` 2026-05-20

| Item | Resolution | Mechanism |
|---|---|---|
| Post-RW IDENTIFY timeout in `ahci_register_block_dev` (`PxCI stuck`) | ✅ Fixed — root cause was missing pre-issue quiescence gate (not PxIS as initially hypothesized; both issuers already W1C-cleared PxIS before issuing) | New `ahci_port_wait_idle(port)` helper polls PxTFD.STS.BSY=0 + DRQ=0, PxCI=0, PxSACT=0 before issuing. Called at the top of both `ahci_identify_device` and `ahci_issue_rw`. Matches Linux's `ata_qc_issue` / `ahci_qc_issue` pattern (drivers/ata/libahci.c). |
| ATA-string trailing-space drag | ✅ Fixed | `ahci_print_id_string` rewritten to scan printed-char sequence back from the end to the last non-0x20 byte; honors the byte-swap (printed-char-index `k` maps to field-byte `k XOR 1`). Matches Linux's `ata_id_c_string`. |
| `AHCI_RW_DEMO` compile gate | ✅ Fixed | `ahci_rw_demo` split into always-on `ahci_read_demo` (LBA 0 readback, no writes) + `#ifdef AHCI_RW_DEMO`-gated `ahci_write_demo` (LBA-5 sentinel write + read-back). Plumbing in `scripts/build.sh` (`AHCI_RW_DEMO=1 ./scripts/build.sh`) + documented in `docs/development/build.md` alongside `KTEST` / `XHCI_VERBOSE`. |

**Validated on iron at Attempt 82 2026-05-20** — first iron burn of agnos 1.31.2 (built `474,600 B` on cycc 6.0.1, agnos's first burn on the v6.0.x toolchain post-pin-graduation) cleared all three patches in a single shot. Post-RW IDENTIFY no longer hangs (`ahci: registered as secondary block_dev (port 0, 3907029168 LBAs x 512B; NVMe primary)` follows the second IDENTIFY's model/serial/fw + LBA48 print); ATA-string trailing-space drag absent on iron (model prints flush against closing quote); `AHCI_RW_DEMO`-gated `ahci_write_demo` compiled out (LBA-5 sentinel write absent from boot output). The `ahci-iron-burn-audit.md` § 7 full-success rubric is now cleared — no new diagnostic letters, no new hypotheses, no carry-forward residue. Detail in [`iron-nuc-zen-log.md` § Attempt 82](iron-nuc-zen-log.md).

#### USB MS iron carry-forward — Phases 2.5 ✅ / 2.6 FALSIFIED@85 / 2.7 LANDED@86 (Reset Recovery worked, post-recovery TUR still failed) / **Phase 2.8 eight-bug repair stack in agnos 1.31.3 → ✅ Attempt 87 PASS 2026-05-21 (1.31.3 cut)**

Third storage-class iron debut (after NVMe at Attempt 80 + SATA at Attempt 81). Three iron burns 2026-05-21 (84/85/86) against a Silicon Motion / SMI commodity stick (`VID=0x090C PID=0x1000`, USB 2.0 / HS). Each burn advanced the failure deeper into the stack — no letter laddering per [`feedback_stop_letter_laddering`](../../../../.claude/projects/-home-macro-Repos-agnosticos/memory/feedback_stop_letter_laddering.md).

**Attempt 84 (Phase 2.5)**: Device-side Reset Recovery (`msc_reset_recovery` per BBB §6.7.3) + 3-retry TUR loop + INQUIRY hoist + REQUEST SENSE all fired as designed on iron. Per-step diagnostics (`data phase timeout` / `CSW transfer timeout` / `CBW transfer timeout`) replaced Attempt 83's misleading "(CSW status != 0)". But the device transport stayed wedged across all three Reset Recovery cycles, and a CSW tag mismatch immediately before the first recovery proved the device's bulk-IN buffer was not drained. → Phase 2.6 controller-side commands justified.

**Attempt 85 (Phase 2.6) → FALSIFIED**: Three new xHCI commands shipped (`xhci_cmd_reset_endpoint` TRB 14 + `xhci_cmd_stop_endpoint` TRB 15 + `xhci_cmd_set_tr_dequeue` TRB 16) + event-ring drain (`xhci_drain_transfer_events`) + rewritten `msc_reset_recovery`. On iron: `msc: Reset Endpoint(bulk-IN) failed` × 2 aborted recovery before Set TR Dequeue Pointer could fire. **Root cause**: Reset Endpoint is only legal from Halted state per xHCI 1.2 §4.6.8, but the transfer-event-timeout wedge left the EP in Running, then Stop Endpoint moved it to Stopped — Reset Endpoint on Stopped returns Context State Error (CC=19), which `xhci_cmd_reset_endpoint` was treating as a failure → recovery aborted.

**Phase 2.7 (post-Attempt-85 carry-forward, agnos 1.31.2 `[Unreleased]`, Attempt 86 burn pending)**: per [`feedback_redesign_dont_reinvent`](../../../../.claude/projects/-home-macro-Repos-agnosticos/memory/feedback_redesign_dont_reinvent.md) refreshed 2026-05-21 with hard "multi-source" rule (after user feedback "LINUX ISN'T THE ONLY RESOURCE OF PRIOR ART"), four-source convergent audit landed in [`msc-reset-recovery-prior-art.md` § 9](msc-reset-recovery-prior-art.md): FreeBSD `umass.c` + `xhci.c` + OpenBSD `umass.c` + EDK2 `UsbMassBot.c` + Linux confirmatory. Convergent findings: (1) device-side reset runs FIRST, before any host-controller resync; (2) Reset Endpoint must be gated on EP State == Halted (FreeBSD reads state explicitly via `xhci_get_endpoint_state`); (3) **100ms post-BOT-Reset device stall** is required (EDK2 explicit `gBS->Stall(USB_BOT_RESET_DEVICE_STALL)`; FreeBSD implicit 50ms `.interval`; Linux `msleep(100)`) — AGNOS Phase 2.5/2.6 had ZERO delay, the most likely contributor to Attempt 84's "Reset Recovery OK but transport stays wedged".

| Phase | Item | Status | Mechanism |
|---|---|---|---|
| 2.5 | Bulk-IN transport wedged: Step 4 CSW receive timeout | ✅ Iron-validated Attempt 84 (executes cleanly; insufficient alone) | `msc_reset_recovery` per BBB §6.7.3 + host-side ring rewind. |
| 2.5 | TUR retry loop | ✅ Iron-validated | 3-retry loop, `msc_reset_recovery` between failed attempts. |
| 2.5 | INQUIRY hoist out of TUR-pass gate | ✅ Iron-validated | `msc_inquiry` runs unconditionally after Configure Endpoint per SPC-4 §6.6. RC10 stays inside TUR-pass branch per SBC-3 §5.10. |
| 2.5 | REQUEST SENSE decoder | ✅ Landed; transport never recovered enough on iron for SENSE to issue | `msc_request_sense` helper. |
| 2.6 | Controller-side EP recovery (TRB 14/15/16) | ✅ Landed agnos 1.31.2; FALSIFIED Attempt 85 (Reset Endpoint dispatch wrong; corrected in 2.7) | Three new helpers in `xhci_cmd.cyr`. |
| 2.6 | Event-ring drain of stale Transfer Events | ✅ Landed agnos 1.31.2 | `xhci_drain_transfer_events(slot_id)`. |
| **2.7** | **Reset Endpoint CSE tolerance** | ✅ Landed agnos 1.31.2 `[Unreleased]`; pending Attempt 86 | `xhci_cmd_reset_endpoint` returns 1 on `XHCI_CC_CONTEXT_STATE_ERROR`. Defensive backstop. |
| **2.7** | **EP-state-aware Reset Endpoint dispatch** | ✅ Landed agnos 1.31.2 `[Unreleased]`; pending Attempt 86 | New `xhci_ep_state(slot_id, dci)` reads Output EP Context dword 0 bits 0-2 per xHCI 1.2 §6.2.3 (`XhciEpState` enum). `msc.cyr` step 7 gates Reset Endpoint on `XHCI_EP_STATE_HALTED`. Mirrors FreeBSD `xhci_configure_reset_endpoint`. |
| **2.7** | **100ms post-BOT-Reset device stall** | ✅ Landed agnos 1.31.2 `[Unreleased]`; pending Attempt 86 | 50M-iter busy-wait after `xhci_control_no_data(0x21/0xFF)`. Matches EDK2 explicit + FreeBSD implicit. **Highest-confidence fix in the 2.7 stack** for Attempt 84's wedge pattern. |
| **2.7** | **Reset Recovery step reorder: device-side first** | ✅ Iron-validated Attempt 86 (Reset Recovery executed correctly: `Reset Recovery OK` × 3 in transcript, no `Reset Endpoint failed`); orthogonal bug stack surfaced | Full `msc_reset_recovery` rewrite. New order: BOT Reset → 100ms stall → CLEAR_FEATURE×2 → drain events → Stop Endpoint×2 → Reset Endpoint×2 (Halted-gated) → ring rewind → Set TR Dequeue×2. Matches convergent reference ordering. |
| **2.8** | **Bulk timeout extension** | ✅ Landed agnos 1.31.3 | New `XHCI_BULK_TIMEOUT_SPINS = 200_000_000` (~1s wall) enum, bulk-specific. **Root cause of Attempt 86**: cmd-ring 10M (~25-50ms) was abandoning the INQUIRY data phase mid-flight against real Silicon Motion silicon. Linux `USB_CTRL_GET_TIMEOUT=5000ms`; FreeBSD comparable. |
| **2.8** | **Strict TRB-pointer matching** | ✅ Landed agnos 1.31.3 | New `xhci_wait_transfer_for_trb(slot_id, expected_trb_phys, expected_len)` + `xhci_last_xfer_bytes` global. Skips stale completion events for prior wedged TRBs (Attempt 86's "CSW tag mismatch" on TUR #0 was a late INQUIRY CSW attributed to TUR). Mirrors Linux `handle_tx_event`. |
| **2.8** | **SHORT_PACKET residue check** | ✅ Landed agnos 1.31.3 | `xhci_wait_transfer_for_trb` reads event dw2 bits 23:0 (residue). If residue ≥ expected_length → 0 bytes transferred → failure. **Direct cause of Attempt 86's repeating "CSW signature mismatch"**: device's ZLP-then-real-CSW pattern after Reset Recovery left csw_phys[0..3]=0. |
| **2.8** | **`msc_scsi_exec` unified retry+recover wrapper** | ✅ Landed agnos 1.31.3 | New wrapper around `msc_bbb_exec`; runs Reset Recovery between failed attempts AND at entry if transport_failed sticky already set. INQUIRY/TUR/RC10/RS/READ/WRITE all migrated. Subsumes the hand-rolled TUR retry loop in `msc_probe_slot`. Linux `usb_stor_invoke_transport` pattern. |
| **2.8** | **Drain reposition: AFTER Stop Endpoint** | ✅ Landed agnos 1.31.3 | `msc_reset_recovery` step 7 (was step 5). Stop Endpoint × 2 posts Transfer Events for pinned in-flight TRBs (xHCI 1.2 §4.6.9.1); pre-Stop drain (Phase 2.7) missed them. Linux drains in `handle_stopped_endpoint`. |
| **2.8** | **`xhci_cmd_set_tr_dequeue` full 64-bit phys** | ✅ Landed agnos 1.31.3 | `param_hi = (deq_ptr_phys >> 32) & 0xFFFFFFFF` (was hardcoded 0). Defensive — archaemenid PMM stays <4GB but malformed for future high-memory placement. |

**Attempt 87 outcome (2026-05-21) — ✅ FULL PASS against rubric (a)**: `xhci: bulk transfer event timeout` absent from the MSC chain; INQUIRY succeeds first try (`vendor='General' product='USB Flash Disk' rev='1100' type=block`); TUR Pass first try; RC10 prints `last_lba=252051455 blk=512B -> 123072 MiB` (252,051,456 LBAs × 512 B ≈ 120 GiB Silicon Motion stick); `1 mass-storage device(s) detected`. NVMe + AHCI bring-up continues clean below. Transcript photo: [`iron-nuc-zen-photos/attempt-87-usb-ms-phase-2-8-inquiry-tur-rc10-success.jpg`](iron-nuc-zen-photos/attempt-87-usb-ms-phase-2-8-inquiry-tur-rc10-success.jpg).

**MVP gate posture**: unaffected — `msc_probe_slot` already returns 1 even on transport failure, so MVP boot-to-shell stays green regardless of MSC outcome. USB MS arc was opportunistic; closed beta depends on kybernet + agnoshi + kernel-on-iron, not on a specific block backend.

**Storage + FS arc status — CLOSED 2026-05-22; 1.32.0 networking arc CLOSED 2026-05-22 (feature-complete); 1.32.1 networking-arc-continued cycle OPEN 2026-05-22:** ~~1.31.3~~ ✅ (USB MS Phase 2.8 / Attempt 87 PASS) → ~~1.31.4~~ ✅ (RAM-disk + VirtIO modern / Attempt 88 PASS no-regression) → ~~1.31.5~~ ✅ (ext2/4 Phase 1-4 / QEMU 5/5 green + Attempt 89 PASS no-regression) → ~~1.31.6~~ ✅ (cleanup + multi-backend probe + partition-aware mount / Attempt 90 PASS — ext4 mounted from NVMe `agnos-fs` p3 on iron) → ~~1.31.7~~ ✅ (ext4 64BIT Phase 5 + shell UX `cd`/`pwd`/bare-name `cat`/`ls -la` / Attempt 91 PASS — Phase 5 BGDT stride iron-validated on user's re-carved 64BIT-enabled NVMe `agnos-fs` p3) → ~~**1.32.0**~~ ✅ **CLOSED 2026-05-22** (networking arc feature-complete: TCP server primitives bite A + UDP server primitives bite F + DHCP client RFC 2131 bite G + r8169 driver Phases 1-4 bite B + DHCP gate fix `main.cyr:655` + Iron Attempts 92 + 93; **Attempt 93 PARTIAL VERIFIED gate fix on iron**) → **1.32.1 OPEN (networking-arc-continued; lean cycle-open per discipline — VERSION bump 1.32.0 → 1.32.1 + CHANGELOG `[1.32.1]` header + tracking surface; bites A-E PENDING IRON; theme: r8169 driver-level OFFER-timeout debug — H1/H7/H8 audit hypotheses now reachable)**.

**Out of cycle scope (parked):**
- AMD Zen scanout residue (Quiet Boot legibility) — separate cycle per [`project_amd_zen_scanout_residue`](../../../../.claude/projects/-home-macro-Repos-agnosticos/memory/project_amd_zen_scanout_residue.md); HUBP `clear_tiling` port or shadow-buffer eval.
- SMP-AP wakeup on real hardware — carry-forward from earlier roadmap.
- Real-iron NIC (e1000e / I225-V / RTL8125) — different device class, separate arc when networking becomes the cycle theme.

This doc holds **volatile state** — what's currently true across the AGNOS dev surface. CLAUDE.md is preferences/process/procedures; this is the live picture. Per [*Docs Go Stale Before the Commit*](../articles/docs-go-stale-before-the-commit.md): rewrite in place when state changes; don't preserve historical snapshots — git history is authoritative.

**Drift caveat**: always verify against actual `VERSION` + `cyrius.cyml` files before acting on any single item in this doc.

---

## Cyrius cycle — v6.0.0 (open 2026-05-19)

**v6.x = "what the language GAINS."** v5.x was "what the language IS"; the v5.x→v6.x boundary marks the pivot from stabilization to expansion (RISC-V rv64, PIE, closures, Class-B FFI, bare-metal — slip path: v5.8.x → v5.10.x → v5.11.x → **v6.x**, per the boundary memory). First slot cut today, 2026-05-19.

### v6.0.0 cycle-open — two-binary rename ceremony

- `cyrc` → **`cybs`** (Cyrius Bootstrap) — the seed/bootstrap compiler in the chain
- `cc5`  → **`cycc`** (Cyrius Computer Compiler) — the self-hosted production compiler

Bootstrap chain is now `seed (asm) → cybs → cycc`. The "Version lives in `VERSION` + `--version`, never in binary names" Key Principle stays — but the names are *forever*. No `cycc6` at v7.0.0, no `cybs7` at v8.0.0; the cc3 → cc5 (v5.0.0) → cycc (v6.0.0) and cyrc → cybs (v6.0.0) sequence was the LAST name-change penalty paid.

**Rename surface**: ~2,100 occurrences across ~157 files. Categorized sed preserved historical anchor text (v5.x CHANGELOG entries, completed-phases.md, archives, audit date-stamps, vidya retros — those names were what the binaries were called at the time). Current-state code + canonical-current docs renamed. File renames via `git mv` (`bootstrap/cyrc.cyr` → `bootstrap/cybs.cyr`, `build/cc5*` → `build/cycc*`, scripts renamed in parallel).

**Back-compat (v6.0.x window)**: `scripts/install.sh` ships symlinks `cc5 → cycc`, `cyrc → cybs`, `cc5_aarch64 → cycc_aarch64`, `cc5_win → cycc_win` in `~/.cyrius/versions/<v>/bin/`. `cbt/core.cyr` compiler-lookup tries new name first then falls back to old. Both drop at **v6.1.0**.

**Mechanical gates at v6.0.0 cut**: cycc self-host byte-identical at **874,240 B** (+8 B vs cc5 at v5.11.69's 874,232 B, from the longer "cycc" binary-name strings). `check.sh` **76/76**. `cyrius test` **152/152**. 3-step bootstrap (old cc5 v5.11.69 → cycc_a → cycc_b byte-identical). Cross-arch: cycc_aarch64 564,456 B, cycc_win 686,632 B.

### v6.0.x carry-forward (from v5.x closeout)

Five accompanying-refactor items pulled forward — surface on next cyrius-side touch. Detail in `cyrius/docs/development/roadmap.md`. Beyond this: RISC-V rv64 backend, PIE, closures, Class-B FFI, bare-metal Cyrius — all v6.x slots, no firm sequencing yet.

### v5.11.x retrospective (closed 2026-05-19 at 5.11.69)

**70 patches across 11 days** (2026-05-09 → 2026-05-19) — the longest minor in Cyrius history. Three same-day bursts of 24/18/17 on 2026-05-11/12/13 carried the bulk; the .56–.69 tail spread across one patch/day cadence with the heavy engineering at .68 (heap-map reorg) and closeout housekeeping at .69.

- **Stdlib annotation arc** — 1,010 unannotated public fns annotated across the 7-phase breakout (foundational / I/O / strings / collections / big consumers / closeout / compiler internals); Phase 1 landed at v5.11.1, all phases closed in the .1–.55 burst.
- **Consumer-issue closeout** — kavach P1 sandbox wrappers (v5.11.0), daimon/bote P2 wave, low-priority cleanups.
- **ELF section-header fix arc** (.29/.30/.31) — GRUB `grub_elf32_get_shnum` rejection traced to `e_shoff=0` from x86 kernel / aarch64 kernel / cyrld emitters; mirrored 5-section table across all three.
- **gvar-init-order zero-reads fix** (v5.11.64) — module-top `var X = INT_LITERAL` read as 0 before init block ran (kmode==1 init-order: top-level asm → PARSE_PROG → EMIT_GVAR_INITS, with kernel's main body in PARSE_PROG never returning). Root cause of the 10-letter Phase-3 cmd-path silent-absorb arc on iron (FF→QQ+QQ2, falsified across Attempts 57-63 chasing what looked like silicon). Fixed via Option 1 — image-static init for literal-RHS gvars at file scope, every backend covered. Issue: [`2026-05-18-gvar-init-order-zero-reads.md`](https://github.com/MacCracken/cyrius/blob/main/docs/development/issues/2026-05-18-gvar-init-order-zero-reads.md).
- **Path A→Path C transition** (.43–.55) — diagnostic + sovereign UEFI-application emit support for the gnoboot lane after GRUB MB2-EFI W^X blocker (per `project_grub_mb2_efi_wx_blocker` memory).
- **Heap-map full reorganization** (v5.11.68) — the true v5.x closeout engineering work; ~9.06 MB reclaimed, 84 → 99 regions, brk-final 0x4E8C000 → 0x4D9D000. v5.11.69 was doc/scripts/vidya closeout sweep (the originally-conditional fold-applied slot — mabda 3.0 fold was dropped per user direction post-.67, so .69 absorbed pre-6.0 cleanup instead).

**v6.0.0 enabling consumers** (cut during v5.11.x):
- **argonaut 1.7.0 + kybernet 1.2.1** (2026-05-11 eve) — BOOT_MINIMAL agnoshi-as-console; unblocked the closed-beta MVP path without aethersafha.
- **kriya 1.0.0** (2026-05-18) — coreutils-equivalent dispatcher graduated to v1.0+.
- **commandress 1.0.0** (2026-05-18) — segment renderer + config layer stabilized; adapter-based for agnoshi/bash/zsh prompt-hook.

V1.0+ binaries cohort now **13**: agnos, agnoshi, argonaut, bannermanor, commandress, cyim, cyim-lsp, iam, kriya, kybernet, mihi, nous, owl. (iam + mihi added 2026-05-20 morning as the first v6.0.x graduations; bannermanor 0.5.0 → **1.0.0** later same day with CLI surface + CYML font format + default font set frozen as the v1.0 contract.)

### v5.10.x retrospective (closed 2026-05-11 at 5.10.50)

**50 patches in 5 days** (2026-05-06 → 2026-05-11). THREE completed arcs plus a compile-perf miniarc:

- **Typed-simd ABI arc — 11 phases** (closeout v5.10.39). `lib/simd.cyr` rewrite: every math op exists in value-form + pointer-form siblings with parser-side `&IDENT → _ptr` overload routing; f64v2 args in XMM0/XMM1 (SysV) / V0/V1 (aarch64), f64v4 in register pairs; PE-gated via `CYRIUS_HAS_VAL_SIMD_PARAMS`. **This is the substrate for Cyrius-native codec work long-term** — typed SIMD primitives + cross-platform ABI-aware register routing is the floor of any handwritten-SIMD codec port (tarang's current dav1d/openh264/libvpx C-FFI layer is the placeholder until that future arc opens).
- **REAL TYPE SYSTEM arc — 5 phases** (Phase 2 v5.10.24, Phase 3 v5.10.25 overload generalize). Per-fn param-type bitmasks, call-site type checking, cstring / Result / Option / Tagged vocabulary on stdlib.
- **Struct-byval ABI arc — 3 phases** (.45 + .46 + .47). Cross-backend struct-byval return surface.
- **Compile-time-perf miniarc** (.40 + .41) — **2.7× total compile speedup**.

Plus one TLS contract pin (.42), one PE premise debunk (.49 — 15-slot phantom pin closed by empirical re-test), 4 open issues closed (str_split, exec_*, parser cosmetics, kernel-reserved-word), and 9+ in-cycle pin re-scopings driven by premise-check discipline.

api-surface 2,769 → 2,876 (+107 public fns). cc5 (x86) 741,048 B → **804,472 B**. check.sh 66 gates stable. cyrius test count 132 → ~146.

### cycc cut state

cycc self-host **874,240 B** at v6.0.0 (was cc5 874,232 B at v5.11.69 close; +8 B for the longer "cycc" binary-name string in `version_str.cyr`). Self-host fixpoint clean. cc5 grew from **804,472 B at v5.11.0** baseline to **874,232 B at v5.11.69** across the 70-patch cycle (+69,760 B over 9 days). check.sh 66 → 76 gates; cyrius test 132 → 152 across the 5.10.x + 5.11.x cycles.

### Genuinely dangling — carry-forward into v6.x triage

| # | Item | Domain | Notes |
|---|------|--------|-------|
| 1 | `cyrlint` multi-line assert | Tooling | Investigated v5.8.41; couldn't reproduce on 4 synthetic tests. Decide: close as moot, or pin a real reproduction case |
| 2 | `ESTORESTACKPARM` stub | Language | Explicitly **held** — needs unhold-or-resolve decision |
| 3 | Optimization arc O3–O6 audit | Compiler | Partial follow-on shipped (O3a IR / O4a–c regalloc / O5 / O6 codebuf) — needs status sweep against v5.6.x deferral list |
| 4 | Consumer rollup — pre-CYML format tail | Ecosystem | `hoosh` and `shravan` only (down from 11 at v5.10) — format migration + pin bump |
| 5 | Consumer rollup — deep-lag tail | Ecosystem | ark (5.1.10), yantra (5.6.17); hisab/agnova/abaco/nous/bazaar/shakti in v5.7.x; libro/majra exited at v5.10.44 |
| 6 | Consumer rollup — v5.7.48 held cluster (3 repos remaining) | Ecosystem | mabda, cyrius-doom, samvada — phylax + agnosys both exited during v5.10.x/v5.11.x |
| 7 | RISC-V rv64 backend | Compiler/backend | Slipped 7+ times; first-class v6.x candidate. Substrate prereqs (typed-simd ABI / REAL TYPE SYSTEM / struct-byval) all landed v5.10.x |
| 8 | Bare-metal Cyrius target | Compiler/runtime | v6.x slot; substrate prereqs landed v5.10.x + v5.11.x stdlib annotation |
| 9 | PIE / closures / Class-B FFI | Language | Three v6.x feature slots per the v5.x→v6.x boundary; no firm sequencing |

---

## Pin-lag spectrum

Each repo's `cyrius = "X.Y.Z"` pin (verified 2026-05-11 eve from local clones, with leading-edge spot updates). **Most pre-CYML consumers migrated**: agnoshi, bote, t-ron, kavach, itihas, nein all moved from `cyrius.toml` v3.x/v4.x to `cyrius.cyml` 5.10.44+. Only `hoosh` and `shravan` remain on pre-CYML format in the local-verified set. **The v5.11.x leading-edge bedrock is now where consumers will pause** — back-compat symlinks (`cc5 → cycc`, `cyrc → cybs`) keep them building unchanged through the v6.0.x window; natural-next-touch will graduate each consumer to v6.0.x and re-pin then.

```
PRE-CYML format / no pin field (remaining tail):
  hoosh (cyrius.toml, no pin field visible in snapshot)
  shravan (cyrius.toml, no pin field visible in snapshot)

CYML format — DEEP LAG (didn't roll forward; may carry latent stdlib breakage):
  v5.1.x:  ark (5.1.10)                              ← extreme lag, port pre-dates pin convention
  v5.6.x:  yantra (5.6.17)
  v5.7.x:  hisab (5.7.10), agnova (5.7.12),
           abaco (5.7.23), nous (5.7.29),
           bazaar (5.7.30), shakti (5.7.33)
  v5.7.48: mabda (3.0.0-rc.2), cyrius-doom (0.26.2),
           samvada (0.2.2)                           ← held-cluster (was 4; phylax exited)

CYML format — WARM CLUSTERS:
  v5.9.x:  sit (5.9.37), vidya (5.9.43)
  v5.10.x: vyakarana (5.10.5), owl (5.10.10),
           cyim (5.10.10), cyim-lsp (5.10.10),
           cyim-lsp (5.10.20),
           aegis (5.10.34)
           — darshana graduated to v6.0.1 cluster 2026-05-20 (color primitives bump)

CYML format — LIVE 5.10.44 BEDROCK (~14 repos, the boot path minus agnos + agnoshi):
  v5.10.44: agnostik (1.2.2), argonaut (1.7.0),
            bote (2.7.2), daimon (1.2.3),
            kavach (3.2.1), kybernet (1.2.1),
            libro (2.6.3) — exited 5.4.x deep-lag,
            majra (2.4.4) — exited 5.4.x deep-lag,
            nein (1.5.1), phylax (1.1.1) — exited 5.7.48 held cluster,
            t-ron (2.1.4)
            — agnoshi graduated to v6.0.1 cluster 2026-05-20 (1.3.2 → 1.3.3)

CYML format — 5.11.x cluster (no leading-edge holdouts after agnos graduation):
  v5.11.59: agnosticos/scripts (genesis 0.1.0)
            — boot pipeline; pin sweep deferred to next boot-side touch
            — only 5.11.x leading-edge member remaining after agnos
              graduated to 6.0.1 mid-1.31.x cycle
  v5.11.4:  agnosys (1.2.6), sigil (3.1.1), sankoch (2.2.5),
            sandhi (1.3.4), niyama (1.0.2), patra (1.9.4),
            sakshi (2.2.4), vani (0.9.3), yukti (2.2.3)
  v5.11.8:  ai-hwaccel (2.2.2)

CYML format — LEADING-EDGE 6.0.x cluster (post-v5.11.x graduations):
  v6.0.1:  agnos (1.31.7), agnoshi (1.3.3), mihi (1.0.0), iam (1.0.0),
           chakshu (0.6.0), bannermanor (1.0.0), darshana (0.3.5),
           hapi (0.5.0)
           — eight repos on the v6.0.x lane. agnos graduated mid-1.31.x
             cycle for binary fixes in cycc 6.0.1 (was 5.11.64, the
             gvar-init-order anchor). The 2026-05-20 terminal-
             aesthetics burst brought six: mihi + iam cut as NEW 1.0.0
             repos straight on v6.0.1; chakshu jumped 0.3.0 → 0.6.0 +
             5.10.20 → 6.0.1; bannermanor (`bnrmr` figlet-equivalent) +
             hapi (stow-equivalent) cut at 0.5.0 straight on v6.0.1;
             darshana graduated from
             v5.10.20 → v6.0.1 with the 0.3.0 → 0.3.5 color-primitives
             bump that bannermanor's banner colors needed.

CYRIUS TOOLCHAIN itself: 6.0.1 (v6.0.0 cycle opened 2026-05-19, same-day .1 patch for UEFI fncall ud2 emit regression; v5.11.x closed at 5.11.69)

NOT VERIFIED LOCALLY (remote-only, presumed pre-CYML or scaffolded):
  avatara, hadara, itihas, takumi, aethersafha, aethersafta, mela,
  seema, samay, kiran, joshua, salai, murti, tanur, encom-hits,
  cyrius-{bb,brynns-tale,stellar-swarm,sunset-drive,super-plumber-twins,
  grapevine,chellys-beach-adventure,nba-jam}
```

**Bands of attention (2026-05-20 PM — post-MVP-gate, post-v6.0.0 cycle-open, agnos + agnoshi graduated mid-1.31.x storage cycle):**
- **6.0.1 leading-edge cluster** (2026-05-20 → -22, **9 repos**): **agnos (1.32.1)** + **agnoshi (1.3.3)** + mihi (1.0.0), iam (1.0.0), chakshu (0.6.0), bannermanor (1.0.0), darshana (0.3.5), hapi (0.5.0), **kii (0.1.0)** (added 2026-05-22 — Hawaiian Polynesian micro-cluster joins via image→ANSI/ASCII converter). The morning brought sys-info substrate (mihi/iam/chakshu) and afternoon brought terminal-aesthetics (bannermanor / hapi / darshana). The 2026-05-20 evening MVP-path graduation pair (agnos + agnoshi) onto cycc 6.0.1 was the load-bearing change; agnos has since rolled through the storage arc and ext2/4 cycle on the same pin: 1.31.2 → 1.31.3 (USB MS Phase 2.8) → 1.31.4 (RAM-disk + VirtIO modern) → 1.31.5 (ext2/4 Phase 1-4) → 1.31.6 (cleanup cycle close) → **1.31.7** (FS follow-ups + shell UX close). The darshana 0.3.0 → 0.3.5 bump was demand-driven (bannermanor needed color escape sequences) — same shared-lib-evolves-to-second-consumer pattern as mihi's cyim → chakshu extraction.
- **5.11.x cluster (no leading-edge holdouts)**: agnosticos/scripts (5.11.59 — boot pipeline; sweep deferred; genesis-repo VERSION flipped CalVer → SemVer at 0.1.0 cycle-open 2026-05-21). agnos vacated 5.11.64 mid-1.31.x cycle. Back-compat symlinks (`cc5 → cycc`, `cyrc → cybs`) keep this cluster building unchanged through the v6.0.x window.
- **5.11.x post-burst cluster** (~10 repos at .4/.8) — sandhi, niyama, patra, sakshi, vani, yukti, agnosys, sigil, sankoch, ai-hwaccel. Ahead of the bedrock but trailing the leading-edge.
- **5.10.44 live bedrock** (~14 repos: kybernet, argonaut, kavach, daimon, bote, t-ron, libro, etc.) — agnoshi exited 2026-05-20, narrowing the MVP-path holdouts to **kybernet + argonaut** (these still pin 5.10.44 and still boot AGNOS on iron). Rest of the bedrock graduates to v6.0.x on natural-next-touch.
- **Deep-lag tail** shrank but didn't vanish: ark (5.1.10) extreme, hisab/agnova/abaco/nous/bazaar/shakti in v5.7.x cluster, yantra (5.6.17). The 5.4.x cluster (libro, majra) FULLY EXITED at 5.10.44.
- **Held cluster at 5.7.48** now **3 repos** (mabda, cyrius-doom, samvada) — phylax exited during v5.10.x. mabda is at 3.0.0-rc.2 (soak before GA fold to Cyrius stdlib); cyrius-doom is at 0.26.2 (gated on Cyrius optimization-arc closeout retroactive verification).
- **Pre-CYML format tail**: only `hoosh` and `shravan` remain in the local-verified set. The previous 11-repo tail collapsed in the v5.10–v5.11 window.

### New repos / milestone bumps since last refresh

| Repo | Version | Pin | Notes |
|------|---------|-----|-------|
| **aegis** | **1.0.0** | 5.10.34 | **Hit v1.0** (was 0.8.2 in last refresh). Real system-security daemon now shipping. Skipped 0.9.x — straight implementation closeout to 1.0.0. |
| **agnos** | **1.32.1** | **6.0.1** | **1.31.x storage + FS arc CLOSED 2026-05-22 / 1.32.0 networking arc CLOSED 2026-05-22 / 1.32.1 networking-arc-continued cycle OPEN 2026-05-22.** Through-line: 1.31.2 → 1.31.3 (USB MS Phase 2.8 eight-bug repair stack / Attempt 87 iron PASS) → 1.31.4 (RAM-disk + VirtIO 1.x modern / Attempt 88 iron PASS no-regression) → 1.31.5 (ext2/4 Phase 1-4 / Attempt 89 iron PASS no-regression) → 1.31.6 cleanup cycle (8 bites + 2 smoke-surfaced fixes + Iron Attempt 90 ext4 victory lap PASS) → 1.31.7 filesystem follow-ups + shell UX CLOSED (ext4 64BIT Phase 5 + `cd`/`pwd`/CWD scoping + bare-name `cat` + `ls -la` flag dispatch + Iron Attempt 91 PASS) → **1.32.0 networking arc CLOSED 2026-05-22**: TCP server primitives + UDP server primitives + DHCP client RFC 2131 + r8169 driver Phases 1-4 + iron Attempts 92 + 93 on archaemenid + DHCP gate predicate fix at `main.cyr:655`. **Iron evidence**: Attempt 92 PARTIAL surfaced gate predicate bug → same-day fix `if (vnet_active != 0 || nic_ready() != 0)` → **Attempt 93 PARTIAL VERIFIED the fix on iron** (`dhcp: DISCOVER` egresses through r8169 path for the first time; new failure mode `dhcp: OFFER timeout`). → **1.32.1 OPEN 2026-05-22** networking-arc-continued cycle-open (lean: VERSION bump 1.32.0 → 1.32.1 via canonical `scripts/version-bump.sh` + CHANGELOG `[1.32.1]` header + tracking surface, no code touches yet). Cycle theme: **r8169 driver-level OFFER-timeout debug** (H1 PHY-not-configured / H7 TX OWN stuck / H8 RX OWN stuck — the now-reachable audit hypothesis surface from Attempt 93's pre-burn rubric). Opening move: §5b extension to [`r8169-iron-burn-audit.md`](r8169-iron-burn-audit.md) per [[feedback_iron_burns_block_other_work]] + [[feedback_redesign_dont_reinvent]]. Carry-forward still in flight: i225-V driver bite C (Intel iron pending, post-archaemenid-migration); BBS + MUD userland (out-of-cycle, separate repos). Strategic destination: AGNOS installable-state on archaemenid → user's planned **dual-boot migration**: 1TB ex-USB drive becomes Beelink internal NVMe + hosts AGNOS-primary; WD Blue SA510 SATA stays in chassis as Linux daily-driver root; 2TB Crucial P3 NVMe migrates to i9. See § *archaemenid migration* above and [[project_archaemenid_install_plan]]. Multi-cycle path: 1.32.x networking → 1.33.x ext4 WRITE → installable-state cycle slot TBD. See § *1.32.0 cycle* (CLOSED) + § *1.32.1 cycle* (OPEN). Pin stable on cycc 6.0.1. Build trajectory: 578,432 B (1.31.7 close) → 601,392 B (1.32.0 close) → **601,392 B (1.32.1 open, zero structural delta — only `version_str` literals shifted across the bump)**. |
| **agnoshi** | **1.3.3** | **6.0.1** | AI shell + closed-beta MVP boot-path console. Was 1.3.2 / pin 5.10.44 (live-bedrock cluster) in last refresh; graduated straight to 6.0.1, skipping the 5.11.x leading-edge tier. After this bump + agnos's 6.0.1 graduation, the MVP-path 5.10.44 holdouts narrow to **kybernet + argonaut** — the two repos still on 5.10.44 that need to graduate before the entire closed-beta MVP path runs on v6.0.x. |
| **chakshu** | **0.6.0** | **6.0.1** | AI-augmented system monitor (`shu` binary). Was 0.3.0 / pin 5.10.20 in last refresh — jumped three patch versions (0.3.0 → 0.6.0) AND graduated pin straight to 6.0.1 (one of the first three v6.0.x graduations). Started consuming mihi for its sys-info probe surface, which let the maturity arc compress. |
| **cyim-lsp** | 1.5.0 | 5.10.20 | LSP server companion to cyim. Pin moved 5.10.10 → 5.10.20. |
| **bannermanor** | **1.0.0** | **6.0.1** | **NEW + graduated to v1.0** (binary `bnrmr`). figlet-equivalent ASCII-art banner generator for login MOTDs / script intros / splash text. English wordplay (commandress/bannermanor naming lane); `bnrmr` vowel-dropped per the `commandress`→`cmdrs` compression pattern. Cut at 0.5.0 straight on v6.0.1 morning; jumped to **1.0.0** later 2026-05-20 — CLI flag surface, CYML font format (schema=1), and default in-tree font set (block / slim / big) all frozen as the v1.0 contract. Drove the darshana 0.3.0 → 0.3.5 color-primitives bump (banner colors). |
| **darshana** | **0.3.5** | **6.0.1** | TTY/raw-mode primitives library (दर्शन — viewing/showing). Extracted from cyim's `src/tty.cyr` once chakshu became a second consumer. Not a TUI framework — just termios + ANSI + cursor positioning. Was 0.3.0 / pin 5.10.20 in last refresh; 0.3.5 added ANSI color escape sequences so bannermanor's banners can render colored, AND graduated the pin straight from 5.10.20 → 6.0.1 in the same touch. |
| **hapi** | **0.5.0** | **6.0.1** | **NEW.** GNU `stow`-equivalent — dotfile / symlink farm manager. Hawaiian हपी (*happy*) + backronym **H**ome **A**sset **P**rovisioning **I**nterface — first Pacific Islands word in the AGNOS naming surface. CYML manifest per package, capability-bounded execution (touches `$HOME` only by default), lightweight audit trail. Cut at 0.5.0 straight on v6.0.1. |
| **kii** | **0.1.0** | **6.0.1** | **NEW 2026-05-22** — `chafa` / `jp2a` / `viu`-equivalent: image → ANSI/ASCII-art converter for terminal display. **Four-layered name** across three language families: (1) Hawaiian *image / picture / likeness* (what it produces); (2) East Asian *ki* (気) / *chi* (氣) — *life-force / vital energy* (kii is the *ki of the terminal*, the animating force that brings the screen to life via images); (3) English-phonetic back-half of **a-scii** (what it emits); (4) functional convergence — produces images via ASCII to animate the terminal, all three language angles describing the same operation. Triple-lane crossover (Polynesian + East Asian + English) is rare in the AGNOS naming surface — typical names sit in one lane. Polynesian Hawaiian micro-cluster with `hapi`, `anuenue`. Cut straight at 0.1.0 on cyrius 6.0.1; reads raster input (PNG, JPEG, GIF, BMP planned), quantizes to terminal-renderable color palette + glyph set, emits ANSI escape sequences sized to terminal cols × rows. |
| **iam** | **1.0.0** | **6.0.1** | **NEW.** fastfetch/neofetch-equivalent system-info display for login MOTD + screenshot flex. Pure inverse of `whoami` — whoami says who the user is, iam says what the system is. Thin presentation layer over the mihi probe library. Cut straight to 1.0.0 on cyrius 6.0.1 — second v6.0.x graduation. Lives in the English-wordplay/trickster naming lane per `feedback_naming_lanes`. |
| **mihi** | **1.0.0** | **6.0.1** | **NEW.** मिही / mihi — system-info probe library (CPU / RAM / GPU / kernel / uptime / distro / hostname). Substrate for iam, chakshu, and any tool that needs "tell me about this box." Maori: the formal self-introduction ceremony — Sanskrit-Hindi/Polynesian semantic naming lane per `feedback_naming_lanes`. First of the three v6.0.x graduations to be cut. |

---

## Active sweeps

### Niyama fold-in (v5.9.0 downstream sweep)

Fresh sibling-fold per niyama ADR 0011. Pattern parallels sandhi-fold (v5.7.0) and vani-fold (v5.8.0).

| # | Action | Status |
|---|--------|--------|
| 1 | External consumer audit (cyim is #1 multi-consumer gate; bare-metal kernel queued #2) | ✅ Done at v5.9.0 |
| 2 | In-tree fixture migration if needed | ✅ N/A — no fixtures touched |
| 3 | cyim → niyama integration verification (regex sweep) | ✅ Done — `cyim/src/main.cyr:51` includes `lib/niyama.cyr` directly |
| 4 | Document the niyama-fold pattern alongside sandhi/vani-fold in `design-patterns.md` | [ ] Pending |
| 5 | Niyama-fold-in article slot | [ ] Subsumed by [*What Justifies a Stdlib Foldin*](../articles/what-justifies-a-stdlib-foldin.md) — per-fold piece optional |

### gnoboot 0.2 merge + version bump (closed 2026-05-15)

Branch `0.2` merged to `main` (`529dfc1`) and tagged `v0.2.0`. Was one commit ahead at `d981fae` ("cleanup of canary and clear framebuffer for initial display"); structurally clean for merge after iron-boot ground truth landed.

| Action | Status |
|---|---|
| CMOS port-I/O blocks stripped (5 inline sites: entry / HandleProtocol / ELF-load / pre-EBS / post-EBS) | ✅ on branch — 0 CMOS refs in 0.2 vs 6 in main |
| UEFI-output collapse (`msg_li_f` … `msg_ebs_f` → shared `msg_fail` + `code_*` table + `efi_fail(st, code)` helper) | ✅ on branch |
| Banner tightened to `gnoboot v<VERSION>: handing off to kernel` (`tests/ovmf_smoke.sh` `EXPECT` synced) | ✅ on branch |
| `efi_clear(st)` called pre-banner so handoff line is the only thing on the framebuffer | ✅ on branch |
| Boot-info struct ABI preserved (magic `0x41474E4F`, RDI handoff, `fb_phys` at +0x48, GOP capture retained) | ✅ verified |
| CHANGELOG `[Unreleased]` section drafted | ✅ on branch |
| Bump `VERSION` 0.1.0 → 0.2.0 | ✅ shipped in 0.2.0 |
| Bump `cyrius.cyml` `version` field 0.1.0 → 0.2.0 | ✅ shipped in 0.2.0 |
| Sync `src/main.cyr` `msg_pre` banner UTF-16LE byte (`0x31` → `0x32`, `v0.1` → `v0.2`) | ✅ shipped in 0.2.0 |
| Sync `tests/ovmf_smoke.sh` default `EXPECT` (`gnoboot v0.1.0` → `gnoboot v0.2.0`, both literal + usage-example) | ✅ shipped in 0.2.0 |
| Rename `[Unreleased]` → `[0.2.0] — 2026-05-15` in `CHANGELOG.md` (Unreleased section preserved as empty placeholder for next cycle) | ✅ shipped in 0.2.0 |
| Rebuild verifies `OK` (`CYRIUS_TARGET_EFI=1 cyrius build src/main.cyr build/BOOTX64.EFI`) | ✅ verified 2026-05-15 |
| Merge `0.2 → main` | ✅ landed (`529dfc1` on main) |
| Tag `v0.2.0` on `main` | ✅ tagged |
| Push branches + tag | ✅ pushed |

Sweep closed 2026-05-15. Per `feedback_bootloader_kernel_ownership` Claude owns gnoboot end-to-end during iron-boot bring-up; merge + tag were user-driven git ops per CLAUDE.md (user handles all git operations).

### Vani audio distlib fold-in (v5.8.0 downstream sweep)

Per [`vani/docs/development/cyrius-stdlib-fold-in.md`](https://github.com/MacCracken/vani/blob/main/docs/development/cyrius-stdlib-fold-in.md). Pattern parallels v5.7.0 sandhi fold.

| # | Action | Status |
|---|--------|--------|
| 1 | External consumer audit (`grep -rn "include.*lib/audio.cyr"` across ecosystem) | ✅ Done at v5.8.0 — zero hits |
| 2 | In-tree fixture migration (3 preprocessor-cap regression tests) | ✅ Done in cyrius repo at v5.8.0 |
| 3 | Document the fold-in pattern alongside sandhi-fold in `design-patterns.md` | ❌ Still pending (now subsumed by fold-in article) |
| 4 | Vani-fold-in article (parallels sandhi-fold article slot) | ❌ Still pending (now subsumed) |

### v5.7.x → v5.8.x → v5.9.x → v5.10.x → v5.11.x debt carry-forward

Status verified 2026-05-11 eve.

| # | Item | Status | Notes |
|---|------|--------|-------|
| 1 | yantra orphan `lib/http_server.cyr` delete | ❌ Pending | File still present at `yantra/lib/http_server.cyr`; cleanup-only, no callers. yantra still at 5.6.17 (deep-lag). |
| 2 | sit orphan `lib/http_server.cyr` delete | ✅ Done | Removed during v5.8.x; sit now at 5.9.37 |
| 3 | vyakarana grammar refresh — index 469 sandhi fns | ❓ Re-verify | vyakarana at 2.2.1 / 5.10.5 — version stable through v5.10–v5.11 burst, content reflection still unverified |
| 4 | vidya per-minor refresh (`language.toml` / `dependencies.toml` / `ecosystem.toml`) | ✅ Likely done | vidya at 2.7.0 / pin 5.9.43 — stable across two minors with active content tree |
| 5 | hoosh / ifran / daimon / mela / ark sandhi-fold audit-confirm | ✅ Confirmed clean | Zero `[deps.sandhi]` and zero include-sandhi refs in any (hoosh still on `cyrius.toml`; daimon now on .cyml 5.10.44; ark on .cyml 5.1.10 deep-lag) |
| 6 | **Boot-to-shell MVP enablement** | ✅ MVP GATE HIT 2026-05-18 (Iron Attempt 68) | argonaut 1.7.0 + kybernet 1.2.1 added agnoshi to BOOT_MINIMAL defaults 2026-05-11 eve. Closed-beta MVP definition (kernel + kybernet + agnoshi typeable on iron archaemenid) clears at agnos 1.30.9, cyrius pin 5.11.64. Active 1.30.x branch is now framebuffer refresh quality (1.30.10). |

### CVE-2026-31431 (Copy Fail) cleanup + audit

Linux kernel LPE in `algif_aead` (AF_ALG in-place AEAD + `splice()` → 4-byte page-cache write → root). Disclosed 2026-04-29; affects mainline kernels from 2017 onward. Roadmap item **S1**.

**AGNOS-native kernel** (`agnos` v1.30.9): structurally immune — verified at `kernel/core/syscall.cyr:32-36`, 26-syscall table contains no `socket`, no `splice`, no AF_ALG family. Bug class is unreachable. (Kernel has moved 1.26.1 → 1.30.9 since the original CVE audit across multiple bring-up cuts; syscall-surface unchanged. Syscall table verification is anchored on the syscall-table invariant, not the kernel patch level — re-verify only if the syscall surface grows.)

| # | Action | Status |
|---|--------|--------|
| 1 | Host defconfigs — pin `# CONFIG_CRYPTO_USER_API{,_HASH,_SKCIPHER,_AEAD,_RNG} is not set` in `agnosticos/kernel/{6.6-lts,6.x-stable,7.0-devel}/configs/*defconfig` and `kernel/configs/edge-{deeplens,nuc,rpi4,rpi5}.config` | ❌ Pending — re-verified 2026-05-09: zero `CRYPTO_USER_API` refs in any host defconfig |
| 2 | Audit local crypto-adjacent repos for `AF_ALG` / `algif_aead` refs: sigil, agnosys, phylax | ✅ Done 2026-05-03 — zero hits |
| 3 | Audit when next cloned: kybernet, libro, kavach, shakti, aegis, t-ron, argonaut, ark, agnostik, bote, daimon, hoosh | ❓ Deferred — repos not all local |
| 4 | Once defconfigs pinned, document the absence-by-design pattern alongside other AGNOS-vs-Linux structural-immunity examples in `design-patterns.md` | [ ] |

### Pending Cyrius ports

| Repo | Current state | Action |
|------|---------------|--------|
| **bhava** | 2.0.0 Rust (no `cyrius.cyml` on remote) | Port can start — v5.10.x stdlib + math additions are the gating concern |
| **goonj** | 1.4.3 Rust (Cargo.toml present locally) | Acoustics — port pending |
| **naad** | 1.2.5 Rust (Cargo.toml present locally) | Audio synthesis — port pending |
| **aethersafha** | 0.1.0 scaffold | Real implementation (Wayland compositor) |
| **aethersafta** | 0.50.0 (media compositing scene graph) | Distinct from aethersafha — not a Cyrius port target, near-stable lib |

### Per-repo housekeeping (P1/P2)

Carry-forward from v5.6.x → v5.7.x → v5.8.x → v5.9.x → v5.10.x. None blocking; bundle with each repo's next natural patch.

- **`docs/development/state.md` migration** (this pattern). **Done**: cyrius, owl (2026-04-23), agnosticos (this file), sandhi, sit, vidya. **Pending**: every other repo that still carries volatile state in CLAUDE.md (verified 2026-05-06: kybernet, daimon, agnos, abaco, hoosh, kavach, mabda, sigil all missing; presume similar for the unverified tail).
- **`cyrius.toml` → `cyrius.cyml` format migration**: tail collapsed v5.10–v5.11. **Migrated** (local-verified 5.10.44+): agnoshi, bote, t-ron, kavach, itihas (remote), nein. **Remaining** (local-verified pre-CYML / no pin): hoosh, shravan. **Remote-only — unverified**: avatara, ai-hwaccel, hadara — these likely migrated alongside the wave but need cloning to confirm.
- **`[build].modules` → `[lib] modules` migration**: sigil, agnosys, shakti pending. sakshi has `dist/sakshi.cyr` but no `modules` block — investigate generation mechanism.
- **`docs/adr/` scaffold** (12 repos still missing): agnosys, sigil, takumi, phylax, ark, nous, sakshi, yukti, bsp, owl, cyrius-doom, majra. Copy `README.md` + `template.md` from sit; don't back-fill historical decisions.
- **`docs/adrs/` → `docs/adr/` rename**: argonaut last offender.
- **kiran pin-field population** — kiran shipped 1.0.0 but `cyrius.cyml` still lacks `cyrius = "X.Y.Z"`. Worth populating now that it's stable.
- **Crate registry refresh** — ✅ swept 2026-05-20 PM (post-Attempt-82 drift sweep).
  - [`planning/shared-crates.md`](planning/shared-crates.md) (full registry, pre-1.0 + v1.0+): footer date now 2026-05-20 PM. All flagged bumps confirmed in-tree (agnostik 1.2.2, agnosys 1.2.6, sigil 3.1.1, sankoch 2.2.5, libro 2.6.3, sandhi 1.3.4, niyama 1.0.2, aegis 1.0.0, cyim 1.7.0, chakshu 0.6.0, darshana 0.3.5). v1.0+ graduations folded: **mihi 1.0.0** (OS & Infrastructure, +1 row), **iam 1.0.0** + **bannermanor 1.0.0** (Binaries & Tools, +2 rows). agnos 1.30.7 → 1.31.2 (pin 5.11.59 → 6.0.1) and agnoshi 1.3.2 → 1.3.3 (pin 5.10.44 → 6.0.1) rows refreshed to reflect storage-arc + iron-validation + mid-cycle pin graduations. gnoboot 0.4.1 → 0.4.2. Bannermanor moved from pre-1.0 to v1.0+ section; pre-1.0 binaries count 13 → 11. v1.0+ Stable Index header count 86 → 89.
  - [`docs/applications/libs/README.md`](../applications/libs/README.md) (v1.0+ stable subset): header date 2026-05-18 → 2026-05-20 PM, lib count 78 → 79 (mihi added to OS & Infrastructure), Binaries & Tools at v1.0+ list expanded to 12 (added bannermanor, commandress, iam alongside existing entries; pointer-link updated to `#binaries--tools-13-crates`).

### CLAUDE.md table refresh

Root [`CLAUDE.md`](../../CLAUDE.md) "Standalone Repos" table also drifted (same pattern as shared-crates.md). When refreshing, prefer pointing to this file or to shared-crates.md rather than re-duplicating versions in CLAUDE.md.

---

## Doc / article update queue

| File | Action |
|------|--------|
| [`roadmap.md`](roadmap.md) | v5.7.x / v5.8.x / v5.9.x / v5.10.x / v5.11.x phase definitions are now historical; v6.x = "what the language gains" (RISC-V rv64, PIE, closures, Class-B FFI, bare-metal). v5.12.x reservation rolled into v6.x. Re-touch on each v6.0.x ship. |
| [`summer-2026-arc.md`](summer-2026-arc.md) | Cycle-theme references need re-anchoring against v5.10.x three-arc retro + v5.11.x stdlib-annotation closeout + v6.0.0 rename-ceremony framing + closed-beta MVP gate hit on iron. |
| [`articles/cyrius-vs-rust-benchmarks.md`](../articles/cyrius-vs-rust-benchmarks.md) | Add v5.9.x / v5.10.x / v5.11.x / v6.0.0 rows; sweep "Pure compute gap" language for closed items |
| [`articles/port-ledger-volume-1.md`](../articles/port-ledger-volume-1.md) | ✅ Refreshed 2026-05-15 — locked to v5.5.4 baseline; accreted body updates stripped; *What Comes Next* expanded to 5-volume arc (V1 baseline → V2 mid-arc → V3 end-of-5.x/v6.0 → V4 post-v6.x → V5 synthesis). Where-Rust-Still-Wins markers point at Volume 2/3. |
| **NEW** [`articles/port-ledger-volume-2.md`](../articles/port-ledger-volume-2.md) | ✅ Shipped 2026-05-15 — mid-arc state-of-things snapshot. Kernel iron-validation receipt (the V2 headline); pin-cluster review across 5.10/5.11 ecosystem; four new native subsystems (aegis 1.0.0, gnoboot 0.2.0, commandress 0.1.0, kriya 0.2.0); V1's "Where Rust Still Wins" reviewed for direction-of-motion. Re-measurement comprehensive-cut deferred to V3. |
| [`articles/doom-in-cyrius.md`](../articles/doom-in-cyrius.md) | v5.11.x rebuild numbers when cyrius-doom ships an unblock release (still on pin 5.7.48) |
| [`articles/sovereign-compiler-vs-brute-force.md`](../articles/sovereign-compiler-vs-brute-force.md) | cycc self-host **874,240 B** at v6.0.0 (was cc5 741,048 B at v5.9.0; +133 KB across the v5.10.x three-arc cycle + v5.11.x 70-patch closeout; +8 B name-string delta at the rename ceremony). Pull current size from `cyrius/build/cycc` before publishing. |
| [`planning/shared-crates.md`](planning/shared-crates.md) | 🔄 Stale as of 2026-05-20 PM. Refresh queue: agnostik 1.2.2, agnosys 1.2.6, sigil 3.1.1, sankoch 2.2.5, libro 2.6.3, sandhi 1.3.4, niyama 1.0.2, aegis **1.0.0**, cyim 1.7.0, chakshu **0.6.0**, darshana **0.3.5**. New (graduated from planned → shipped): argonaut 1.7.0, kybernet 1.2.1, bannermanor 0.5.0, hapi 0.5.0, iam 1.0.0, mihi 1.0.0. |
| [`docs/applications/libs/README.md`](../applications/libs/README.md) | Bump versions for v1.0+ subset; "Last Updated 2026-04-15" predates three minors |
| [`first-party/first-party-documentation.md`](first-party/first-party-documentation.md) | Re-read at each v6.0.x patch — meta-irony from *Docs Go Stale Before the Commit* |
| [`first-party/first-party-standards.md`](first-party/first-party-standards.md) | ✅ Refreshed 2026-05-09 — full Cyrius-first rewrite; Rust-era archive at `docs/archive/first-party-standards-rust-era.md` |
| **NEW** ✅ [*What Justifies a Stdlib Foldin*](../articles/what-justifies-a-stdlib-foldin.md) | Shipped 2026-05-06 — meta-process article covering the gate framework, anti-criteria, mechanism, and three-instance pattern across sandhi/vani/niyama. Subsumes per-instance article slots. |
| **NEW** ✅ Phase-3-stdlib-foldin retrospective | Landed 2026-05-06 in vidya at `content/cyrius/field_notes/compiler/retros/foldin_arc_v57_v59.cyml`. Companion to *what-justifies-a-stdlib-foldin* (process) — the retro is the experiential ledger. |
| **NEW** [*v5.10.x: three arcs in five days*] (working title) | v5.10.x retro candidate — reframe past *REAL TYPE SYSTEM in 24 patches* working title. Cycle closed 2026-05-11 at .50 with three completed arcs (typed-simd ABI 11 phases, REAL TYPE SYSTEM 5 phases, struct-byval ABI 3 phases) + 2.7× compile-perf miniarc + PE premise debunk. Draftable now. |
| **NEW** [*Typed SIMD: the substrate for native codec ports*] (working title) | Companion to *port-ledger* — the typed-simd ABI arc (v5.10.28 → v5.10.39) is the foundation that turns tarang's "framework-only, codecs via C FFI" placeholder into an eventual handwritten-SIMD-codec lane (dav1d/FFmpeg territory). Frame as the *prerequisite landed; codec arc is future-arc work post-bare-metal*. Tarang competition framing piece. |
| **NEW** ✅ darshana extraction note | When darshana ships 1.0.0, document the cyim-private → shared-library extraction pattern (single-consumer-private → second-consumer-triggers-extraction) alongside other extraction examples. |
| **NEW** [*Why AGNOS-native agents can't be drained by a tweet*] (working title) | Black Hat / summer-2026-arc Beat 2 article — AGNOS agent-injection defense as second instance of the absence-by-design structural-immunity pattern (kernel CVE-2026-31431 was the first). Spec: [`planning/agent-injection-defense.md`](planning/agent-injection-defense.md). Roadmap: Phase 15A. Draft after Phase 1 ships (post-closed-beta). |

---

## Refresh procedure

When a v6.0.x patch closes:

1. Pull current `VERSION` + `cyrius.cyml`/`cyrius.toml` for affected repos
2. Update the pin-lag spectrum if any repo crossed a band (and exited the v5.11.x leading-edge bedrock by re-pinning to v6.0.x)
3. Tick off swept items
4. Re-anchor "Last refresh" date in the header
5. Re-anchor "Cyrius toolchain" if a new minor cut

When v6.0.x cycle closes:

1. Move v6.0.x slot list closeout summary into a brief retrospective (one paragraph)
2. Repoint all `6.0.x` references to whichever cycle is next (v6.1.x — back-compat symlinks drop here, plus whichever v6.x feature slot lands first)
3. Don't archive — rewrite in place. Git history is the snapshot.

---

## Related

- [`CLAUDE.md`](../../CLAUDE.md) — preferences/process/procedures (this doc holds the volatile state CLAUDE.md should NOT carry)
- [`applications/shared-crates.md`](applications/shared-crates.md) — authoritative crate registry (versions + roles)
- [`roadmap.md`](roadmap.md) — Cyrius milestone definitions and timeline
- [Cyrius CHANGELOG](https://github.com/MacCracken/cyrius/blob/main/CHANGELOG.md) — authoritative source for cycle status
- [Articles: *Docs Go Stale Before the Commit*](../articles/docs-go-stale-before-the-commit.md) — the rationale for state.md as a pattern
- [Articles: *Your Docs Are About to Rot*](../articles/your-docs-are-about-to-rot.md) — the broader drift argument
- Per-repo `docs/development/state.md` (where it exists) — source of truth for that repo's local state; verify before acting

---

*Refresh in place per [*Docs Go Stale Before the Commit*](../articles/docs-go-stale-before-the-commit.md). Per-day refresh narratives previously accreted here have been pruned — git history is authoritative for prior-state recovery; CHANGELOGs + iron-nuc-zen-log are the canonical event ledgers.*
