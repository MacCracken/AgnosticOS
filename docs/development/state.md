---
name: AGNOS Ecosystem State
description: Live cross-repo state — kernel head, Cyrius pin/cycle pointer, active sweeps, carry-forward debt, registry pointers
type: state
---

# AGNOS Ecosystem — Current State

> **⚠ NOT A LOG.** This file is **live state with pointers** — current truth only, plus links to where the history lives. Iron attempt history → [`iron-nuc-zen-log.md`](iron-nuc-zen-log.md). Per-repo release history → each repo's `CHANGELOG.md`. Crate versions → the two registry pointers below. If you find yourself writing prose narrative here, it belongs in one of those other files.
>
> **Cyrius pin**: agnos holds **6.3.43** (`cyrius.cyml`, bumped 2026-07-03 from 6.3.9 to get on near-latest before the v6.3.x closeout; earlier 6.2.44→6.3.9 on 2026-06-30). The freestanding agnos kernel is `cmp`-byte-identical across pins (the 6.3.9→6.3.43 bump kept `build/agnos` at 1,345,512 B, `cmp`-proven — the freestanding kernel has no per-thread stacks, so **6.3.13's array-locals-to-stack default-on doesn't touch it**; hda-smoke + agnsh-smoke re-confirmed green post-bump), so the pin is provenance only — but **the symlink#63 syscall (1.51.0) created a hard floor: its userland peer `sys_symlink` exists only in cyrius ≥ 6.3.6**, so agnos (home of the syscall + the `symtest` exerciser that calls it) must pin ≥ 6.3.6 to cohere with the feature it ships. agnos now pins **6.3.43**; ark + the `symtest` exerciser still pin **6.3.9** (the version with the peer — bump them alongside when convenient). Latest released cyrius tag is **6.3.43** (the last v6.3.x work before closeout — cross-host stdlib verification + PE lint; v6.3.x is nearing its close). agnosticos boot-pipeline (`scripts/cyrius.cyml`) pins **6.0.14** (boot-script-only). ⚠ `scripts/build.sh` runs the cyrius wrapper **without `--strict-pin`**, so a newer installed `cycc` silently builds agnos (warn-only) until the pin is bumped — check `cyrius --version` when a build's provenance matters. Cyrius is **hands-off** here: cycle status + v6.x carry-forward triage (RISC-V rv64, bare-metal, PIE/closures/Class-B FFI, the O3–O6 opt-arc audit, held `ESTORESTACKPARM`, consumer-rollup tails) live in cyrius `roadmap.md` + `CHANGELOG.md`. v6.x opened 2026-05-19 at the v5→v6 boundary (v5.11.x closed at 5.11.69); **v6.3.x is the active minor**, v6.0.x/v6.1.x/v6.2.x closed.
>
> **Last refresh**: 2026-07-05. **Kernel head: agnos 1.52.8** (LAPIC-calibration cut) — the single tracked dev head; the per-repo CHANGELOG + [`agnos/docs/development/state.md`](https://github.com/MacCracken/agnos/blob/main/docs/development/state.md) own cut-by-cut detail. **✅ 1.52.x AUDIO-OUTPUT (HDA/Azalia) arc CLOSED + IRON-VALIDATED 2026-07-05:** all four gates QEMU-green, first sound iron-fired at 1.52.5; the iron echo/underrun was root-caused to an **uncalibrated LAPIC timebase** (hardcoded QEMU-tuned reload ran ~12× slow on real Zen → the 100 Hz HDA servicer starved; found via the new `tonegen` diagnostic) and FIXED at **1.52.8** (boot PIT-ch0 calibration + `snd_close` ring-silence); Gate 3/4 = the cyrius **`vani`** audio-lib agnos backend (vanitone) landed + folded into **cyrius 6.4.3** (with yukti 2.2.8 / patra 1.12.8; consumers cleaned — cyrius-doom 0.31.5 / polyomino 0.5.2 / bb 0.8.1). **BURN 4 CLEARED** — cyrius-doom (0.31.5) plays with CLEAN sound during ACTUAL `run /bin/doom` gameplay out the archaemenid front jack; the headline acceptance ("runs DOOM *with sound* on iron") is MET (the parked contingency fixes — underrun silence-guard, HDA DMA-position-buffer — were not needed). **Next arc: 1.53.x kernel FP/SIMD** (f64 in ring-3; plan-doc + agnoshi `>`-redirect iron-capture companion staged). **Backlog:** audio via video-cable (HDMI-audio, the `04:00.1` function — a separate codec/stream from the analog ALC897, its own later task) + the non-blocking `AUDIO_AGNOS_TARGET` latency trim (256 ms → ~60-90 ms). Detail → [`#tracker-152x-cycle`](iron-nuc-zen-log.md#tracker-152x-cycle). ━━━ historical arc detail (B0-B4, arc-open) below ━━━ first sound from sovereign agnos: kernel HDA driver (`kernel/core/hda.cyr`, 6 bites **B0–B5**, polled-no-IRQ) → ring-3 `snd_*` syscall band **`#64–#69`** → cyrius `vani` agnos backend (⚠ two-sided; cyrius `sys_snd_*` peer HANDS-OFF, surface to user) → proof-app = **`cyrius-doom` with sound** out the archaemenid front jack (Realtek ALC897 on `04:00.6` AMD Ryzen HD Audio). Acceptance = "runs DOOM *with sound*" on iron. **▶ B0 LANDED + QEMU-validated 2026-07-03** — `kernel/core/hda.cyr` `hda_probe()` (read-only: locate controller → BAR0 UC → bus-master → GCAP/version) wired into `main.cyr` device-init; `scripts/hda-smoke.sh` green: **`hda: found 8086:2668 OSS=4 ISS=4 v1.0`** (QEMU intel-hda, OSS=4≥1). Probe order `pci_find(0x1022,0x15e3)`→`pci_find(0x8086,0x2668)`→class-fallback (archaemenid's HDMI-audio `04:00.1` is same-class + enumerates before the `04:00.6` controller, so a bare class-probe would bind the wrong function). **▶ B1 LANDED + QEMU-validated** (`hda_reset()`: CRST enter/exit handshake with polled bit0 + settle budgets 200 µs/1500 µs ≥ the 521 µs/25-frame §4.3 floor + STATESTS re-read → **`hda: reset OK, codecs=0x0001`**; adds `hda_udelay` rdtsc-spin GAP-1 + `hda_write*` readback-flush accessors; smoke attaches an `hda-duplex` codec). **▶ B2a LANDED + QEMU-validated** (`hda_ring_init`/`hda_verb`/`hda_codec_probe`: 256-entry CORB/RIRB DMA rings [nvme admin-ring mapping], CORBRP two-step reset, phase-bit-free verb poll → **`hda: codec0 vendor=0x1af40022 nodes=1`**, consecutive verbs). **Caught+fixed a consecutive-verb stall**: QEMU's `corb_run` gates on `rirb_count==RINTCNT`, so `RINTCNT=1` drained exactly one verb then stalled (2nd verb→`nodes=255`); fix = `RINTCNT=0xFF` + per-verb RIRBSTS ack (correct on iron too). **▶ B2b-1 LANDED + QEMU-validated** (`hda_codec_enum`: root → AFG → widget walk, classify DAC/Pin, dump each output pin's CONFIG_DEFAULT → **`hda: afg 0x01 dacs=1 pins=2 outpins=1`** + `hda: pin 0x03 cfg=0x4010 dev=0 conn=0 loc=0x00`). The pin-dump IS the mandatory iron pre-flight (reveals the ALC897 front-jack node before routing freeze). **▶ B2b-2 LANDED + QEMU-validated → B2 COMPLETE** (`hda_codec_route`: score-select output pin [accept HP-Out+Line-Out, reject NONE, prefer HP-Out+Front+jack-detect, no hardcoded nid] → DAC conn-list trace → enable [Realtek-COEF vendor-gated → power D0 → PIN_CTL out+HP → EAPD PINCAP-gated → unmute+~75% gain] → **`hda: route pin=0x03 dac=0x02 dev=0`** + `hda: output path enabled`). Machinery QEMU-proven; **ALC897 amp/EAPD/COEF + front-jack routing are iron-only** (archaemenid burn). **▶ B3 LANDED + QEMU-validated + HDA-1.0a-spec-verified** (`hda_stream_arm`: BDL ring [WB] + WC PCM ring [`pmm_alloc_2mb`+`vmm_remap_wc_2mb`] + output SD at `0x80+ISS*0x20`, SRST→tag→CBL→**SDnFMT 0x0011**→LVI→BDL→RUN with tag preserved, codec DAC format-bound both sides → **`hda: stream running (LPIB advancing)`**; device reads phys, driver via `pmm_kva_for_access`). **5-agent adversarial spec-verify: 5/5 CONFIRMED, 0 defects** on the iron-only traps QEMU masks (SDnFMT pitch, format-match, sd_base, SRST+tag ordering, BDL align/LVI/CBL, phys-vs-VA). **▶ B4 LANDED + QEMU-validated — 🔊 FIRST SOUND FROM SOVEREIGN AGNOS** (`hda_stream_arm` build-gated `HDA_TONE`: fills a ~375 Hz integer triangle [128 frames/period, click-free loop; `store16` into the WC ring] then arms → `hda-tone-smoke.sh` QEMU `-audiodev wav` **RMS=5135** — tone flowed DMA→stream→codec→output). Gated so production stays silent (default build `cmp`-identical to 1.52.3). **Next bite: B5** (double-buffer refill on LPIB-midpoint → gap-free multi-second playback; **closes Gate 1** = the kernel HDA driver). **Iron gate pending:** same `HDA_TONE` kernel → archaemenid ALC897 front jack (audible). **Released: 1.52.0(B0)/1.52.1(B1)/1.52.2(B2)/1.52.3(B3)**; B4 staged → 1.52.4. Plan → [`planning/audio-arc-152x-plan.md`](planning/audio-arc-152x-plan.md) (the verified "how"), scope → [`planning/audio-arc-152x.md`](planning/audio-arc-152x.md). **▶ 1.51.x sovereign-package-manager-kernel-surface + net-latency RX-IRQ arc CLOSED + iron-validated 2026-07-03** (the net-latency sub-arc burned green on archaemenid 2026-07-02 — `r8169: RX MSI LIVE`; **1.51.9 bote-serve-on-real-kernel test infra = the arc-closing cut**). Base/server kernel surface squared away. ━━━ arc detail below ━━━ **▶ 1.50.x RAM full-usage continuation arc CLOSED + iron-validated; 1.51.x OPENED 2026-06-29 — sovereign-package-manager kernel surface** (the ark v2 / agnova prerequisite gaps; `ark → agnova → server-stage exit`). **(a) `symlink`#63 ✅ DONE 2026-06-30** — both halves shipped (kernel `ext2_symlink` 1.51.0 + cyrius `sys_symlink` peer 6.3.6, current 6.3.9) and the on-agnos round-trip is **proven**: `agnos/scripts/symlink-smoke.sh` boots a `--agnos` exerciser that calls `sys_symlink("/etc/hostname","/hn_link")`, the symlink lands (`debugfs`: `Type: symlink` / `Fast link dest: "/etc/hostname"`), `open()` follows it to the target bytes, and `e2fsck -fn` is clean. ark's install path is wired (`ark/src/portable.cyr` `ark_symlink`→4-arg peer + `ark_symlink_supported()`→true; pin 6.3.9; `cyrius lib sync`; agnos cross-build compiles at **15.9 MB**). **The 15.9 MB ark binary now LOADS + RUNS in ring 3 on agnos ✅ (2026-06-30)** — `agnos/scripts/ark-run-smoke.sh` PASS (ark parses argv + prints its usage + exits cleanly). It first triple-faulted, root-caused NOT to binary size but to a **kstack VA-space collision**: the per-process interrupt stacks (`proc_rsp0`) AND per-CPU syscall kstacks (`pcpu_syscall_kstack_top`/`top2`) lived in region 7 (phys 14–16 MB), inside the low identity window where `elf_load` maps user PT_LOADs — ark's segment (to ~20 MB) overrode `PD[7]` in its own CR3 and clobbered the kstacks → triple fault. **Fixed by relocating both kstack kinds onto the direct-map alias** (`DIRECTMAP_BASE + phys`, 8 GB+, mirrored into every per-proc CR3, never segment-overridden) — the >256 MB/direct-map infra (1.49.x) reused exactly as intended; phys stays region-7-reserved, data-only change (`proc.cyr` + `syscall_hw.cyr`). ark was NOT shrunk (a 16 MB binary is fine on the full-RAM box). No regression: ring3-smoke / exec-smoke / check.sh 11/11 / agnsh-smoke all green. Issue `agnos/docs/development/issues/2026-06-30-large-binary-exec-stack-overflow.md` RESOLVED; standing regression `agnos/scripts/ark-run-smoke.sh` (`ARK_SELFTEST` gate). **ark v2 M3 is PROVEN end-to-end on agnos (2026-06-30)** — a takumi-built symlink-bearing `.ark` installs via `ark install` on the agnos-fs: the regular file `/arkroot/lib/libfoo.so.1` AND the symlink `/arkroot/lib/libfoo.so` → `libfoo.so.1` both land, `e2fsck -fn` clean (`agnos/scripts/ark-install-smoke.sh`, `ARK_INSTALL_SELFTEST`). The full `sovereign ark → agnova → server` critical path is demonstrated: symlink syscall → ark runs on agnos (kstack fix) → ark installs a `.ark` on agnos. Surfaced + fixed a 2nd ark ABI-portability gap on the way: `ark_mkdir` (agnos `sys_mkdir`#9 is `(path,pathlen)`, not host `(path,mode)`) — same class as `ark_symlink`. Open follow-on (filed): ark ignores its mkdir/write/symlink return codes, so a failed install reports success — should fail loud. ark M3 fixture builder: takumi `tests/mkfixture` (`create_file_list` + `ark_write`). kriya `ln -s` on agnos (`K_HAVE_SYMLINK==0`, pin 6.2.24) is a clean follow-on consumer now that the syscall exists. Remaining arc items (b boot-slot / c nested-exec / d argv-cap / e build-confinement) filed, gated on the ark self-host milestones. See agnos roadmap 1.51.x + [[project_ark_v2_sovereignty_path]]. **▶ 1.51.3–1.51.6 net-latency sub-arc (separate thread):** hoosh's Linux-vs-agnos connect bench surfaced agnos net latency floored at the 100 Hz tick (no NIC RX IRQ — `hlt` only woke on the timer, [[project_agnos_net_latency_tick_paced]]). Fixed across four patches: 1.51.3 busy-poll stopgap; 1.51.4 per-connect ephemeral src-port (busy-poll exposed a timer-derived TIME_WAIT collision); 1.51.5 **virtio-net MSI-X RX interrupt (QEMU-validated** — busy-poll-off bench 2000/2000 @ ~1.2 ms proves the IRQ carries the load, first interrupt-driven device beyond timer/keyboard); 1.51.6 **r8169 (RTL8168H) MSI RX interrupt** (QEMU has no r8169; strictly additive — the tick drain still pulls RX if the MSI misfires, so no boot-to-shell risk; derive-and-refute pre-burn review 2026-07-01 caught the RDU/FOVW self-clear hazard → `IMR=RxOK|RxErr` only); 1.51.7 **RX-IRQ-LIVE dispositive latch** — a one-shot `r8169: RX MSI LIVE …` FB line (ISR sets a flag, `net_poll` prints once) that turns "the MSI fired on silicon" from unobservable (busy-poll + tick-drain mask it) into a single line. **✅ IRON-VALIDATED + ARC CLOSED 2026-07-02 on real Zen (archaemenid):** the burn (1.51.7 = 1.51.6 MSI + 1.51.7 latch) shows `r8169: RX MSI LIVE (first hardware IRQ serviced, vector 0x50)` printing between `dhcp: DISCOVER` and `dhcp: OFFER 192.168.1.195` — the r8169 MSI serviced the DHCP OFFER RX on hardware — then a real lease, `net: L2 OK`, `[ASSIST] >`, and `yo google.com` = **4/4, 0% loss** (three RTTs at sub-tick `0.00 ms`), NO storm/hang/canary. The whole `100 Hz-tick-paced net latency` root cause is now fixed AND hardware-proven. **1.51.8 = the arc's cleanup cut** — trimmed the 1.51.3 busy-poll `NET_BUSY_SPINS` 20000 → 512 (belt-and-suspenders now that the RX IRQ carries the wire path; kept non-zero because loopback has no interrupt and must complete in-spin — loopback-smoke 5/5 confirms). Arc CLOSED. Outcome block at [`#tracker-151x-cycle`](iron-nuc-zen-log.md#tracker-151x-cycle). **★ 1.50.0 bite 2 (boot-CR3 → own-PML4 switch) IRON-VALIDATED 2026-06-29** on real 64 GB Zen (full FB-render boot to the agnsh prompt + live keyboard `iam`→`Memory: 61 GiB` + ~62 GB RAM; the pre-burn review's `vmm_remap_uc_2mb` guard fix held on silicon — FB stayed WC while all device BARs UC-remapped into the shared PDPT[3]). **1.50.1 PROMOTED the switch DEFAULT-ON** (opt-out `BOOTCR3_KEEP_GNOBOOT_CR3=1`); **a second clean iron burn of the default-on kernel gates the NEXT RAM items** (user mmap-arena). See [`#tracker-150x-cycle`](iron-nuc-zen-log.md#tracker-150x-cycle). **▶ 1.49.x >256 MB RAM arc IRON-VALIDATED + CLOSED 2026-06-28** (re-burn of 1.49.12 boots to the agnsh prompt on real 64 GB Zen, full RAM online; also cleared the unburned 1.47.x/1.48.x work that rode along). **▶ 1.50.x RAM full-usage continuation OPEN — 1.50.0 lands two QEMU-validated foundation bites** (`-m 8G`/`32G`): **bite 1 — full-RAM stress self-test** (`PMM_RAMSTRESS_SELFTEST`) caught + fixed a real latent corruption (the bitmap migration left UEFI-memmap GAPS like the 2–4 GB PCI hole free above 256 MB → `pmm_alloc_2mb` returned unbacked regions; fix = reserve-default + free only `EfiConventionalMemory`, re-stress `regions=16354 bad=0`); **bite 2 — boot-CR3 → own complete PML4 switch** (`BOOTCR3_OWN_PML4`, gated off) moving the kernel off gnoboot's transient boot CR3 onto its own `0x1000` PML4, placed late (after the FB WC remap, before `dev_init`) so memmap/boot_info reads are done + the FB is WC not WB (the WB-cache was the prior "freeze at Page tables" mirage; serial showed the full boot the whole time). QEMU `-m 8G`: serial → `[ASSIST]` prompt + a QMP framebuffer screendump renders the *entire* post-switch boot (NVMe/AHCI/GPT/VFS/ext2/heap/sched/kybernet/agnsh on `0x1000`). **▶ boot-CR3 switch DEFAULT-ON (1.50.1) + user mmap-arena FULL LIFT (1.50.2) BOTH IRON-VALIDATED 2026-06-29 → 1.50.2 TAGGED + RELEASED.** The mmap full lift burn shows `mmap-himem-e2e: >1GB map+free PASS` on real 64 GB Zen — a 1.026 GB contiguous high-VA arena (PDPT[128..511], direct-map-backed) maps + tears down with no leak, lifting the ~768 MB single-process cap, all under the default-on switch. **mmap follow-ons 1.50.3–1.50.6 ALL LANDED (QEMU-green):** high-range `munmap` (`proc_unmap_2mb_hi`, 1.50.3), per-process cursor (`proc_himmap_next[16]`, 1.50.4), NX/W^X on anonymous arena pages (1.50.5), PF_X-aware ELF mapping + NX user stack (1.50.6 — W^X finding: cyrius emits one RWE PT_LOAD/binary, segment-level W^X needs a cyrius-side split; proposal filed). The ring-3 userland >1 GB program is DEFERRED (desktop is the trigger). **▶ 1.50.7 — process-isolation hardening (2 sovereign items from the kavach↔agnos compat audit):** **(a) bg-job fault teardown** — a background (`&`) proc's CPL3 fault halted the box (only the foreground exec child got `kernel_resume`); now the bg path `sti;hlt`-yields to the scheduler (proven preemption path). Foreground unchanged (`fault-kill-smoke: PASS`); **bg-fault on-iron survival is the one iron-pending item this cut.** **(b) real `proc_get_ppid`** — was a stub returning 0 → `kill#16`'s child-only gate was inert; now a flat `proc_ppid[16]` filled at `proc_create_user` + the gate extracted into a testable `proc_may_signal` (`PPID_SELFTEST` → `ppid: child-gate PASS`). check.sh 11/11, agnsh-smoke PASS. **▶ 1.50.8 — waitpid#4 ownership gate** (the one 1.44.22-cleanup item 1.50.7 un-blocked; `sched_next` single-pass was already done at 1.47.8, MADT-AP-enum + loader-DRY stay deferred — never ppid-blocked). `waitpid#4` had no ownership check → any proc could reap any dead proc, stealing the exit code + collapsing the slot under the real parent's waitpid. New `proc_may_reap(caller,target)` (sibling of `proc_may_signal`): init reaps anyone, a non-init caller reaps only its direct child; gated before the alive/dead test. Validated: `ring3-smoke` PASS incl. `parent spawn+wait OK` (spawn#3 + waitpid#4 parent-reaps-child = the gate's happy path), extended `ppid: child-gate PASS`, check.sh 11/11. (The agnsh-bg `&` consumer was thought to be blocked by a `spawn_path#43` breakage — **that framing was WRONG**, corrected at 1.50.9 below.) **▶ 1.50.9 — agnsh `&` background jobs RESTORED + 1.50.10 hardening (CURRENT HEAD; ◀ NEXT IRON BURN).** #43 is correct; agnsh died with a `#PF` after its banner because the migrated PMM bitmap was reached by its **identity VA `0x400000`** — the fixed ring-3 code VA (`PD[2]`) every per-proc CR3 overrides — so `sys_mmap`#27, run under agnsh's CR3, read agnsh's own code → "0 free 2 MB regions" → `mmap`=0 → null-deref, which then HUNG the box at the 1.46.x reap fence. **Fix (1.50.9):** rebase the bitmap onto the **direct-map** (CR3-stable, mirrored by every per-proc CR3) + the fence clears `on_cpu` for a this-CPU-owned (kernel_resume-terminated) proc instead of spinning. **1.50.10 hardening:** always-on `pmm: bitmap-dm OK free2mb=N` guard + fence self-owned check (in `PPID_SELFTEST`) + sibling audit (the bitmap was the unique `PD[2]` victim). Regression window **1.50.3–1.50.6** (the mmap/NX rework) — the 1.50.1/1.50.2 iron burns ran agnsh + `iam`, so the bug came AFTER. QEMU: agnsh boots to `[ASSIST] >`, `agnsh-bg-test`/`agnsh-multijob-test` PASS, `check.sh` 11/11. **✅ IRON-VALIDATED 2026-06-29 on real 64 GB Zen** (FB: `iam &` → `[1] 6` → `[1] Done`, prompt stayed live, `iam | anuenue` rainbow-piped on agnsh 1.8.1, `Memory: 61 GiB`). → [`#tracker-150x-cycle`](iron-nuc-zen-log.md#tracker-150x-cycle). ━━━ decaying history below (1.47.x/1.48.x/1.49.x detail, compression-pass candidate) ━━━ **1.47.0 CUT 2026-06-27** shipped **(1) proc-teardown-on-fault** (a ring-3 fault kills the proc + returns to the agnsh prompt instead of halting) and **(2) per-process fd tables** (2a seam / 2b storage+inheritance / 2c pipe refcount → SMP-safe/streaming pipes + the 4 KB pipe-leak fix); both QEMU-validated, iron burn pending. **▶ Now in the 1.47.x perf-patch series** (perf tail + KASLR deferred from the cut, landing per-patch): **1.47.1 = ext2 block-write batching** — `ext2_write_block` issues 1 multi-LBA NVMe write per 4 KB block instead of 8 single-sector submits (`block_write_amp` multi=1 single=8, ext2-write-smoke green); **1.47.2 = ext2 dir-lookup inode reuse** — `ext2_path_lookup` keeps the current dir's inode resident + reuses it per component instead of re-fetching (a K-component path drops ~2K→~K+1 inode reads; `dirlk reuse=0 reread=1`, read-only so no write-invalidation risk, ext2-write-smoke + symlinks green); **1.47.3 + 1.47.4 = full-binary KASLR (Option A)** — the kernel binary's physical load base is randomized per boot (build `CYRIUS_PIE=1` → relocation-free ET_DYN / RIP-relative kernel; **gnoboot 0.6.0** picks an RDRAND-slid, 2 MB-aligned base in [32 MB, 254 MB) + jumps to base+e_entry). Above the 16 MB PMM ceiling + below the 256 MB per-proc-CR3 identity window → no kernel mem-rework, no relocation walk; the "cyrius has no PIE mode" blocker was a stale-doc misread (cyrius PIE shipped v6.1.6/6.1.8). `kaslr-smoke.sh` two-boot base-diff PASS + exec-smoke 15/15 on a slid kernel; `KASLR: kernel_base` probe; **1.47.5 = ext2 single-indirect read reuse** — `ext2_read_at` caches the `l1` indirect block across a sequential read (per-call local, no global state) instead of re-reading it per block, so a 589 KB single-indirect binary read drops ~135 `l1` reads → 1 (`indirect reuse=1 reread=2`; exec-smoke 15/15, e2fsck-clean); **1.47.6 = ext2 multi-block read coalescing** — `ext2_read_at` coalesces physically-contiguous block runs into ONE direct multi-LBA DMA (up to 16 blocks / 64 KB) straight into a DMA-safe dest (page-aligned + in the 0-256 MB identity window; gated on `iommu_active==0`, Intel VT-d falls back to bounce), **no bounce scratch / no PMM cost** — `block_coalesce_amp multi=1 perblk=16`, exec-smoke 15/15, e2fsck-clean; kill-switch `ext2_read_coalesce_max=1`. First ext2-read use of the NVMe >2-page PRP-list path → iron-DMA validation rides the burn; **1.47.7 = ext2 multi-block WRITE coalescing** (the write mirror of 1.47.6) — `ext2_write_at` coalesces contiguous full-block writes from a DMA-safe src into ONE direct multi-LBA NVMe command (skipping the per-block copy + bounce; non-coalesced fallback now word-wide memcpy/memset), same gate + shared `ext2_read_coalesce_max` kill-switch — `block_coalesce_write_amp multi=1 perblk=16` + `write-coalesce 32KB round-trip OK` (data byte-identical), e2fsck-clean, exec-smoke 15/15; **1.47.8 = scheduler single-pass round-robin** — `sched_next` collapses its two-pass idle-deprio scan into one (first ready+pickable non-idle wins; first idle remembered as the fallback) — byte-identical selection at half the per-tick iterations in the idle case (the 1.44.22 behavior-preserving cleanup); ring3-smoke + thread-smoke + agnsh-smoke + check.sh green. **Perf-list status:** the SUBSTANTIAL 1.47.x perf items are now covered (ext2 I/O fully collapsed + the scheduler scan); the perf-tail remainders — **syscall entry/exit** (the dispatch is already well-ordered, hot syscalls at depths 1-7 → near-zero win) + the **context-switch hand-asm** (low-headroom / higher-risk per the 1.44.22 audit) — are DEFERRED-with-reasons. The **1.47.x perf series is essentially complete** (1.47.8). **▶ 1.48.x OPENED 2026-06-27 — other-FS perf review (FAT/exFAT):** **1.48.0 = cluster-read amp** — `exfat_read` + `fatfs_read` read each cluster in ONE multi-sector backend call (`exfat_blk_read_sectors`/`fat_blk_read_sectors`) straight into buf instead of sector-by-sector + a byte-copy — the FAT/exFAT analog of `block_read_amp` (a ≤4 KB cluster → 1 NVMe command vs sectors_per_cluster; >4 KB falls to per-sector, no regression); exfat-smoke + fat-smoke green (multi-cluster chain read byte-exact); **1.48.1 = cluster-WRITE amp** (the write mirror of 1.48.0) — `fatfs_write_file` + `exfat_write_file` write each cluster's full data sectors in ONE multi-sector write straight from src (`fat_blk_write_sectors`/`exfat_blk_write_sectors`) instead of per-sector + byte-copy — the analog of ext2's `block_write_amp` (a ≤4 KB cluster's data → 1 NVMe command); exFAT's contiguous clusters write the whole extent's data in one call; fat-write-smoke + exfat-write-smoke green (write+read-back + `fsck.exfat -n` clean). **1.48.2 = multi-cluster read coalescing** — `exfat_read`/`fatfs_read` coalesce a physically-contiguous cluster run into one ≤4 KB read (the cluster-level 1.47.6 analog; spc=1 → up to 8 clusters/command, capped so no >4 KB per-sector regression; FAT-scan hits the cached FAT block); exfat-smoke + fat-smoke green (multi-cluster chain read byte-exact). The **cluster-chain re-read reuse** (the 4th planned item) was found ALREADY DONE — both FS cache the FAT block (`fatfs_fat_buf`/`exfat_fat_buf`, re-read only on a FAT-block change). **▶ 1.48.x FAT/exFAT perf review COMPLETE** (read-amp 1.48.0 + write-amp 1.48.1 + multi-cluster coalescing 1.48.2 + FAT-block reuse pre-existing). Deferred-with-reason (roadmap): the 1.47.x perf-tail **syscall entry/exit** (dispatch already well-ordered) + **context-switch hand-asm** (low-headroom/risky per 1.44.22). **▶ 1.49.x OPENED 2026-06-27 — kernel-capability gaps** (active arc; **loopback picked first** — user 2026-06-27): **(1) full RAM init — ▶ bites 1-3 DONE (1.49.1–1.49.3)**: `pmm_probe_memmap` reads the UEFI memmap gnoboot passes (boot_info 0x28/0x30/0x34) → always-on boot log `RAM: usable=NMB top=` (sums EfiConventionalMemory pages + top-of-RAM); ram-probe-smoke green (256M→205MB, 1024M→973MB — reads real RAM, scales vs old hardcoded 128MB). **Discovery only** — PMM still allocs from the 4-16MB pool. **bite 2 (1.49.2)**: `pmm_extend_to_memmap` lifts the `pmm_alloc` 4 KB cap from 16 MB to the full 128 MB bitmap (additive — pmm_init's 0-18 MB reservations byte-identical; reserves firmware/MMIO memmap holes in the new window) → **~104 MB newly usable** (26752 free pages above 16 MB at -m 256M); pmm-fullram-smoke 3/3 + exec-smoke full pass (boot-to-shell + exec + e2fsck-clean) with it live. **bite 3 (1.49.3): the PIE auto-disable was a FALSE ALARM** — the failure was the full-RAM smoke's FAT-only ESP (no ext2 root, kybernet can't `exec /bin/agnsh`; non-PIE falls to the emergency shell, PIE doesn't), NOT the extension. REMOVED — PIE/KASLR now gets the extension too. Validated: PIE exec-smoke FULL PASS at kernel_base=58 MB (in-bitmap → kernel-image reservation load-bearing, top-down `pmm_alloc` skips it); kaslr-smoke PASS with the extension live (bases 132/252 MB, above-bitmap → slide varies + boots). **▶ >128 MB RAM arc OPEN** (user: "DO THE RAM", [[feedback_dont_dodge_large_items]]): **bite 1 (1.49.6) DONE — 128→256 MB** (per-proc CR3 identity PD[8..127] + bitmap 512→1024 u64; ~198 MB usable; pmm-fullram alloc_top=65535 + exec-smoke PIE+non-PIE green). **bite 2 (1.49.7) DONE — kernel direct-map**: all phys RAM mapped at `DIRECTMAP_BASE (8 GB) + phys` in the existing kernel PDPT @ 0x2000 (PDPT[8..511], 504 GB reach); per-proc CR3 inherits it (PDPT[1..511] mirror) → kernel reaches ANY phys page under ANY CR3, decoupled from the user mmap arena. Placement lesson: canonical high-half (`0xFFFF8…`, bit-63/negative-i64) AND a 16 TB new-PML4-entry **both faulted despite correct page-table chains**; **8 GB GB-range in the apic.cyr-proven 0x2000 PDPT works** ([[project_agnos_directmap_placement]]). `pmm_memmap_ram_top` (conventional top, NOT the MMIO-inflated memmap top) bounds it; ram-probe-smoke `directmap: low+hi OK` + exec-smoke PIE+non-PIE green. **bite 3a (1.49.8) DONE — user pages → 256 MB**: `pmm_alloc_2mb` ceiling 64→128 regions (2-128 MB → 2-256 MB) via `pmm_2mb_top_region`, with `pmm_free_2mb`/`pmm_count_2mb_free` tracking 256 MB. 1.49.6 grew the bitmap+identity but pmm_alloc_2mb (ELF code/stack/mmap source) stayed at 128 MB. Identity-safe (NO direct-map — 256 MB is identity-mapped via per-proc PD[8..127] + elf.cyr on-demand vmm_map covering 0-1 GB); pmm-fullram-smoke `PMM 2mb: >128MB OK` (102 regions, max ~218 MB) + exec-smoke PIE+non-PIE green. **bite 3b→3c (1.49.10–1.49.11) DONE — >256 MB RAM ONLINE (the 1.49.9 "cyrius high-VA blocker" was a MISDIAGNOSIS)**: 1.49.9 saw `store64`/`load64` to the 8 GB direct-map VA return 0 with no fault and blamed cyrius. **1.49.10 root-caused it**: the probe ran under gnoboot's **TRANSIENT boot CR3** (`0x3fc01000`, whose `PDPT[8]` is a 1 GB *identity* huge page to unbacked phys 8 GB → reads 0, no fault), NOT the kernel's `0x1000` PML4 where the direct-map lives; the bite-2 `directmap: low+hi OK` was a two-zeros false match. Under **CR3 `0x1000`** (kernel threads + every per-proc CR3) the SAME store/load round-trips → **cyrius EXONERATED** (issue doc updated, NOT patched — [[feedback_cyrius_hands_off]]); the boot probe was rewritten to validate a true >256 MB VA under CR3 0x1000. **1.49.11 brought >256 MB RAM online** via a **dynamic RAM-backed PMM bitmap** (`pmm_migrate_bitmap` — after the RAM probe, alloc one 2 MB bitmap region covering the full machine RAM, carry the 0–256 MB reservations + reserve non-conventional memmap above 256 MB, lift `pmm_2mb_top_region` to full RAM; one 2 MB bitmap = 64 GB, >64 GB capped+logged) + lifting `pmm_alloc_2mb`'s 256 MB cap. 4 KB `pmm_alloc` STAYS ≤256 MB (identity); only direct-map-reachable 2 MB user pages go higher (zeroed via `DIRECTMAP_BASE + phys` under the per-proc CR3). QEMU: `PMM_HIRAM_SELFTEST` proves a >256 MB alloc round-trips through the direct-map (`2mb_top_region=511` at -m 1024M), ram-probe 4/4, mmap-smoke 2/2. **BURN 1 RAN 2026-06-28 (64 GB box): RAM init ✅ (`16177224 free pages`) but LOCKED UP launching agnsh** — a latent >256 MB raw-phys bug the cap-lift exposed (`sys_mmap` + `elf_load` zeroed/copied a >256 MB region via its raw phys as an identity VA → #PF-halt; `elf_load_from_file` was already correct). **FIXED 1.49.12** (route through `pmm_kva_for_access`), reproduced + verified in QEMU at `-m 8G`/`32G`; **RE-BURN PENDING**. [`#tracker-149x-cycle`](iron-nuc-zen-log.md#tracker-149x-cycle); if clear → the >64 GB multi-region bitmap + the boot-CR3→own-PML4 cleanup go to 1.50.x. [[project_agnos_directmap_placement]], **(2) loopback — ▶ bites 1+2 DONE (1.49.0)**: `net_tx` routes IP frames to 127.x/`net_ip` into an 8-slot loopback queue (not `nic_send`); `net_lo_drain` (in `net_poll`, works with NO NIC up) drains them through `net_demux_frame` (extracted from `net_rx_drain`); `udp_send` routed through `net_tx`. loopback-smoke PASS (UDP to 127.0.0.1 round-trips with no wire peer) + dns-smoke 3/3 (no NIC-path regression) + check.sh 11/11. **bite 2 DONE**: TCP (`tcp_send_pkt`) + ICMP echo paths route through `net_tx` too; loopback-smoke 3/3 (UDP + ICMP self-ping + full TCP handshake over `lo`, SYN/SYN-ACK/ACK via the queue — self-loop to `net_ip` so replies loop with no source-selection); no wire regression (dns 3/3, icmp pass, kernel accepted a real wire conn `tcp_accept: conn_id=1`). Pre-existing/unrelated: `tcp-listen-smoke.sh` greps a stale string + needs `nc` installed, **(3) socket-as-VFS-fd — ▶ COMPLETE, bites 1+2 (1.49.4–1.49.5)**: `sock_accept`#57 now returns a `VFS_SOCK` (tag 10) fd wrapping the TCP conn_id; `read#5`/`write`/`close#6` dispatch to `tcp_recv`/`tcp_send`/`tcp_close`, so an accepted conn is a uniform VFS handle. loopback-smoke 4/4 (read back THROUGH the fd byte-exact) + exec-smoke unregressed. **bite 2 (1.49.5): epoll DONE** — `tcp_readable` (rx-ring-has-data / conn-dead) backs a VFS_SOCK arm in `epoll_wait`; loopback-smoke 5/5 (readiness flips 0→1 across the client send — EPOLLIN fires only when readable). **Gap 3 COMPLETE** — an accepted TCP conn is a uniform epoll-able fd (the event-loop keystone). **The OWED >128 MB RAM extension is DONE** — >256 MB RAM is online (1.49.11), QEMU-validated, iron-burn pending (see gap (1)); the remaining 1.49.x capability-gap items (loopback, socket-as-VFS-fd) are also complete. CHANGELOG `[1.49.0]`–`[1.49.11]` + roadmap own the detail. Backlogged to the roadmap Deferred table: the 1.47.x perf-tail (syscall-entry/context-switch) + the 1.48.x out-of-scope tail (exFAT bitmap-scan, fatfs multi-cluster-write [structurally blocked], metadata single-sector I/O). Iron-burn fixes also land as patches. *(state.md line is long — the closed 1.47.x/1.48.x per-patch detail above is a candidate for a compression pass.)* **▶ RE-BURN 1.49.12 PASSED 2026-06-28** (boots to agnsh on real 64 GB Zen, >256 MB RAM usable — 1.49.x arc iron-closed); the current head + next-burn status is the 1.50.x lead at the top of this line → [`#tracker-150x-cycle`](iron-nuc-zen-log.md#tracker-150x-cycle).
>
> **1.46.x SMP / multi-threading arc — COMPLETE + iron-validated.** STEP-2 (`smp_sched_aps=1`, real ring-3 procs on woken APs) PASSED on archaemenid 2026-06-26; the whole STEP-1 → STEP-1-at-IF=1 → STEP-2 ladder is iron-green (IF=1 preemptive agnsh, working coreutils, boot-to-shell all confirmed on real Zen). The 2-week IF=1 `#GP` was the AMD SYSRET SS-RPL bug, fixed one byte (`syscall_hw.cyr` STAR[63:48] `0x10`→`0x13`) at **1.46.5**. **Live kernel gates**: `exec_preempt=1` (P0 — IF=0 revert PROHIBITED by user), `smp_sched_aps=1` (=0 reverts to STEP-1), `smp_wake_enabled=1` (APs woken). Detail → agnos CHANGELOG `[1.46.x]` + iron-log [`#tracker-146x-cycle`](iron-nuc-zen-log.md#tracker-146x-cycle) + agnos `smp-arc-plan.md`; memory [[project_agnsh_if1_preempt_iron_blocker]] / [[project_multithreading_future_arc]].
>
> **Post-SMP landings (on top of the closed arc):** `input_lock` (1.46.9, kb_buf SMP lock) · exec argv cap 8→16 (1.46.10) · two-stage shell pipes `cmd1|cmd2` (kernel read#5 stdin-from-pipe at 1.46.11 + **agnoshi 1.8.1** `sh_run_pipeline`; store-and-forward, single-foreground; QEMU-validated `iam|anuenue`. **agnoshi 1.8.1** made `_sh_find_pipe` tolerate a missing space around `|` — a dropped iron keystroke ate a space in `anuenue | brnmr` → bareword launch → `run: unsafe path`; now splits on a bare `|` (spaces optional), QEMU-validated no-space/partial-space). **cyrius-doom 0.30.3** — sustained-play ring-3 `#PF` root-caused + fixed (sprite.cyr `clip_top` OOB / unclamped `sprite_w`, was masked by a cyrius-6.1.37 continue-miscompile already fixed in 6.2.x; the earlier "DOOM locks up after ~1-2 min" finding is now CLOSED). **anuenue 1.1.5** — truecolor-on-agnos color fix; pins cyrius 6.2.44.
>
> **Open carry-forward:** **→ PROMOTED into the 1.47.x arc as the headline bites:** kernel should TERMINATE a faulting ring-3 proc + return to the agnsh prompt instead of halting (server/desktop maturity, non-MVP) + per-process fd tables (streaming / SMP-safe / N-stage pipes; the pipe-buffer refcount free-on-close rides with it). **Still parked:** the `owl`→`sit`/`SYS_CHDIR` agnos port (owl needs `sit` on agnos — `sit` calls `SYS_CHDIR`, and agnos has **no chdir by design** — plus the TLS peer; the agnos delegation runs owl 1.3.8 cat-only) · thorough per-project net-tool validation (`yo`/`dig`/`whirl`/`taar`) deferred — they already run at decent capacity on iron.
>
> **Closed arcs (history → each repo's CHANGELOG + iron-log, NOT here):** 1.31.x storage · 1.32.x networking · 1.33.x ext2/4 write · 1.34.x FAT/exFAT write · 1.35.x comms · 1.36.x refactor · 1.37.x ext4 extent-alloc · 1.38.x jbd2 · 1.39.x VFS write-lift · 1.40.x exec-from-disk · 1.41.x shell→agnoshi · 1.42.x perf/sysinfo/klug · 1.43.x graphics/DOOM · 1.44.x preemptive scheduling · 1.45.x TLS→net-tools · 1.46.x SMP — all iron-validated. MVP gate (boot-to-shell on iron) green since 1.30.9. The 1.45.x net-syscall surface (#45–#57, winsize#60, net_config#61) + timer-ISR RX drain (`net_rx_drain`) are landed kernel capability; `cyrius-yeomans-descent` 1.1.3 runs on agnos over server sockets (sock_listen#56/accept#57; harness `docker/descent-sweep/boot-serve.py`); the Docker service-sweep harness (net + sched, TCG-default) found/fixed the TCP CLOSE_WAIT listener slot-leak (1.45.11). attn11 PARKED (MTP arc complete at 1.10.2; parked training-science backlog in its roadmap).
>
> **Iron-log roads** (split by maturity era; active keeps the bare name): **base** (active — [`iron-nuc-zen-log.md`](iron-nuc-zen-log.md)) → **server** (self-hosting / public-beta gate) → **platforms** (1.5x+ hardware). Chain: [`-mvp`](iron-nuc-zen-log-mvp.md) → [`-mvp2`](iron-nuc-zen-log-mvp2.md) → active.
>
> **Crate registries** (per-repo versions/pins drift fast — consult these + each repo's live `VERSION`/`cyrius.cyml`, never an embedded table): [`planning/shared-crates.md`](planning/shared-crates.md) (full, incl. pre-1.0) · [`docs/applications/libs/README.md`](../applications/libs/README.md) (v1.0+ libs) · [`docs/applications/binaries.md`](../applications/binaries.md) (v1.0+ binaries) · CLAUDE.md's version-free role-map for orientation. (The old 2026-06-01 pin-lag spectrum + per-repo version table were deleted — they were stale by orders of magnitude.)
>
> **agnosys → agnodrm decomposition (2026-06-19):** agnosys was over-scoped; the device/DRM model survives as **agnodrm** (udev + DRM/KMS), trust→sigil, security/mac/audit→kavach, pam→aegis, logging→sakshi, syscall layer→cyrius (the Linux-eccentric group bootloader/update/netns/fuse/journald parked post-v1). Detail → CLAUDE.md role-map + agnodrm `docs/development/2026-06-18-agnosys-to-agnodrm-decomposition-plan.md`.

**Out of cycle scope (parked):**
- AMD Zen scanout residue (Quiet Boot legibility) — separate cycle per [`project_archaemenid_hardware_target`](../../../../.claude/projects/-home-macro-Repos-agnosticos/memory/project_archaemenid_hardware_target.md) (§ scanout residue); HUBP `clear_tiling` port or shadow-buffer eval.
- Kernel-side FB **double-buffer** (tear-free flips inside blit#39) — backlogged 2026-06-11 when zero-copy FB-mmap was closed as superseded. Trigger: observed tearing on iron, or future shell-launched games beyond cyrius-doom needing it. Always kernel-mediated (`fb_phys` stays unexposed — the hardened posture is the decision).
- i225-V NIC driver — queued for Intel iron post-migration (the r8169 / RTL8125 path is DONE + iron-verified, 1.32.x; i225-V is a separate hardware line, not an AMD blocker).

This doc holds **volatile state** — what's currently true across the AGNOS dev surface. CLAUDE.md is preferences/process/procedures; this is the live picture. Per [*Development Speed and How It Effects Documentation*](../articles/development-speed-and-documentation.md): rewrite in place when state changes; don't preserve historical snapshots — git history is authoritative.

**Drift caveat**: always verify against actual `VERSION` + `cyrius.cyml` files before acting on any single item in this doc.

---

## ML / AI reference arc (live)

The sovereign ML/AI work runs as a **separate thread** from the kernel arc above (own repos, no iron burns — validated in QEMU / on host). Live map: [[project_ml_ai_arc_overview]] memory + [`planning/type3-weight-import.md`](planning/type3-weight-import.md) (COMPLETE) + [`planning/software-port-path.md`](planning/software-port-path.md) + **[`planning/ifran-port.md`](planning/ifran-port.md) — SHIPPED: ifran 2.0.0 (2026-07-05), the training control plane, ported M0→M6 in two days and proven by attn11+tarka+anukūlana running entirely as ifran jobs; 2.1.0 same day = Lane 1 executor hardening (timeout/reaper/quoted-args/`show`), with the cross-repo lanes closed by anukūlana 1.1.1 (`--sk`) + tarka 1.1.2 (`--pref`); 2.2.0 = the rust-old pre-removal-audit additives (sweep leaderboard+budgets, `dataset validate`, 4-state prefs+conf), with the rust-old audit DONE and removal gated only on preserving the 3 SY-contract docs.** Per-repo detail lives in each repo's CHANGELOG — this is a pointer, not a log. **Runs-on-agnos gate (whole family): the kernel FP/SIMD arc** (agnos `planning/kernel-fp-arc-153x.md`, slotted 1.53.x) — agnos ring-3 enables no FP today, so all on-agnos inference (incl. tentib's integer kernel, whose dequant scales/norms are f64) waits on it; host/QEMU validation is unaffected.

**Substrate-freeze day (2026-07-04) — Type-3 M1 COMPLETE (the real foreign checkpoint runs AND matches HF exactly) + the substrate froze:** The exact-fidelity gate closed via `gpt2-oracle` + a **committed HF-logits fixture** (torch ran once in a disposable venv — never a dependency): argmax identical at all 48 positions, last-row maxrel 1.05e-6 (fp32-rounding scale), gate frozen at 1e-5 → **anukūlana 0.3.0 cut**. Same day, **rosnet 1.0.0 — the ML-substrate freeze** (six-consumer surface frozen, CPU + GPU profiles; benchmarks + audit captured; graduated to the [libs registry](../applications/libs/README.md)). **LoRA (M3) CLOSED + QLoRA/NF4 (M4 core) COMPLETE same day** (anukūlana `[Unreleased]`): FD-gated LoRA primitive set (two rosnet `linear_bwd` passes + hand-derived Adam) + `gpt2-lora` head adapter (xent 10.79→0.0000, argmax 1/8→8/8, base bit-frozen; head-scope accepted by user — deeper adapters would patch attn11, allowed **except SIMD** which cyrius delivers next arc) → sovereign **NF4 codec** (blockwise-64 + double-quant, 8-test exactness gate) + `gpt2-qlora`: **the whole 124M base at 4 bits, adapter recovers the task 8/8 over it, codes bit-frozen** (honest 124M-scale note: raw 4-bit forward drifts — the gated thesis is trainability at 4-bit memory). Findings: plain SGD diverges on real-GPT-2 features (massive-activation outliers → Adam); NF4's largest quantile gap is the negative side's. **The persistence tail shipped in anukūlana 0.5.0 (same day)** — signed NF4 checkpoint (63.8 MB) + adapter (3.3 MB) via tula, bit-identical round-trips, Ed25519 tamper/wrong-key rejection; the base NF4 codec was **reconciled to delegate to tula's shipped codec** (the 0.4.0 hand-roll duplicated tula 1.0.0's frozen surface — the know-the-ecosystem catch; only superblock-256 double-quant stays anukūlana-local). **anukūlana shipped 1.0.0 STABLE same day** (API frozen, fuzz gate + 2 audit fixes in the foreign parser, benchmarks + SECURITY.md; post-1.0 headline = **GGUF import**). **The Type-3 arc is COMPLETE and frozen end-to-end** (tula 1.0.0 · rupantara 0.4.0 · anukūlana 1.0.0 on rosnet 1.0.0). Chain state:

| Repo | Ver | Role | State |
|------|-----|------|-------|
| [`tula`](https://github.com/MacCracken/tula) | 1.0.0 | weight-file format (M0) | format v1 FROZEN (105 assert + 2M fuzz + audit) |
| [`rupantara`](https://github.com/MacCracken/rupantara) | 0.4.0 | transformer-forward lib (attn11→libs #4) | whole-forward parity-proven bit-identical vs attn11 + KV-cache decode |
| [`anukūlana`](https://github.com/MacCracken/anukulana) | **1.1.1** | Type-3 reference — **STABLE** | **charter FULLY built + FROZEN** (import → run → match-HF → LoRA/QLoRA → signed persistence; api.md + STABILITY + SECURITY + audit [2 st_open DoS fixes] + fuzz gate + benchmarks). **Post-1.0 headline GGUF import SHIPPED as 1.1.0 (2026-07-05)**: sovereign GGUF v2/v3 parser + GPT-2 `blk.N.*` mapping; `gpt2-gguf` PASS on the real 124M file; **`gpt2-cross` cross-format gate: both doors bit-identical (123.6M params / 402k logits, 0 diffs)**; +35k GGUF fuzz rounds; suite 80→121. **1.1.1 (2026-07-05): `--sk` operator-key signing — ifran Lane 2 CLOSED** (`gpt2-tula --sk` + `anuk_sk_load`; ifran `store add` now records `verified` end-to-end, proven on the real checkpoint). Next lanes: quantized GGML payloads, llama-arch (TinyLlama) mapping |
| [`attn11`](https://github.com/MacCracken/attn11) | **1.13.0** | GPT transformer + vision ref | CPU leaf-op re-fold onto rupantara `ru_*`; **1.13.0 (2026-07-05) added the VISION lane** (`--vision` — small CNN on rosnet conv2d, the sight proof: loss 1.377→0.0012, held-out 1000/1000, assembled backward FD-gated; suite 1049→1060) |
| [`ganita`](https://github.com/MacCracken/ganita) | 1.0.2 | linalg/math lib | `f64_tanh` NaN-overflow fix (surfaced by the real forward; folded into cyrius stdlib **6.3.31**) |
| [`rosnet`](https://github.com/MacCracken/rosnet) | 1.1.0 | f64 tensor/BLAS substrate | FROZEN 1.0.0 (2026-07-04); **1.1.0 (2026-07-05) added conv2d/conv1d** (FD-gated, per-axis stride/pad) — **the modality axis is substrate-complete** |

Other siblings: **tarka 1.1.2** (RL — the 1.1.x preference set DPO+KL / IPO+KTO shipped, closing out [`planning/tarka-preference-rlhf-extensions.md`](planning/tarka-preference-rlhf-extensions.md); **1.1.2 (2026-07-05) closed ifran Lane 3**: `tarka --pref <prefs.jsonl>` ingests ifran's `pref export` and trains DPO/IPO/KTO from the curated set — e2e proven, suite 73), prajna 1.0.0 (meta), tentib 0.4.0 (ternary — 0.4.1 int-SIMD kernel HARD-gated on cyrius integer SIMD; in progress in the 6.4.x line but not landed as of cycc 6.4.7, so parked — do not chase), amuzesh 0.1.0 (classical). **Substrate leafs FROZEN 2026-07-05: akshara 1.0.0 + tyche 1.0.0** — clean multi-consumer-soak freezes (no behavior change; api.md each), completing the rosnet/tyche/akshara extraction trio at v1.0+; both graduated to the [libs registry](../applications/libs/README.md). **The SIGHT PROOF PASSED same day — shipped as attn11 1.13.0** (vision lane, `--vision` — user-homed): small CNN on rosnet conv2d over a synthetic sovereign shape set, loss 1.377→0.0012, held-out **1000/1000**, assembled backward FD-gated (attn11 suite 1049→1060, rosnet pin → 1.1.0); the modality axis has its first consumer proof (hearing = STFT+mel glue next, on demand). anukūlana pins cyrius **6.3.31** (to pick up the folded ganita fix). **Current head (2026-07-05):** the three long-standing ML gaps are now closed or advanced — **Type-3 COMPLETE + frozen** (charter built end-to-end, GGUF shipped as anukūlana 1.1.0/1.1.1), **the ifran control-plane port SHIPPED** (2.0.0→2.2.0; all post-2.0 cross-repo lanes closed; `rust-old/` audited + removed), and gap #3 = **tanur** (the desktop model-studio — a puka desktop *app*, agnosticos [`planning/tanur.md`](planning/tanur.md); ifran stays CLI-only, tanur consumes it), **desktop-stage, backlogged**. Trigger-gated remainder: tentib 0.4.1 int-SIMD (parked on cyrius), the hearing proof (STFT+mel over hisab FFT, on demand), the DSpark decode lane (below).

**Mapped-but-unbuilt lane (2026-07-03):** DeepSeek **DSpark** (speculative decoding) was investigated and homed as an **attn11 decode lane** (not a new sibling) — it reuses attn11's MTP heads (self-speculative draft) + planned KD objective (draft training); correctness gate = losslessness; load-aware verification splits to hoosh/murti. Forward-design map: [`planning/speculative-decoding.md`](planning/speculative-decoding.md). Design reserved, build **not triggered** — attn11 is active again (1.13.0 vision lane), so the barrier is now a build-decision, not a reopening; the trigger is a decision to build speculative decoding (MTP-draft + KD), unchanged.

---

## Active sweeps

Open housekeeping (none blocking; bundle with each repo's next natural touch):

- **yantra** orphan `lib/http_server.cyr` delete — still pending (cleanup-only, no callers; yantra deep-lag).
- **vyakarana** grammar refresh — re-verify the 469 sandhi-fn index reflects content.

### CVE-2026-31431 (Copy Fail) — roadmap item S1

`algif_aead` AF_ALG-in-place-AEAD + `splice()` Linux LPE (disclosed 2026-04-29). **AGNOS-native kernel structurally immune** — the sovereign syscall table (`kernel/core/syscall.cyr`) has no `socket`, no `splice`, no AF_ALG family, so the bug class is unreachable; re-verify only if the syscall surface grows. **Open:**
1. Pin `# CONFIG_CRYPTO_USER_API{,_HASH,_SKCIPHER,_AEAD,_RNG} is not set` in host defconfigs (`kernel/{6.6-lts,6.x-stable,7.0-devel}/configs/*defconfig` + `kernel/configs/edge-*.config`).
2. Finish the deferred per-repo `AF_ALG`/`algif_aead` audits (repos not all local).
3. Once defconfigs pinned, document the absence-by-design pattern alongside other AGNOS-vs-Linux structural-immunity examples in `design-patterns.md`.

### Pending Cyrius ports

| Repo | Current state | Action |
|------|---------------|--------|
| **bhava** | 2.0.0 Rust | Emotion/sentiment — port can start (stdlib + math additions gating) |
| **aethersafha** | 0.5.0 (in progress) | Wayland compositor — built-in-apps framework landed (Bite C1+C2, fork+execve spawn); backends **bhumi 1.0.0** + **mehman 1.0.0** |

> **Completed ports (out of this table 2026-07-03):** goonj → **2.0.0** Cyrius (37 modules, 3585 parity asserts) · naad → **2.1.0** Cyrius (+ post-port audit). Both part of the audio-synthesis wave feeding the 1.52.x audio arc; svara → **3.0.0** and nidhi → **2.0.0** Cyrius ports also landed same day.

> Note: **aethersafta** (Rust 0.50.0, media-compositing scene graph) is DISTINCT from aethersafha (the Wayland compositor) — a near-stable lib and a **Rust→Cyrius port target** (a Cyrius-native version is where it goes). ⚠ The earlier "**not** a Cyrius port target" note was **false** — it confused *agnos running various-language binaries* (swallow/compat, for foreign third-party apps) with AGNOS's own components, which always go Cyrius-native for implementation sovereignty. "agnos could just run the Rust binary" is never a reason to leave one of our own libs in Rust. See [[project_os_agnostic_layer_is_the_swallow_mechanism]].

### Per-repo housekeeping (P1/P2, none blocking)

- `cyrius.toml` → `cyrius.cyml` format migration: **hoosh, shravan** remaining (last pre-CYML holdouts).
- `[build].modules` → `[lib] modules`: **sigil, agnodrm, shakti** pending.
- `docs/adr/` scaffold (~12 repos still missing); copy `README.md` + `template.md` from sit, don't back-fill historical decisions.
- `docs/adrs/` → `docs/adr/` rename: **argonaut** last offender.
- **kiran** `cyrius.cyml` pin-field population (shipped 1.0.0 but lacks the `cyrius = "X.Y.Z"` field).
- `docs/development/state.md` migration for repos still carrying volatile state in CLAUDE.md (kybernet, daimon, agnos, abaco, hoosh, kavach, mabda, sigil, … presumed similar for the unverified tail).

### CLAUDE.md table refresh

Root [`CLAUDE.md`](../../CLAUDE.md) "Standalone Repos" table also drifts — when refreshing, prefer pointing to this file / shared-crates.md / the registries rather than re-duplicating versions (it is intentionally version-free).

---

## Doc / article update queue

Open writing/refresh tasks only:

| File | Action |
|------|--------|
| [`roadmap.md`](roadmap.md) | Re-touch on each v6.x ship (v5.x phase defs are historical; v6.x = "what the language gains"). |
| [`summer-2026-arc.md`](summer-2026-arc.md) | Re-anchor cycle-theme references (v6.x framing + closed-beta MVP gate hit on iron). |
| [`articles/cyrius-vs-rust-benchmarks.md`](../articles/cyrius-vs-rust-benchmarks.md) | Add v6.x rows; sweep closed "pure compute gap" language. |
| [`articles/doom-in-cyrius.md`](../articles/doom-in-cyrius.md) | Rebuild numbers when cyrius-doom ships an unblock release. |
| [`articles/sovereign-compiler-vs-brute-force.md`](../articles/sovereign-compiler-vs-brute-force.md) | Pull current `cycc` self-host size from `cyrius/build/cycc` before publishing. |
| [`articles/port-ledger-volume-3.md`](../articles/port-ledger-volume-3.md) | 🌱 **OPEN / accreting** (seeded 2026-06-01) — fills per-port as 6.x re-benchmarks land. Volumes 1 + 2 are **FROZEN**. |
| [`planning/shared-crates.md`](planning/shared-crates.md) + [`docs/applications/libs/README.md`](../applications/libs/README.md) | Periodic version refresh of the registries. |
| [*Why AGNOS-native agents can't be drained by a tweet*] (working title) | Agent-injection-defense article (Phase 15A) — second instance of the absence-by-design structural-immunity pattern (CVE-2026-31431 was the first). Spec: [`planning/agent-injection-defense.md`](planning/agent-injection-defense.md). Draft after Phase 1 ships (post-closed-beta). |

---

## Refresh procedure

When a cyrius patch or agnos cut lands:

1. Pull current `VERSION` + `cyrius.cyml` for affected repos.
2. Tick off swept items.
3. Re-anchor "Last refresh" date, kernel head, and pins in the header.

When a cycle closes:

1. Collapse the closed arc to a one-line pointer in the "Closed arcs" ledger (history → repo CHANGELOG + iron-log).
2. Repoint any stale version references.
3. Don't archive — rewrite in place. Git history is the snapshot.

---

## Related

- [`CLAUDE.md`](../../CLAUDE.md) — preferences/process/procedures (this doc holds the volatile state CLAUDE.md should NOT carry).
- [`planning/shared-crates.md`](planning/shared-crates.md) + [`docs/applications/`](../applications/) — authoritative crate registries (versions + roles).
- [`roadmap.md`](roadmap.md) — AGNOS / Cyrius milestone definitions and timeline.
- [Cyrius CHANGELOG](https://github.com/MacCracken/cyrius/blob/main/CHANGELOG.md) — authoritative source for cyrius cycle status.
- [Articles: *Development Speed and How It Effects Documentation*](../articles/development-speed-and-documentation.md) — the rationale for state.md as a pattern + the broader drift argument.
- Per-repo `docs/development/state.md` (where it exists) — source of truth for that repo's local state; verify before acting.

---

*Refresh in place per [*Development Speed and How It Effects Documentation*](../articles/development-speed-and-documentation.md). Per-day refresh narratives previously accreted here have been pruned — git history is authoritative for prior-state recovery; CHANGELOGs + iron-nuc-zen-log are the canonical event ledgers.*
