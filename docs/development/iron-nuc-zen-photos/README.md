# Iron Boot Photo Catalog

Photographic record of iron-boot test attempts on **archaemenid** (NUC AMD,
Beelink SER, Zen-class x86_64). Each photo captures the framebuffer state
at attempt closeout — visual canary cells, kernel banner, xhci status,
HID-enumeration output, shell prompt, or framebuffer noise.

**Companion logs**:
- [`iron-nuc-zen-log-mvp.md`](../iron-nuc-zen-log-mvp.md) — Attempts 1–68 (closed-beta MVP era, capped 2026-05-19)
- [`iron-nuc-zen-log.md`](../iron-nuc-zen-log.md) — Attempts 69+ (post-MVP, 1.30.10+)

**Photo naming convention**: `attempt-NN-<short-handle>[-reshot].jpg`. "reshot" suffix indicates the same attempt was photographed twice for clarity / focus / re-exposure — both retained.

**Source of truth for attempt narrative is the log file**, not the catalog entry. The catalog is a navigation aid keyed on filename, not a substitute for reading the attempt body.

---

## Visual canary era (Attempts 15–29)

Early boot bring-up before xhci work began. Goal: paint colored cells via `cp_fb` to confirm the kernel reached each subsystem-init stage, then graduate to text rendering via fb_console.cyr.

| Photo | Date | What it shows |
|-------|------|---------------|
| `attempt-15-boot-colors.jpg` | 2026-05-13 | First successful cp_fb cell paint on iron — boot_shim canary + early arch stages |
| `attempt-16-boot-colors.jpg` | 2026-05-14 | More cells reached; subsystem-init progress visible |
| `attempt-18-boot-colors-reset-only.jpg` | 2026-05-14 | Reset-only burn (no behavioral change) for baseline |
| `attempt-25-boot-colors-racy.jpg` | 2026-05-15 | Race-condition narrative — cell paint shows non-deterministic stop point |
| `attempt-28-mvp-spine-alive.jpg` | 2026-05-15 | MVP spine reached scheduler + userland; cell grid fully painted through scheduler color |
| `attempt-29-shell-logging-cleanup.jpg` | 2026-05-15 | Shell prompt visible on iron for the first time (text console live); pre-typeable |
| `attempt-29-shell-visible-no-keys.jpg` | 2026-05-15 | Same attempt, shell visible but no keyboard input — the seed of the xhci HID arc |

---

## xhci Phase 1: controller discovery (Attempts 30)

Locate the xHCI controller on the PCI bus; the first half is "did PCI enum find a USB 3.x controller class," the second is "is BAR 0 valid."

| Photo | Date | What it shows |
|-------|------|---------------|
| `attempt-30-xhci-phase1-no-controller-found.jpg` | 2026-05-15 | First Phase-1 burn — controller-not-found case |
| `attempt-30-xhci-phase1-controller-found.jpg` | 2026-05-15 | Phase-1 success — xHCI controller discovered on PCI |

---

## xhci Phase 2: initialization scaffolding (Attempts 31, 33)

USBSTS / USBCMD register I/O, command ring setup, event ring scaffolding.

| Photo | Date | What it shows |
|-------|------|---------------|
| `attempt-31-xhci-phase-2.jpg` | 2026-05-15 | Phase-2 controller-init scaffolding working |
| `attempt-33-phase-2-5-corrupted.jpg` | 2026-05-16 | Phase-2.5 — corrupted output indicates init-order bug |

---

## xhci Phase 3 silent-absorb arc (Attempts 38–55)

Port reset / PortSC silent-absorb — multi-letter repair ladder (Repair S → BB → CC → DD → EE) chasing what turned out to be `xhci_portsc_write`'s inner re-mask stripping the RW1S PR bit. Closed at Attempt 55 / Repair EE / agnos@41ee6dc with a one-line fix.

| Photo | Date | What it shows |
|-------|------|---------------|
| `attempt-38-xhci-phase-3-pp-asserted-reset-failed.jpg` | 2026-05-16 | Phase-3 entry — PP asserted, port reset failed |
| `attempt-39-xhci-r10-pls-polling-pr-still-absorbed.jpg` | 2026-05-16 | Repair R.10 — PLS polling; PR bit still absorbed |
| `attempt-40-xhci-repair-s-pp-collapse.jpg` | 2026-05-16 | Repair S — PP collapse signature |
| `attempt-43-xhci-repair-v-f5-confirmed-bar-wb-cached.jpg` | 2026-05-16 | Repair V (F5) — BAR memtype confirmed WB-cached (audit win) |
| `attempt-44-xhci-repair-x-uc-remap-still-absorbed.jpg` | 2026-05-17 | Repair X — UC-remap; symptom survives |
| `attempt-49-xhci-plumbing-bundle-msi-x-still-absorbed.jpg` | 2026-05-17 | Plumbing bundle + MSI-X — symptom survives |
| `attempt-50-xhci-repair-aa-scratchpad-installed-still-absorbed.jpg` | 2026-05-17 | Repair AA — scratchpad installed; symptom survives |
| `attempt-51-xhci-repair-bb-dnctrl-still-absorbed.jpg` | 2026-05-17 | Repair BB — DNCTRL; symptom survives |
| `attempt-52-xhci-repair-cc-dd-still-absorbed.jpg` | 2026-05-17 | Repair CC + DD bundled; symptom survives |
| `attempt-54-xhci-phase-4-5-iron-clean-still-absorbed.jpg` | 2026-05-17 | Phase 4–5 iron-clean; symptom survives |
| `attempt-55-xhci-reset-unblock-enable-slot-ccode-0.jpg` | 2026-05-17 | **Repair EE — Phase-3 unblocked** at agnos@41ee6dc (Enable Slot CCode=0 success); silent absorb closed |

---

## xhci Phase 4 cmd-path silent-absorb arc (Attempts 56–63)

10-letter repair ladder (FF → GG → HH → JJ → KK → LL → MM → NN → OO → QQ+QQ2) chasing what turned out to be a **Cyrius compiler bug**: gvar-init-order zero-reads at file scope. `XHCI_CMD_TIMEOUT_SPINS = 10000000` and `XHCI_EVT_RING_SEGMENT_SIZE = 256` read as 0 → spin loop never executed, ERST segment 0-sized. Root cause fixed in cyrius **v5.11.64** (issue `2026-05-18-gvar-init-order-zero-reads.md`); the entire 10-letter ladder was falsified silicon hypotheses chasing a compile-time bug.

| Photo | Date | What it shows |
|-------|------|---------------|
| `attempt-56-xhci-instrumentation-burn-events-seen-zero.jpg` | 2026-05-17 | Instrumentation burn — `events_seen=0` confirmed |
| `attempt-57-xhci-repair-ff-ie-set-events-seen-still-zero.jpg` | 2026-05-17 | Repair FF — IMAN.IE=1; events still 0 |
| `attempt-58-xhci-repair-gg-amdvi-disabled-events-seen-still-zero.jpg` | 2026-05-17 | Repair GG — AMD-Vi disabled; events still 0 |
| `attempt-58-xhci-repair-gg-amdvi-disabled-events-seen-still-zero-reshot.jpg` | 2026-05-17 | Same attempt — reshot for clarity |
| `attempt-59-xhci-edit-b-delivered-cycle-bit-clean-events-still-zero.jpg` | 2026-05-17 | Repair JJ/Edit-B — cycle bit clean; events still 0 |
| `attempt-60-stack-bundled-still-zero.jpg` | 2026-05-18 | Stack-bundled (KK/LL/MM); events still 0 |
| `attempt-60-stack-bundled-still-zero-reshot.jpg` | 2026-05-18 | Same attempt — reshot |
| `attempt-61-xhci-repair-nn-erdp-before-erstba-crcr-after-imod-events-still-zero.jpg` | 2026-05-18 | Repair NN — ERDP→ERSTBA reorder, CRCR after IMOD; events still 0 |
| `attempt-62-shell-visible-events-still-zero.jpg` | 2026-05-18 | Shell visible on iron (visual MVP); xhci events still 0 |
| `attempt-63-shell-visible-on-iron-events-still-zero.jpg` | 2026-05-18 | **Visual MVP gate hit** (agnos 1.30.7) — FB renders agnoshi banner; xhci events still 0 |
| `attempt-63-shell-visible-on-iron-events-still-zero-reshot.jpg` | 2026-05-18 | Same attempt — reshot |

---

## xhci Phase 4 root-cause + Phase 5: HID → MVP gate (Attempts 65–68)

Post-cyrius-v5.11.64 fix burns. Each Phase-4/5 milestone landed in a single audit-then-burn pass with zero letter ladder, per `feedback_known_knowledge_first` + `feedback_redesign_dont_reinvent`.

| Photo | Date | What it shows |
|-------|------|---------------|
| `attempt-65-xhci-silent-absorb-hurdled-hid-config-9-timeout.jpg` | 2026-05-18 ~19:07 PDT | Phase-3 cleared end-to-end on iron (Enable Slot + Address Device + GDD-8/18 all succeed); new blocker: `xhci_get_config_descriptor(slot, 0, 9)` timeout in `hid_kbd_configure` |
| `attempt-66-ep0-control-transfer-hardening-hid-config-9-still-timeout.jpg` | 2026-05-18 ~20:08 PDT | Repair RR (Linux-canonical EP0 control-transfer hardening) — falsified; GCD-9 still times out |
| `attempt-67-ep0-mps-reconciliation-hid-configured-typing-silent.jpg` | 2026-05-18 ~20:58 PDT | **Phase-4 cleared** — EP0 MPS reconciliation (xHCI 1.2 §4.6.7) lands; full HID enumeration succeeds; keyboard configured; FB shows `agnoshi shell v1.30.8`; new blocker: keypresses produce no characters (interrupt-IN silent) |
| `attempt-68-typeable-shell-on-iron.jpg` | 2026-05-18 ~21:30 PDT | **🎯 MVP GATE HIT** (agnos 1.30.9) — SET_CONFIGURATION + canonical FS interval + ISP bundle lands; `agnos> echo "Assembly Up!"` echoed back on iron Logitech (VID=1452 PID=591) |
| `attempt-68-bench-three-tier-on-iron.jpg` | 2026-05-18 ~21:30 PDT | 3-tier bench running on iron under typeable shell — fibonacci 133 c/op, syscall_write 31 c/op, open+read+close 256 c/op, serial putc ~11.6 c/op. **Pixel-pattern noise visible in lower FB region** → 1.30.10 framebuffer-refresh scope |

---

## Post-MVP era (Attempts 69+)

| Photo | Date | What it shows |
|-------|------|---------------|
| `attempt-70-help-me-build-an-entity-chart.jpg` | 2026-05-19 | **First natural-language user input on iron.** 3-tier bench output visible above (`syscall_write1: 31 c/op`, `vfs_open_read_close: 256 c/op`, `=== done ===`). Then `agnos> Help me build an entity chart` typed by a new user (Alicia), `unknown: Help` from the shell (bareword parser hit "Help" before the LLM lane exists), retry as `agnos> help` produces the 18-verb command list. Captures the gap between AI-native user intent and pre-userland kernel verbs — exactly what later phases (daimon/hadara/agnoshi LLM wiring) close. WB→WC residual pixel-pattern band visible in upper-middle FB region; lower half (shell list render) clean — 1.30.10 framebuffer-refresh signature post-Attempt-70 u64 block-copy. |
| `attempt-71-quickboot-vga-pass-reshot.jpg` | 2026-05-19 | Same attempt — reshot of the 1.30.11 VGA-spec PASS boot log with cleaner exposure; the original `13011_attempt_gnoboot_updated.jpg` filename reflected the gnoboot 0.3.0 → 0.4.x update milestone that landed in this same burn. |
| `attempt-71-quickboot-vga-pass.jpg` | 2026-05-19 | **1.30.11 hardening burn — QuickBoot ON + VGA-spec display path PASSES end-to-end.** Full boot log visible top-to-bottom: `AGNOS kernel v1.30.11`, ring-3 ready, US QWERTY installed, xHCI brings up 4 slots / 6 ports, USB keyboard enumerated (`slot=1 VID=1452 PID=591` — the Logitech from the MVP-era Attempt-68 gate-hit), `boot protocol on EP=129 polling 8-byte reports`, scheduler ticked 154 timer interrupts before sched arm, kybernet starting init with 354 free pages, `AGNOS shell v1.30.11 (type 'help')` prompt landed. Companion outcome — **Quiet Boot ON path still returns the Attempt-33 garbled-glyph signature** on the same kernel binary (BIOS toggle is the only variable) — falsifies the pf-aware-PixelFormat hypothesis that motivated 1.30.11's hardening (pf-guard would have produced a black screen if pf were ≥ 2; observed garble means BGRX branch took and paint fired against corrupted geometry). Drives the Attempt 72 diagnostic-extension cycle (gnoboot 0.3.0 + agnos working-tree FB-handoff observability — GOP `Mode->Mode`/`MaxMode` capture, serial `fb: mode=N/M …` diagnostic, CMOS extended-bank stamp at `0x90..0x9F` for archaemenid-no-serial read-back via `read-boot-log.sh`). |

| `attempt-77-quiet-boot-true-font-lines-off.jpg` | 2026-05-20 | **1.30.12 true-font swap landed — VGA path confirmed by user (new font legible + slight scroll speed improvement); Quiet Boot path still has render-math misalignment** ("lines off" — horizontal banding cuts through glyphs, suggesting pitch/stride or row-stride math is wrong somewhere in the quiet-mode display path). Photo captures the quiet-mode failure mode; VGA-pass outcome reported in chat (paraphrase from user): *"can confirm the new font and slight speed improvements for VGA; quiet mode is still got some off math some where."* |
| `attempt-80-nvme-iron-debut-crucial-p3.jpg` | 2026-05-20 | **NVMe iron debut on archaemenid — Crucial P3 2 TB enumerated end-to-end.** VID=0xC0A9 (Micron), model `CT2000P3SSD8`, firmware `P9CR30A`, NSZE 3907029168 × 512B = 1.86 TB usable, LBA0 read returned 8 zeros (blank surface), registered as block_dev, kernel walked through VFS → scheduler → kybernet → `AGNOS shell v1.31.0`. First-iron-try success for the entire Phase 1-5 NVMe stack — contrast with the xHCI arc's 5-week 19-attempt 9-letter ladder. |
| `attempt-81-ahci-iron-debut-wd-blue-sa510.jpg` | 2026-05-20 | **AHCI/SATA iron debut on archaemenid — WD Blue SA510 2.5" 2 TB SATA SSD enumerated, IDENTIFY decoded, LBA-5 round-trip PASSED.** HBA at `0xFCDA0000`, version `0x10300` (1.769 → AHCI 1.3 rev), NP=1 NCS=32 ISS=3 (6 Gbps) SNCQ=1 S64A=1, GHC bit 31 set (AHCI enable), PI=1 → port 0 only. Port 0 DET=3 SPD=3 SIG=257 (SATA), CL @ 0x5B6800, FIS @ 0x5B7000. IDENTIFY: model `WD Blue SA510 2.5 2TB`, serial `24313QD00663`, fw `5304 00WD`, LBA48=3907029168 sectors (1907729 MiB ≈ 2 TB). LBA0 first 8 bytes `146 20 0 0 0 111 111 116` (real-disk content, not zeros — different surface than the NVMe). **LBA-5 write-then-read round-trip PASS** — full bidirectional DMA I/O working on real silicon. **§4 mitigation NOT applied** for this burn — the audit's compile-gated write-demo lever was deferred and the sentinel landed on the WD's GPT partition-entry array (entries 12-15, likely unused). Caveat captured for the attempt body. **Second IDENTIFY in `ahci_register_block_dev` timed out** (`PxCI stuck`) — registration silently bailed → AHCI did NOT register as a secondary block_dev. Boot continued cleanly: GPT parsed the NVMe (hdr-CRC-OK arr-CRC-OK, 2 partitions), VFS → kybernet → `AGNOS shell v1.31.1`. Second iron debut of the 1.31.x storage arc. |
| `attempt-86-usb-ms-phase-2-7-csw-signature-mismatch.jpg` | 2026-05-21 | **USB MS Phase 2.7 (agnos 1.31.2 close) → FALSIFIED on iron.** Same Silicon Motion / SMI stick as Attempts 83-85 (`VID=0x090C PID=0x1000`, USB 2.0 / HS) at xhci port. Phase 2.7's four-patch multi-source-converged Reset Recovery (FreeBSD + OpenBSD + EDK2 + Linux) executed *correctly* — visible in the transcript as three clean `msc: slot 2 Reset Recovery OK` lines following each `transport wedged` trigger — but every post-recovery TUR retry failed with **`msc: CSW signature mismatch`** before reaching INQUIRY. Closing line: `msc: slot 2 TEST UNIT READY -> not ready after 3 retries` → `msc: CBW transfer timeout` → `msc: 1 mass-storage device(s) detected` (probe returns 1 unconditionally so MVP boot continues). NVMe enumeration follows clean (Crucial P3, `CT2000P3SSD8`, `P9CR30A`) → AHCI/SATA bring-up. Eight-bug audit landed in `[1.31.3]` from this transcript — `XHCI_CMD_TIMEOUT_SPINS=10M` (~25-50ms wall) was being applied to bulk transfers and abandoning the INQUIRY data phase mid-flight; the ZLP-then-real-CSW pattern after Reset Recovery left `csw_phys[0..3]=0` so sig != `0x53425355` on every retry. Per `feedback_stop_letter_laddering`, escape plan written *before* the next iron burn, not after another falsification. |
| `attempt-87-usb-ms-phase-2-8-inquiry-tur-rc10-success.jpg` | 2026-05-21 | **🎯 USB Mass Storage iron success — agnos 1.31.3 Phase 2.8 eight-bug repair stack cleared the full SCSI chain on archaemenid.** Same Silicon Motion / SMI stick that wedged through Attempts 84-86 (`VID=2316 PID=4096` = `0x090C/0x1000`, HS) at xhci port 3 slot 2. Phase 2.8 fix list — `XHCI_BULK_TIMEOUT_SPINS=200M` (~1s wall, bulk-specific) + strict TRB-pointer matching + SHORT_PACKET residue check + `msc_scsi_exec` unified retry+recover wrapper + drain repositioned AFTER Stop Endpoint + Reset Endpoint CSE tolerance + `xhci_cmd_set_tr_dequeue` full 64-bit phys — all eight bugs released the data phase. Verbatim chain on iron: `msc: slot 2 BBB intf=0 bulk-IN=130 bulk-OUT=1 MPS(in/out)=512/512 MaxLUN=0` / `msc: slot 2 INQUIRY: vendor='General' product='USB Flash Disk' rev='1100' type=block` / `msc: slot 2 TEST UNIT READY -> ready (Pass)` / `msc: slot 2 READ CAPACITY: last_lba=252051455 blk=512B -> 123072 MiB` (252,051,456 LBAs × 512 B ≈ 120 GiB) / `msc: 1 mass-storage device(s) detected`. NVMe + AHCI continue clean below. **Third storage-class iron debut closes** (after NVMe @ Attempt 80, SATA @ Attempt 81) — 1.31.3 cut on iron evidence; cycle moves to **1.31.4 RAM-disk + VirtIO-blk modern**. |
| `attempt-88-agnos-1.31.4-iron-debut-pt1-xhci-usb-ms.jpg` | 2026-05-21 PM | **agnos 1.31.4 iron debut — part 1/2** (cycle that landed RAM-disk + VirtIO 1.x modern, both paravirt/RAM-only by construction so the iron question is regression-only). Part 1 captures memory init → ACPI → PCI → amdvi → xhci enumeration → USB-MS INQUIRY/TUR/RC10. xhci heap addresses (`scratchpad ready, array=7114752`, `ERDP=7131136`) are visibly higher than Attempt 87's (`3383296`/`3399680`) confirming the larger 1.31.4 binary. Same Silicon Motion stick as Attempts 83-87 enumerates clean through Phase 2.8 (`INQUIRY: vendor='General' product='USB Flash Disk' rev='1100' type=block` / `TEST UNIT READY -> ready (Pass)` / `READ CAPACITY: last_lba=252051455 blk=512B -> 123072 MiB`). Phase 2.8 eight-bug stack holds across the 1.31.4 build. Top three lines (Page_tables / PMM / KMSLR) are camera-motion-blurred but the meaningful content starts at `PMM test: 3583 free`. |
| `attempt-88-agnos-1.31.4-iron-debut-pt2-nvme-ahci-gpt-vfs.jpg` | 2026-05-21 PM | **agnos 1.31.4 iron debut — part 2/2.** Continuation from pt1 — NVMe full enumeration (`model='CT2000P3SSD8' serial='2342E880DED6' firmware='P9CR30A' ns1 NSZE=3907029168 LBAS=512B size=1907729MB`, LBA-0 read = 8 zeros, registered as block_dev) → AHCI port 0 spin-up (`port 0 model='WD Blue SA510 2.5 2TB' serial='24313QD00663' fw='5304 00WD' LBA48=3907029168 sectors (1907729 MiB)`, two IDENTIFYs both complete — the Attempt-82 quiescence-gate carry-forward keeps holding, registered as secondary) → MSC tertiary (`registered as tertiary block_dev (slot 2, 252051456 LBAs x 512B; NVMe primary)`) → GPT (`hdr-CRC-OK arr-CRC-OK`, 2 partitions parsed — ESP + Linux root) → VFS / SYSCALL / canary / IRQ / `Timer ticks before sched: 6`. Full storage trio registered cleanly, kernel reaches scheduler bring-up. **No-regression burn PASS** — 1.31.4's RAM-disk + VirtIO changes did not break NVMe / AHCI / USB-MS bring-up. RAM-disk compiled out (no `RAMDISK_ENABLE=1` build flag); VirtIO has nothing to probe on bare metal. |
| `attempt-89-agnos-1.31.5-ext2-no-regression-pre-bite-g-pt1-xhci-usb-ms-nvme.jpg` | 2026-05-21 evening | **agnos 1.31.5 interim no-regression burn — part 1/2** (the smoke burn the PRE entry called out as the cheaper question to answer first). Part 1 captures xhci / HID / USB-MS / NVMe bring-up. Storage trio byte-matches Attempt 88: Logitech (`VID=1452 PID=591`) + Silicon Motion stick (`VID=2316 PID=4096`) + Crucial P3 (`CT2000P3SSD8` / `P9CR30A` / `NSZE=3907029168`). The +47 KB ext2/ext4 Phase 1-4 code drop (build → 568,960 B) did not regress any of the three. |
| `attempt-89-agnos-1.31.5-ext2-no-regression-pre-bite-g-pt2-ahci-gpt-3parts-vfs-kybernet.jpg` | 2026-05-21 evening | **agnos 1.31.5 interim no-regression burn — part 2/2.** AHCI WD Blue SA510 byte-matches Attempt 88; new NVMe carve enumerates as 3 active partitions (ESP / unknown / **(unknown type) agnos-fs** at LBA 3898638336-3907026943, 4096 MiB). p2 shrunk by 4 GiB online for the new p3 (Linux-FS GUID `0FC63DAF-...` / `mkfs.ext4 -O extents,^huge_file,^64bit,...`, on-disk incompat `0x242` inside mask `0x6746`). VFS → kybernet kicks off. **No `ext2: mounted` line** — `blk_active = BLK_NVME` makes `ext2_init` read whole-disk LBA 2-3 = btrfs interior, magic mismatch, silent return. Exactly the predicted miss; bite (G) multi-backend probe is the 1.31.6 unlock. p3 prints `(unknown type) agnos-fs` because of the one-byte GUID-classifier typo at `gpt.cyr:214` (smoke-surfaced; fix landed in 1.31.6). |
| `attempt-90-agnos-1.31.6-ext4-victory-lap-pt1-xhci-usb-ms-nvme.jpg` | 2026-05-22 | **agnos 1.31.6 ext4 victory lap — part 1/3** (Phase 4 payoff burn the whole 1.31.x storage arc was building toward). Part 1 captures xhci / HID / USB-MS / NVMe bring-up. Storage trio byte-matches Attempt 89 (build → 571,296 B, +2,336 B vs 1.31.5; cyrius 6.0.1). NVMe `agnos-fs` p3 unchanged from Attempt 89. |
| `attempt-90-agnos-1.31.6-ext4-victory-lap-pt2-ahci-gpt-ext4-mounted.jpg` | 2026-05-22 | **agnos 1.31.6 ext4 victory lap — part 2/3.** AHCI WD Blue SA510 byte-matches Attempt 89; GPT enumeration now prints `[2] Linux FS agnos-fs LBA 3898638336-3907026943 (4096 MiB)` (the `gpt.cyr:214` GUID-classifier typo fix landed in 1.31.6 — iron-validated). Bite (G) multi-backend probe fires: `ext2: probe matched backend=2 partition_lba=3898638336` (NVMe-primary policy wins per priority order). Bite (H) partition-aware mount: `ext2: mounted (blocksize=4096, inode_size=256, inodes_per_group=8192)` — real ext4 (extents incompat) mounted from real Linux-FS partition on iron NAND through the FreeBSD-shape extent leaf walker. |
| `attempt-90-agnos-1.31.6-ext4-victory-lap-pt3-shell-ls-cat-help.jpg` | 2026-05-22 | **agnos 1.31.6 ext4 victory lap — part 3/3** (shell session against the live ext4 mount). `agnos> ls` → `./ ../ lost+found/ hello.txt` (four entries byte-exact from the on-disk dirent walk via `ext2_path_lookup("/", 1)` → root inode 2 → `ext2_print_dir(2)` → extent walker → real ext4 data block). `help` enumerates 18 verbs. Known papercut captured in body: bare-name `cat hello.txt` falls through to initrd lookup and returns `file not found` because Phase-3 ext2 fast-path requires leading `/` — fix queued as 1.31.7 bite B. **Storage arc closes here**: five iron debuts (NVMe @ 80 / SATA @ 81 / USB-MS @ 87 / RAM-disk+VirtIO @ 88 / ext4 @ 90) plus four no-regression burns across the 1.31.0 → 1.31.6 trajectory (+150 KB / ~6,500 LOC). |
| `attempt-91-agnos-1.31.7-ext4-64bit-shell-ux-pt1-xhci-usb-ms-nvme-ahci.jpg` | 2026-05-22 | **agnos 1.31.7 ext4 64BIT + shell-UX victory lap — part 1/4** (Phase 5 BGDT-stride-aware mount + new shell verbs cycle-close burn). Part 1 captures xhci scratchpad bring-up + HID + USB-MS + NVMe + AHCI (build → 578,432 B, +7,136 B / ~260 LOC vs 1.31.6). NVMe + USB-MS byte-match Attempt 90; AHCI re-prints full per-port detail (`NP=1 NCS=32 ISS=3`, `GHC=2147483648 PI=1`, `port 0 DET=3 SPD=3 SIG=257`). |
| `attempt-91-agnos-1.31.7-ext4-64bit-shell-ux-pt2-ahci-gpt-ext4-mounted.jpg` | 2026-05-22 | **agnos 1.31.7 ext4 64BIT + shell-UX victory lap — part 2/4.** NVMe `agnos-fs` p3 re-carved with default `mkfs.ext4` (drops `-O ^64bit`, so on-disk `s_feature_incompat` now sets bit `0x80` = `EXT4_FEATURE_INCOMPAT_64BIT`). New `ext2_init` parse block reads `s_desc_size`=64; `ext2_get_inode` now strides BGDT entries by 64 bytes. **Bite (A) ext4 64BIT (Phase 5) IRON-VALIDATED** — `ext2: mounted (blocksize=4096, inode_size=256, inodes_per_group=8192)` with the 64BIT-incompat-bit-set partition; supported_incompat mask `0x67C6` accepted cleanly; `bg_inode_table_hi` guard didn't fire (high block# stays zero on 4 GiB partition, as expected). Kernel walks to `AGNOS shell v1.31.7`. **Closes row 7b of agnos roadmap.** |
| `attempt-91-agnos-1.31.7-ext4-64bit-shell-ux-pt3-shell-cd-ls.jpg` | 2026-05-22 | **agnos 1.31.7 ext4 64BIT + shell-UX victory lap — part 3/4** (bites C + D — `cd` + CWD scoping). `agnos> cd lost+found` → `ls` returns `./ ../` (empty mke2fs-reserved subdir); `agnos> cd` (bare) returns to root; `ls` returns full root listing. CWD-relative `ls` consumes `sh_cwd_inode`, `cd ..`-via-`cd` shortcut works, `sh_is_dir` type-check accepts directory inode. Multi-component CWD traversal validated end-to-end on real ext4. |
| `attempt-91-agnos-1.31.7-ext4-64bit-shell-ux-pt4-cat-validated.jpg` | 2026-05-22 | **agnos 1.31.7 ext4 64BIT + shell-UX victory lap — part 4/4** (bite B — bare-name `cat` ext2 fall-through closes the Phase-3 papercut from Attempt 90). `agnos> cat hello.txt` returns byte-exact `agnos 1.31.7 iron Attempt 91   ext4 64BIT validated   2026-05-22` (the seeded file content). Bare-name path now prefixes `sh_cwd_path` + `/` + filename before ext2 lookup. At Attempt 90 the identical command returned `file not found`. **1.31.7 cycle closes here** — ext4 64BIT pin closed; three Phase-3 shell-UX papercuts closed; one no-regression iron burn validated the full 1.31.6 storage stack + new verbs in a single shot. |
| `attempt-92-agnos-1.32.0-r8169-iron-debut-dhcp-silent-pt1-xhci-usb-ms-r8169-nvme.jpg` | 2026-05-22 | **agnos 1.32.0 r8169 NIC iron debut — part 1/2** (first real-iron NIC burn on archaemenid). Build → 600,520 B (`TCP_LISTEN_SMOKE=1`, +22,088 B / ~770 LOC vs 1.31.7; cyrius 6.0.1). Six expected `r8169:` lines print verbatim from the convergent-port (RTL8111H datasheet + Linux + FreeBSD/NetBSD/OpenBSD + Haiku) Phase 1-4 stack: `found at 4243603456` (= `0xFCF04000` byte-identical to lspci BAR2); `MAC=176:65:111:12:228:37` (= `b0:41:6f:0c:e4:25` byte-identical to lspci); `chip-rev byte=0x87`; `reset OK; Phase 1 complete`; `RX ring up (16 desc  2KB bu...`; `TX ring up (16 desc  2KB bu...`. Storage trio + ext4 mount byte-match Attempt 91. |
| `attempt-92-agnos-1.32.0-r8169-iron-debut-dhcp-silent-pt2-nvme-ahci-gpt-ext4-mounted.jpg` | 2026-05-22 | **agnos 1.32.0 r8169 NIC iron debut — part 2/2.** NVMe + AHCI + GPT + ext4 mount byte-match Attempt 91. Kernel reaches `Interrupts enabled` cleanly. **DHCP block ABSENT** (expected: `dhcp: DISCOVER` / `OFFER` / `REQUEST` / `ACK` — observed: none). Post-burn audit found root cause one layer upstream of any wire/driver question: `main.cyr:655` DHCP gate keys on `vnet_active`, not the abstracted `nic_ready()` — so on iron (virtio-net absent), `dhcp_init()` is never invoked. One-line fix landed same-day (`vnet_active != 0 || nic_ready() != 0`, per user "if or" direction — generic abstraction, not r8169-specific, so future i225-V / Wi-Fi backends don't re-edit the gate). Pre-burn audit's H1/H7/H8 driver hypotheses **not yet reachable** — call-site gate intercepted before any wire activity. H3 (MAC garbage) falsified by the byte-matched MAC print. |
| `attempt-93-agnos-1.32.0-dhcp-gate-fix-verified-offer-timeout.jpg` | 2026-05-22 | **agnos 1.32.0 DHCP gate-fix re-burn — gate VERIFIED, new failure mode `dhcp: OFFER timeout`.** Boot-tail crop: AHCI → GPT → ext4 mount → `Interrupts enabled` → **`dhcp: DISCOVER`** (new — gate fix worked) → **`dhcp: OFFER timeout`** (new — driver-level surface now reachable) → `tcp_listen smoke` block (start / listen / no-connection within timeout / done — correlated, not independent: no DHCP lease → no LAN-reachable IP → external curl couldn't have reached :8080 either) → kybernet → `AGNOS shell v1.32.0`. Build delta vs Attempt 92: **zero structural change** (same `601,392 B` binary that already incorporated the one-line gate fix). Discriminator from Attempt 92's pre-stated rubric fires per spec: presence of `dhcp: DISCOVER` alone proves the gate was the issue. H1 (PHY not configured) / H7 (TX OWN stuck) / H8 (RX OWN stuck) all NOW REACHABLE — next-cycle audit needs to disambiguate which one is firing (zero post-DISCOVER instrumentation currently). Storage trio + ext4 mount + kybernet + shell all byte-clean — no regressions from Attempt 91/92. |
| `attempt-94-agnos-1.32.1-phy-init-instrumentation-offer-timeout-persists.jpg` | 2026-05-22 | **agnos 1.32.1 PHY init + CMOS discriminator instrumentation iron burn — r8169 NOT the cause; H7+H8 directly falsified by CMOS readback.** Build → 603,784 B (`TCP_LISTEN_SMOKE=1`, +2,392 B / ~80 LOC vs 1.32.0). Bites: (B) CMOS-bank discriminator stamps at slots 0x58-0x5F in `r8169.cyr` + `read-boot-log.cyr`; (C) `r8169_phy_init` MDIO + autoneg poll (~85 LOC, claimed OpenBSD-converged). Boot-tail crop: MSC tertiary → GPT (3 parts) → ext2 mount → SYSCALL/SYSRET + canary + IRQ + scheduler → `dhcp: DISCOVER` → `dhcp: OFFER timeout` → `tcp_listen smoke` block (start / listen / no-connection / done) → kybernet → `AGNOS shell v1.32.1`. CMOS post-mortem: `[0x58]=0x01` probe-done; `[0x59]=0x02` autoneg timeout (**bite C bug — BMSR-latching-low false-negative**: IEEE 802.3 §22.2.4.2 defines BMSR bit 2 as latching-low, requires double-read; bite C reads once per poll → 301st read would have returned 1, and by the time DHCP DISCOVER goes out the link IS functional enough to egress frames); `[0x5A]=0x01` 1 TX send (DISCOVER built and queued); `[0x5B]=0xb0` TX desc OWN+FS+LS wrote-but-never-cleared (H7-partial); `[0x5D]=0x80` / `[0x5E]=0x00` RX OWN=1 with zero DMA (H8 fires). At write time the body claimed root cause moved UPSTREAM of the NIC (server reachability / `udp_recv_from` / OFFER-match); Attempt 95 evidence later inverts this — the gate is DOWNSTREAM of phy_init, in the TX/RX descriptor handshake. (The pre-burn audit and the same-day 6-FIX bundle that closed the 1.32.1 cycle both lean on this entry — see `[r8169-iron-burn-audit.md]` § 10 + `[dhcp-end-to-end-audit.md]` for FIX-bundle derivation.) |
| `attempt-95-agnos-1.32.1-post-fix-phy-up-tx-rx-still-wedged.jpg` | 2026-05-22 PM | **agnos 1.32.1 post-FIX iron-validation burn — FIX #3 verified on iron, FIX #1/2/4/5/6 do not unwedge the NIC; OFFER-timeout gate moves downstream of `r8169_phy_init`.** First iron burn after the 6-FIX bundle that closed the 1.32.1 cycle on QEMU evidence ([`iron-nuc-zen-log.md`](../iron-nuc-zen-log.md) Attempt 94 post-audit follow-through). Boot-tail crop only (no pt1 storage shot taken): GPT (3 partitions — ESP / unknown / Linux FS agnos-fs) → ext2 mount (`blocksize=4096`, `inode_size=256`) → Heap → SYSCALL/SYSRET → stack canary → interrupts → `Timer ticks before sched: 6` → `dhcp: DISCOVER` → `dhcp: OFFER timeout` → kybernet (`0 processes`, `3500 free pages`, `launching shell`) → `AGNOS shell v1.32.1 (type 'help')` → `agnos>` prompt. **No `tcp_listen smoke` lines** (vs Attempt 94, which had them) — production 604,096 B build, not the `TCP_LISTEN_SMOKE=1` 604,904 B variant; user noted "not getting all the messages as before" — explained by the build-flag difference, not a regression. **CMOS post-mortem signature (vs Attempt 94)**: `[0x59]=0x01` **PHY = LINK UP** (Attempt 94 was `0x02` autoneg-timeout false-negative from BMSR-latching-low; FIX #3 `r8169_phy_init` non-blocking kick **verified on iron**); `[0x5B]=0xb0` TX desc OWN+FS+LS wrote-but-never-cleared (unchanged from 94's H7-partial); `[0x5D]=0x80` / `[0x5E]=0x00` RX OWN=1 with zero DMA (unchanged from 94's H8). FIX #1 (MAC accessor) + FIX #2 (`net_init(0,0,0)` iron path) + FIX #4 (chaddr validation) + FIX #5 (timeout 200→800) + FIX #6 (RxConfig constant) all in the binary but **don't move the TX-wedged / RX-silent gate**. OFFER-timeout root cause now confirmed **downstream of phy_init** — next-cycle bite G triage target. Photo file name reflects user's phone-side label `1321_Last_Log_Failure` (1.32.1 + "last log failure" — the truncated FB-tail vs Attempt 94, which turned out to be benign build-flag drift). |
| `attempt-97-agnos-1.32.3-pt1-r8169-link-up-preserved-bios-rings-up-storage-clean.jpg` | 2026-05-23 ~14:30 PDT | **agnos 1.32.3 r8169 RX-path 5-part bundle — part 1/2** (NIC init + storage trio block). Build → 617,000 B production (cyrius 6.0.1 / gnoboot 0.4.2 unchanged), agnos commit `a065f45 "another repair bundle for rx"`. Boot block prints `r8169: found at 4243603456` (BAR2 0xFCF04000 byte-match lspci), `MAC=176:65:111:12:228:37` (b0:41:6f:0c:e4:25 byte-match), `chip-rev byte=0x87`, `reset OK`, **`PHY link up (preserved from BIOS)`** (FIX #10 safe-path branch fired), `link up`, `Phase 1 complete`, `RX ring up (16 desc 2KB bu`, `TX ring up (16 desc 2KB bu`. Storage trio + GPT + ext2 mount byte-clean. Bundle = Part A multi-frame budget loop + Part B `RES`/`FS\|LS` gating + Part C `r8169_rx_rearm` w/ EOR read-preserve + Part D state-transition CMOS stamps + Part E `RxMaxSize` 0x05F3 → 0x4000. See iron-nuc-zen-log § Attempt 97 for full evidence + Linux/BSD line cites. |
| `attempt-97-agnos-1.32.3-pt2-rx-multi-frame-loop-16-frames-offer-timeout-persists.jpg` | 2026-05-23 ~14:30 PDT | **agnos 1.32.3 r8169 RX-path 5-part bundle — part 2/2** (shell prompt + DHCP `OFFER timeout`). `AGNOS shell v1.32.3 (type 'help')` reached, kybernet up. DHCP block: `dhcp: DISCOVER` → `dhcp: OFFER timeout`. CMOS post-mortem: `[0x5A]=0x02` (DISCOVER + FIX #9 retransmit, no REQUEST → OFFER never matched in `dhcp_init`), `[0x5B]=0x30` TX desc healthy, `[0x5C]=0x10` 16 frames consumed (Part A + Part D landed), `[0x5D]=0x78` EOR+FS+LS+MAR (Part B + Part C landed), `[0x5E]=0x01` IPv4 multicast first byte — **all 16 consumed frames were multicast, no broadcast/unicast OFFER admitted**. 5-part bundle mechanically validated but root cause moves UPSTREAM of `r8169_poll` (chip-level RX filter or server same-MAC-lease silence). PARTIAL verdict — drove the post-Attempt-97 multi-source convergent audit that identified RxConfig profile mismatch. |
| `attempt-98-agnos-1.32.3-rxcfg-cf00-offer-timeout-persists.jpg` | 2026-05-23 15:56 PDT | **agnos 1.32.3 RxConfig `0xE700 → 0xCF00` chip-rev fix (Linux v7.0 mac_version 46 / RTL8168h) — FALSIFIED on iron.** Production build (617,000 B, no `TCP_LISTEN_SMOKE=1` → no smoke block in FB), agnos commit `1e9d26a "burn ready"` @ 15:41 PDT, kernel mtime 15:37:35 PDT. Boot-tail crop: AHCI port 0 (`WD Blue SA510 2.5 2TB` 1907729 MiB) → MSC tertiary (`252051456 LBAs × 512B`) → GPT (3 active / 128 reserved: ESP / unknown / Linux FS agnos-fs LBA 3898638336-3907026943, 4096 MiB) → VFS → ext2 probe matched backend=2 → ext2 mounted (blocksize=4096, inode_size=256) → Heap → SYSCALL/SYSRET → canary → interrupts → `Timer ticks before sched: 6` → `dhcp: DISCOVER` → `dhcp: OFFER timeout` → kybernet (`0 procs`, `3500 free pages`) → `AGNOS shell v1.32.3`. The one-constant change (`R8169_RXCFG_DEFAULTS = 0xE700` → `0xCF00`) **did not unblock OFFER** — Early-RX-OFF flag landing didn't help, falsifying the high-confidence multi-source convergent hypothesis on the first iron burn. See iron-nuc-zen-log § Attempt 98 for the falsification receipt + escalation to Attempt 99 (`ab913aa "more rx fixes"`) and the Attempt 100 BSD/iPXE rewrite (post-Linux-clone reckoning per user direction). |
| `attempt-100-agnos-1.32.3-bsd-ipxe-rewrite-broadcast-admitted-offer-still-times-out.jpg` | 2026-05-23 20:10 PDT | **agnos 1.32.3 BSD/iPXE-shape r8169 rewrite — PARTIAL on iron (chip-level RX filter unblocked but OFFER still doesn't reach `dhcp_init`).** Build = `TCP_LISTEN_SMOKE=1` variant 617,984 B (matches Attempt 100 prep rubric exactly — `build/agnos` mtime 19:17 PDT, `r8169.cyr` mtime 19:14:38 PDT), code change = **−343 LOC net** (deleted `r8169_hw_start_8168h_1` 250-LOC Linux MCU body + Cfg9346 envelope + `mac_ocp_*` / `ephy_*` / `eri_*` helpers; rewrote `r8169_probe` post-reset to iPXE 14-LOC shape + `r8169_init_tx` tail to single store32 BEFORE `CR=TE\|RE`; `R8169_RXCFG_DEFAULTS = 0xCF00 → 0xEF00` per BSD 8168G_PLUS with EARLYOFFV2). Multi-source convergent (iPXE / FreeBSD / OpenBSD / NetBSD / RTL datasheet / Linux erratum patch) post-user-pushback against the Linux-clone audit lineage. Boot-tail crop: GPT (3 active) → VFS → ext2 mounted → Heap → SYSCALL/SYSRET → canary → interrupts → `Timer ticks before sched: 6` → `dhcp: DISCOVER` → `dhcp: OFFER timeout` → **`tcp_listen smoke: start` / `tcp_listen(8080) lid=0` / `tcp_listen smoke: no connection within timeout` / `tcp_listen smoke: done`** → kybernet → `AGNOS shell v1.32.3`. CMOS readback at [`attempt-100-cmos-readback.txt`](attempt-100-cmos-readback.txt): `[0x5A]=0x03` (≥ prep PASS target — 3 TX sends), `[0x5B]=0x30` healthy, `[0x5C]=0x10` 16 frames consumed, `[0x5D]=0x72` EOR+FS+LS+BAR (**BAR bit set = broadcast frame received** vs Attempt 97's `0x78` MAR-multicast), `[0x5E]=0xff` **broadcast first byte ≡ prep PASS target — chip ADMITTED a broadcast frame for the first time across the entire 1.32.x DHCP arc**. Wire-side filter unblocked; the OFFER-timeout symptom now lives strictly DOWNSTREAM of `r8169_poll` (in DHCP matcher, `udp_recv_from` routing, OR — alternative — the broadcast frame admitted wasn't actually the DHCP OFFER but some other LAN broadcast traffic such as ARP/NetBIOS/mDNS). See iron-nuc-zen-log § Attempt 100 for full evidence + next-step disambiguation rubric. |
| `attempt-100-cmos-readback.txt` | 2026-05-23 19:53 PDT | CMOS post-mortem (`scripts/read-boot-log.sh --verbose`) captured between Attempt 100 burn and the boot-tail photo at 20:10. Full slot decode for 0x50-0x87 with semantic cheat-sheet (`gnoboot magic / kernel checkpoint / CR4 / PMM scan / r8169 1.32.3 state-transition slots / xhci xECP walk / page-table walks / scratchpad / DNCTRL / extended-CMOS bank sentinel / event-ring drain`). Sibling artifact to `attempt-100-…broadcast-admitted-offer-still-times-out.jpg` — same burn. **Note**: trailing `verdict: gnoboot never launched (CMOS[0x53] != 0xCD)` line at the bottom is a stale heuristic in `read-boot-log.sh`; the boot clearly succeeded (shell prompt reached in companion photo). The verdict checks for `0xCD` magic but archaemenid's gnoboot stamps a different sentinel — non-load-bearing decode bug, queued for a future read-boot-log cleanup. |
| `attempt-101-agnos-1.32.4-arp-timeout-bug-below-dhcp.jpg` | 2026-05-23 ~22:27 PDT | **agnos 1.32.4 ARP-probe diagnostic — gate disambiguates Attempt 100's (c1) vs (c2) split decisively toward (c1): bug is BELOW DHCP, in the L1/L2/L3 NIC/eth/IP/UDP layer**, NOT in the `dhcp_init` OFFER matcher. New diagnostic landed in commit `9b07cb2 "arp request fire"`: instead of waiting for a DHCP OFFER that may never arrive, the kernel now (a) installs a STATIC IP + gateway directly (`net: STATIC ip=192.168.1.222 gw=192.168.1.1`), then (b) emits a raw ARP request to the gateway and (c) gates an L1/L2/L3 PASS/FAIL verdict on receipt of the ARP reply. Boot-tail crop: AHCI port 0 (`WD Blue SA510 2.5 2TB` 1907729 MiB, LBA48=3907029168) → MSC tertiary (`slot 2, 252051456 LBAs × 512B`) → GPT (3 active / 128 reserved: ESP / unknown / Linux FS agnos-fs LBA 3898638336-3907026943, 4096 MiB) → VFS → ext2 probe matched backend=2 → ext2 mounted (`blocksize=4096, inode_size=256, inodes_per_group=8192`) → Heap → SYSCALL/SYSRET → stack canary → interrupts → `Timer ticks before sched: 6` → `Activating scheduler...` → **`net: STATIC ip=192.168.1.222 gw=192.168.1.1`** → **`arp: request 192.168.1.1`** → **`arp: TIMEOUT  gateway did not reply within ~`** → **`net: L1/L2/L3 FAILED  bug is BELOW DHCP (NIC/eth/IP/U[DP])`** → `Launching kybernet...` → `kybernet: starting init` → `kybernet: 0 processes` → `kybernet: 3501 free pages` → `kybernet: launching shell` → `AGNOS shell v1.32.4 (type 'help')` → `agnos> ` prompt. **Interpretation**: at Attempt 100 the chip admitted *a* broadcast frame (CMOS 0x5E=0xff) but OFFER timed out — two candidates were (c1) admitted broadcast wasn't the DHCP OFFER, (c2) OFFER matched but lost in `dhcp_init`/`udp_recv_from`. The ARP probe bypasses DHCP entirely — direct L2 broadcast (`ff:ff:ff:ff:ff:ff` ARP-who-has) to the same gateway that's known-reachable from the Linux session on the same MAC. ARP timeout means our TX frames either aren't reaching the wire OR the chip isn't receiving the gateway's ARP reply on the unicast/broadcast return path. The L2 layer itself is the gate, NOT a DHCP-state-machine bug. (c2) falsified; root cause is strictly in the NIC TX/RX wire path — RxConfig accept-bits letting one broadcast through doesn't mean the chip's TX is actually transmitting, or that subsequent RX (unicast ARP reply) lands. Storage trio + GPT + ext2 mount byte-clean — no regression from Attempt 100. See iron-nuc-zen-log § Attempt 101 for the disambiguation receipt + next-step audit candidates (TX descriptor wire-egress confirmation via switch port-stats or `tcpdump -i enp1s0 arp` from Linux session while burn runs). |
| `attempt-102-agnos-1.32.4-pt1-laa-mac-b2-r8169-nvme-ahci-up.jpg` | 2026-05-24 ~15:51 PDT | **agnos 1.32.4 sovereign-MAC (LAA) build — boot-log page 1 (device enumeration).** Shows the LAA override ACTIVE: `r8169: MAC=178:65:111:12:228:37` = `b2:41:6f:0c:e4:25` (U/L bit flipped on byte 0). xhci controller running (port 1 FS keyboard VID=1452, port 3 HS VID=2316) → hid keyboard configured → msc `USB Flash Disk` (READ CAPACITY last_lba=252051455 → 123072 MiB) → `r8169: found at 4243603456` + `chip-rev byte=0x87` + `reset OK` + `PHY link up (preserved from BIOS)` + RX/TX rings up → **`nvme: model='CT2000P3SSD8'` (Crucial P3 2TB) now enumerating in the NVMe slot post drive-swap**, ns1 registered as block_dev → `ahci: found`. Confirms the drive reshuffle (AGNOS boot drive → internal NVMe slot, Linux → SATA WD Blue) enumerated with **zero hardcoded-drive assumptions**. Sibling of pt2. See iron-nuc-zen-log § Attempt 102. |
| `attempt-102-agnos-1.32.4-pt2-arp-timeout-laa-build-isolated-to-rx.jpg` | 2026-05-24 ~15:51 PDT | **agnos 1.32.4 LAA build — boot-log page 2 — FALSIFIED.** `ahci: port 0 model='WD Blue SA510 2.5 2TB'` (SATA, now Linux's drive) registered secondary block_dev → `gpt: present` (1 active: `[0] EFI System AGNOS-BOOT LBA 2048-524287 255 MiB`) → VFS init → Heap → SYSCALL/SYSRET → stack canary → interrupts → `Timer ticks before sched: 6` → Activating scheduler → **`net: STATIC ip=192.168.1.222 gw=192.168.1.1`** → **`arp: request -> 192.168.1.1`** → **`arp: TIMEOUT -- gateway did not reply within ~5s`** → **`net: L1/L2 FAILED -- cannot reach gateway, skipping L3 test`** → kybernet (3511 free pages) → `AGNOS shell v1.32.4`. The LAA `b2` build still ARP-times-out. **Post-burn (zero additional burn)**, two Cyrius AF_PACKET probes proved AGNOS's frame construction wire-correct — `arp-probe-raw` drew `gateway 192.168.1.1 is at d4:6a:91:ce:70:60` from the real `b0` MAC claiming `.222`, `dhcp-probe-raw` leased `.129` — isolating the bug to **r8169 RX delivery on iron**. The LAA override was then removed (`r8169.cyr:367`) for Attempt 103 (EEPROM `b0`, build 621,816 B). See iron-nuc-zen-log § Attempt 102. |
| `attempt-103-agnos-1.32.4-eeprom-b0-mac-arp-timeout-rx-isolated.jpg` | 2026-05-24 ~17:24 PDT | **agnos 1.32.4 EEPROM `b0` MAC (LAA override removed) — FALSIFIED.** Build 621,816 B, agnos HEAD `18a6fc4 "release cut"`, kernel mtime 16:20 PDT. With the LAA U/L-bit flip gone (`r8169.cyr:367`), the IDR reverts to the EEPROM-autoloaded `b0:41:6f:0c:e4:25` — byte-identical to the `arp-probe-raw` frame the gateway answered on Linux at Attempt 102. Boot-tail crop: AHCI `WD Blue SA510 2.5 2TB` registered secondary → GPT (2 active: `[0] EFI System AGNOS-BOOT` 255 MiB / `[1] Linux FS agnos-fs` 25600 MiB — the realized internal-install layout) → VFS → `ext2: probe matched backend=2 partition_lba=524288` → `ext2: mounted (blocksize=4096, inode_size=256)` → Heap → SYSCALL/SYSRET → canary → interrupts → `Timer ticks before sched: 6` → `Activating scheduler...` → **`net: STATIC ip=192.168.1.222 gw=192.168.1.1`** → **`arp: request -> 192.168.1.1`** → **`arp: TIMEOUT -- gateway did not reply within ~5s`** → **`net: L1/L2 FAILED -- cannot reach gateway, skipping L3 test`** → kybernet (`3511 free pages`) → `AGNOS shell v1.32.4`. **Exactly the pre-burn rubric's falsification branch**: ARP still times out with the EEPROM `b0` MAC ⇒ **filter MAC EXONERATED** (it now matches the proven probe), bug isolated **purely to r8169.cyr RX delivery on iron** — next move is MMIO / descriptor-DMA observability on the RX ring, NOT another construction/chip-init audit (that surface is closed). See iron-nuc-zen-log § Attempt 103. |
| `1326-agnos-1.32.6-arp-retransmit-rx15-arpin11-arpans2-unicast-drop-systematic.jpg` | 2026-05-25 ~02:40 PDT | **agnos 1.32.6 bite 1 (ARP-retransmit, RFC 1122 §2.3.2.1) — SYSTEMATIC unicast drop confirmed.** User phone-label `1326_LX2_Still_No_Gate`. The FB self-test now reads `net: L2 RX ALIVE rx=15 arp_in=11 arp_ans=2 -- gateway unicast reply pending` after a clean boot tail (AHCI `WD Blue SA510 2.5 2TB` → GPT 2-active ESP + Linux-FS `agnos-fs` → ext2 mounted `blocksize=4096 inode_size=256 inodes_per_group=8192` → Heap → SYSCALL/SYSRET → canary → interrupts → `Timer ticks before sched: 6` → `Activating scheduler...`). `net: STATIC ip=192.168.1.222 gw=192.168.1.1` → `arp: request -> 192.168.1.1` → `arp: gateway reply not seen within ~5s` → the `L2 RX ALIVE` line → kybernet (`0 processes`, `3511 free pages`, `launching shell`) → `AGNOS shell v1.32.6`. The 4×/sec ARP retransmit egressed (`arp_ans=2` = AGNOS answered 2 inbound who-has-`.222`) and the ring delivered 11 broadcast ARP frames (`arp_in=11`, `rx=15` total) — yet the gateway's *unicast* reply never cleared `arp_pending_ip`. **Per the bite-1 rubric this fires the SYSTEMATIC branch: transient-elicitation-miss RULED OUT — the unicast drop is a real, repeatable RX-class gap, not a one-shot timing fluke** → unblocks bite 2 (deep driver APM re-derive). Storage trio + GPT + ext2 + shell byte-clean (scope was the `main.cyr` ARP loop only). **Bite 2 then burned photoless** (user-reported FB line, no shot): `rtl_rar_set` IDR write-back reorder + clean post-`CR.RE` RxConfig write → `net: L2 RX ALIVE rx=15 arp_in=7 arp_ans=1 -- gateway unicast reply pending` → FALSIFIED (still `reply pending`). See iron-nuc-zen-log § [`#tracker-1326-cycle`](../iron-nuc-zen-log.md#tracker-1326-cycle). |
| `attempt-104-agnos-1.32.5-aap-rxcfg-arp-timeout-rx-downstream-of-l2-filter.jpg` | 2026-05-24 ~19:58 PDT | **agnos 1.32.5 AAP RxConfig (`0xEF0F`) + FS\|LS discard-gate removal — FALSIFIED.** Build 621,704 B (−112 B vs Attempt 103). RX changes: AAP added (`accept = AAP\|AB\|AM\|APM`) + unconditional `FS\|LS` discard gate removed from `r8169_poll`. Boot-tail crop: GPT (2 active: `[0] EFI System AGNOS-BOOT` 255 MiB / `[1] Linux FS agnos-fs` 25600 MiB) → VFS → `ext2: probe matched backend=2 partition_lba=524288` → `ext2: mounted (blocksize=4096, inode_size=256)` → Heap → SYSCALL/SYSRET → canary → interrupts → `Timer ticks before sched: 6` → `Activating scheduler...` → **`net: STATIC ip=192.168.1.222 gw=192.168.1.1`** → **`arp: request -> 192.168.1.1`** → **`arp: TIMEOUT -- gateway did not reply within ~5s`** → **`net: L1/L2 FAILED -- cannot reach gateway, skipping L3 test`** → kybernet (`0 processes`, `3511 free pages`, `launching shell`) → `AGNOS shell v1.32.5`. **The AAP bisector fired its falsification branch**: accept-mask + FS\|LS-gate removal still don't deliver the unicast reply ⇒ **L2 accept-mask layer EXONERATED**, bug is DOWNSTREAM in r8169 RX descriptor OWN/DMA/ring delivery. **Companion** second-machine pcap `1325_pcap_test.pcapng` (top-level) **RE-PROVES TX** (`b0 > ff:ff:ff who-has .1 tell .222` at 19:58:36, byte-identical to 1324) — but capture host `42:c2` is a regular switched port, so the gateway's unicast reply is invisible from that vantage (same Attempt-101 trap). See iron-nuc-zen-log § Attempt 104. |

Anticipated photo themes for 1.30.12+ (Attempt 72 forward):

- **Framebuffer refresh — before/after scroll perf fix** — bench output capturing visible refresh quality before and after the chunked block-copy rewrite of `fb_scroll_up`.
- **Pitch-padding right-edge check** — full-screen view to disambiguate whether `ppl > hres` is leaking stale firmware paint into the right column band.
- **VGA-spec vs Quiet Boot diff (Attempt 72 deliverable)** — two-boot diff under the new FB-handoff observability bundle. `attempt-72-vga-spec-baseline.jpg` + `attempt-72-quiet-boot-fail.jpg` photographed back-to-back, plus the `read-boot-log.sh` text capture for both CMOS post-mortems showing the GOP `mode#`, `w/h/pitch`, `pf`, `mode_max` values archaemenid's firmware exposed under each BIOS toggle. The actual diagnostic record is the two text dumps; the photos anchor the visual outcome to those numbers.
- **Glyph-to-font extraction** — pre-extraction baseline + first-render after externalizing CGA 8x8 glyphs from `fb_console.cyr` inline tables into a font-file format.

---

## Photo workflow conventions

- One-shot per attempt at attempt closeout. If exposure/focus is poor, add a `-reshot` sibling.
- Filename describes the attempt's signature (repair letter or symptom or milestone) so the catalog and log line up grep-wise.
- New photo lands → catalog entry follows in the same commit.
- Catalog is grouped by arc, not strictly chronological — easier to find "the photo of the Phase-3 closeout" than to scroll through 38 timestamps.
